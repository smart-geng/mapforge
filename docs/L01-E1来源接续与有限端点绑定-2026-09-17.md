# L01：E1来源接续与有限端点绑定

2026-09-17。接续[完整事件E1检查](L01-完整事件E1来源与端部准入-2026-09-17.md)；属于v0.6完整分合流事件设计准备，不是修复地图。主方案v1.74和分开验收口径不变。

## 交付结果与边界

原39项未分配来源区间均已建立可回放的对应关系：35项对应实际XML断面区间，4项保留为有限端点观测。**来源约束可进入整段形状设计，不代表完整纵向覆盖、E1整体通过或形状合格。** 原结构账本及其39项历史记录没有改写。

问题在于上一检查只消费旧结构切分账本，没有接上已有原节点/TOPO接触层。该层早已记录37项原件接续、2项包外接触；本次从只读SHP重新编译核对，未用最近邻猜接续。35项中20项为物理边界、15项为通行路径。

实现位于`mapforge/repair_web/event_source_binding.py`，实跑入口为`scripts/bind_split_event_sources.py`，证据目录为`out/node4-event-source-binding-20260917/`。目录已有证据，不重复执行入口覆盖；回归测试可以重跑。

## 对应关系如何建立

- 按原要素ID、part、原始弧长和节点/TOPO证明接续；只接受已证明的普通连续关系，零宽出生不并入普通边界链。
- 按实际XML车道元数据及原左右边界关系选消费者，不按几何距离选邻道；每个原始片段保留全部顶点和两端，禁止外推、裁源。
- 35项区间的残差重新计算原片段与实际XML多项式极值，不信任旧报告数值。诊断按语义断面分区，**不是增加XODR短段**。
- 出生之前、父路起点之外的4项不得冒充已有纵向车道区间；绑定有限端点并保留完整原片段，其几何覆盖仍未证明。
- 九处已批准角色逐项重放；原通行路径与物理道路边界分开，不新增第十处决定。

## 四项有限端点观测

| 原要素 | 绑定类型 | 当前残差及处理 |
|---|---|---|
| boundary `2023041110483940807` | 出生点前，完整约1.85cm原片段→实际出生端点 | 最大3.057cm；不把未出生支线挂到母边界，不移动出生站位 |
| boundary `2023061914592890866` | 父路起点外，完整约4.46mm原片段→实际s=0端点 | 最大4.479mm；不外推参考轴 |
| lane `2023041111104128071` | 出生点前的独立通行路径观测 | 对车道中点约1.792m仅为诊断；沿用已批准物理边界/完整路径分离，不能称通过或改路径 |
| lane `2023061915005380083` | 父路起点外的独立路径观测 | 对车道中点约2.238mm仅为诊断；保留原路径身份与哈希 |

两个物理端点沿用已冻结接触策略`source_error_budget_m=0.35`，均在预算内；这只是端点约束登记条件，不是新设的视觉验收门槛。两个通行路径继续独立验收，不套物理中点等式。

## 仍然失败或未验收的部分

- 35个区间中，物理边界对实际XML的最大横向残差为**0.556834m**；路径对当前车道中点诊断最大0.485561m。它们不是形状通过结论。
- 上轮111连接路外缘尾段双向采样最大约**0.573245m**仍保留；接缝通过不能抹去这段偏差，采样数值不是连续Hausdorff证书。
- 冻结上游/口部、整段形状意图、误差与趋势、长区间表示仍需联合设计。原r6鼓包和旧动态FAIL未消失。
- `source_relation_status=BOUND`、`source_constraint_status=READY_FOR_SHAPE_DESIGN`仅指本次来源约束；`written_extent_status`明确不是完整区间覆盖，`map_accepted=false`、`export_allowed=false`。
- 无求解、无新XODR、无Web变动；本次未生成新形状，因此没有新地图截图或esmini验收。SHP/MAP整图均未完成。

## 验证与复现

新增15项测试覆盖原件接触重编译、完整区间/端点、九处角色回放、禁止母边界冒充、伪造原节点拒绝、实际XML变动影响残差、禁止优化器调用等。以下相关回归**83项通过（37.10秒）**，不是全仓库或地图验收：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_event_source_binding.py tests/test_split_event_admission.py tests/test_source_contacts.py tests/test_physical_continuations.py tests/test_source_roles.py tests/test_source_roles_real.py tests/test_source_transition_domains.py -q
```

证据`source-bindings.json`包含39份原始区间、证明、实际消费者与残差；`binding.json`冻结136项输入/代码/测试，`status.json`记录产物哈希。原r6文件SHA仍为`5d1cec2baea21200fbdcf78ca9267bfe7bb94e9cf441539d759370fffdaaea98`。

收尾复核：本轮136项、上一E1的127项、两个旧Web项目各47项绑定均无漂移，产物哈希一致，`git diff --check`通过。以上是磁盘绑定核验，不是浏览器操作或内存撤销链迁移。

## 唯一下一步

把这**35个实际区间＋4个有限端点观测**带入E2完整事件形状/表示设计，同时完成E1剩余锚点形状准入；不能把旧账本改为零未分配就当地图通过。

设计仍围绕road11约150m五物理边界和106–111依赖：明确各原件残差/整段趋势/形状极值、上游及口部冻结条件、长区间表示和独立通行路径。原0.75m研究护栏不是视觉合格标准；不得通过临时放宽阈值、短段加密、删除源片段或重跑旧R2绕过失败。E1/E2全部准入后才登记E3单次共同重建，E4才接独立Web事务。

若设计证明固定口部存在冲突，先列具体原件与条件再提L02范围变更；本次既未证明必须移动，也未授权移动。原件、限速、九处角色、旧FAIL和8765/8766不变，不commit/push。
