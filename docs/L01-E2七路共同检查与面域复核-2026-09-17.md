# L01 E2：七路共同检查与面域复核

2026-09-17，接续用户“继续”。**完成原件/形状检查接入、全图面域复核和主路记录级回读；没有完成修形、没有生成新XODR，也未进入共同优化。** 已批准的road11＋106–111范围不变，入口不再等待同一范围确认。

## 本轮实现

新增四个后端模块及一个真实数据执行脚本：

- `coupled_event_sources.py`：直接复用既有来源所有权和原件身份，将六路完整via与两端普通道路尾段接入同一状态。54个消费关系对应36个物理边界part、18个独立movement part；不是54个不同原要素。保留所有原顶点、原分配平面和弧长区间，不最近邻换线，不修改原件。
- `coupled_event_guards.py`：主路五边界按连续三次多项式求与原折线的横向误差极值、宽度极值；原研究形状护栏仍在原s100–201.7074107域单独检查。六路普通尾段0.35m与via指标分开，不能用路口内均值稀释尾段超限。19条主路movement、18个转向movement消费者继续独立记录，没有强行约束到道路边界中点。
- `coupled_event_surface.py`：按实际XML的车道类型、width/offset/section构建全图面域，同一快照替换七路、其他道路不变。可见铺装和driving车道分别计算；发现自交直接报告，不调用`buffer(0)`、凸包或自动填洞来掩盖。
- `coupled_event_layout.py`：35个width、9个laneOffset在内存中序列化后重新解析，按消费端堆叠顺序复算物理边界。**只是诊断XML片段，不是完整OpenDRIVE文件、XSD或esmini验收。**
- `scripts/check_coupled_event_guards.py`：统一运行旧图与代数测试状态的检查、两种面域分辨率、记录回读、原件叠图、哈希核验，拒绝覆盖本轮证据目录。

转向106的出口右边界有一个空的既有分配区间，完整原part仍由普通道路背景持有。没有制造零长道路或删掉该原件；空区间和后续所有权检查缺口在报告中显式记录。

## 真实文件结果：旧失败保留

输入仍是r6原文件，SHA256：`5d1cec2baea21200fbdcf78ca9267bfe7bb94e9cf441539d759370fffdaaea98`。本轮没有写新地图或覆盖用户图。

| 检查 | 实算结果 | 解释 |
| --- | --- | --- |
| road11完整原件对照区间最大横向差 | 0.749997701m | 在旧0.75m研究包络内，不是最终保真通过；26个检查子区间超过普通0.35m口径，数量不等于26处独立缺陷 |
| 边界3原目标s178 | 0.125363206m | 仍偏12.54cm，鼓包未修复 |
| 普通尾段超限 | 106左、107左/右、110右、111右，共5项 | 全部保留，不借via放宽 |
| 111右侧普通尾段最大采样误差 | 0.568563151m | 超0.35m；旧单点见证0.554103m及其他采样约0.573m仍有效，窗口/采样口径不同，不是精度改善 |
| 主路诊断片段堆叠回读最大差 | 2.45e−15m | 只说明当前序列化/回读一致，不说明形状合格 |

六路对解析曲线的显示/测距网格用二阶导数范数计算弦误差上界，实算不超过0.1mm；细网格**只用于检查，不写入OpenDRIVE原语**。浮点根求解/积分不是形式区间证明。普通尾段的旧端点投影分区暂时保留用于回归，不能称完整连续所有权认证或据此给PASS。

## 面域分开看，不能用铺装替代道路

在只读SHP路口轮廓内比较同一旧图：

| 采样步长 | 可见铺装未覆盖面积 | 仅driving车道未覆盖面积 |
| --- | --- | --- |
| 0.10m | 0.475572504m² | 99.418442832m² |
| 0.05m | 0.474015389m² | 99.419410478m² |

两档差分别约0.001557m²和0.000968m²，只是分辨率复核，不是收敛证明或验收阈值。

**99.419m²不能直接宣称为“缺失道路”**：原路口面不等于每一点都必须属于一条允许转向的driving车道。它说明可见铺装覆盖和可通行车道覆盖有明显区别，不能把补色当成拓扑/通行验收。还需逐域区分合法间隙、转向覆盖、路缘/岛及实际缺口；固定其他道路的旧问题也不能擅自纳入七路修改范围。

面域输入为492个实际车道section带。读取零宽生灭端点时，仅将约7.47e−15m的浮点尾差归零用于构网，并记录修正量；不改文件、不裁剪内部负宽、不放宽实际宽度门槛。极小浮点碎片/孔数量不能解释为道路断裂数量；路口之外普通道路面积也不能当作“多铺面积”。

已实际查看四格叠图：[原件/面域/主路鼓包/111尾段](../out/node4-coupled-event-guards-20260917/reference-source-surface.png)。下左仍见s约163–183的鼓包、下右仍见111入口尾段向外偏；上排可见铺装近似覆盖不能抵消这两处错误。图明确标注“unchanged r6 / NOT accepted”，不是新修复成果。

## 长区间和失败测试状态

主路5个既有短语义width记录仍存在：2个1.0543m、3个1.6089m。内存记录回读确认它们是同一长三次多项式的换原点表达，最大系数函数差约1.69e−16m，没有独立短段自由度。**本轮没有完整七路writer，更不能据此宣布最终短记录门禁豁免。** 新转向表示仍各五长原语/五横断面区间，没有逐点加段。

代数接口测试坐标没有优化：主路原件误差约7.079m、11项旧形状护栏失败、主路4个带自交/退化，参考末端最大闭合误差约8.49m。它只用来验证共同检查能拦住明显错误，不是候选、旧地图无损投影或适合交付的初值。本轮`optimizer_calls=0`、`new_xodr=false`、`trial_admitted=false`。

## 剩余工作，按单一事件收口

E2状态为`E2_GUARDS_CONNECTED_NOT_TRIAL_ADMITTED`。已经实现的来源目录、旧失败回放、全图面域计算和片段回读，不再作为下一轮从头盘点任务。

下一步是**剩余准入的实现**，不是继续调权重或重跑旧候选：

1. 将普通尾段由旧端点投影诊断落实为明确目标区间所有权；对有限原件与完整目标曲线建立误差上下界，交叉/折返不得通过最近点跳线规避。范围外完整原件继续保留，不能为达标而裁源。
2. 为s61.8028–211.7074107完整事件补齐来源趋势和整段形状约束，保留原域研究护栏但不误称用户质量标准；面域检查需分类判据，不能把整个原路口面直接设为driving填满目标。
3. 实现七路完整文件内存编译、记录/语义重表达、固定区域及拓扑/速度不变的独立回读、原子替换验收；片段回读不能代替该步骤。
4. 上述形成完整约束契约后，登记一份联合算法和不重置总预算，再运行一次七路共同候选。旧S2/R2不重开，不逐路拟合后拼装，不新增未经确认的编辑范围。

明确通行路径/速度工况的动态验收仍独立；原限速及旧动态FAIL保留。L01、MAP同等级支持、Web应用和整图验收尚未完成。

## 测试与证据

14项新增检查，连同共同状态/来源/接触/角色/过渡域等，共**143项相关测试通过（34.19秒）**；非全仓库、非地图验收。

证据目录：`out/node4-coupled-event-guards-20260917/`，包括旧图/代数状态检查、两档面域GeoJSON几何及数值、记录回读、四格图、状态与绑定。193项输入绑定，继承上一轮154项；两个旧Web项目各47项绑定保留。测试不访问旧页面内存撤销链，服务不重启、不覆盖用户状态。无commit/push，原件/九处角色/源速度/旧FAIL不改。
