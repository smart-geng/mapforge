# L01 E2：七路联动契约与长曲线状态

2026-09-17。接续用户对“主路与106–111六条关联转向纳入同一次共同修形、长曲线、先保持口部位置/原件/限速不变”的“继续”。**共同坐标和端部依赖已实装；形状准入、共同求解和地图修复尚未完成。** 本轮不是新的数值优化试验，不占用或重开旧S2/R2执行槽，不修改旧失败记录。

## 1. 本次范围与口部固定的具体含义

决策文件：`profiles/repair/node4-west-event-and-six-turns-v1.yaml`。共同处理road11的s61.8028–211.7074107五物理边界，以及106–111六条转向的长参考线和横断面。

- 固定road11参考轴、0–61.8028上游、两处车道出生站位、路口口部的五边界坐标和断面宽度；不改其他普通道路、ID/TOPO、原件、CRS、源限速及九处来源角色。
- **本次把“口部位置不变”落实为坐标和断面固定，而非继续锁死旧曲线的切向/曲率。** 主路端部的一阶/二阶状态成为五边界各自唯一的公共变量，由全部关联转向读取。本轮已向用户说明此解释；不能将其描述成用户提供了新的测绘切向或允许口部平移。
- 六条转向的另一端使用未修改的其他道路，世界位置/切向/曲率全部固定。
- 上一E2文档提案中的“口部位姿固定”和v0.6全端部锁定属于此前策略，当前实现以上述明确细化为准。若要移动坐标、出生站位或其他道路，需要另行明确范围，不从本次批准推导权限。

旧E2的五处固定点超限仍是真实历史失败。本次批准解除的是“六路几何不可变”的范围限制，不是把那五处误差标记为已消除。

## 2. 本轮代码完成到哪里

`mapforge/repair_web/coupled_event_contract.py`：

- 核对实际参考XML SHA、原始来源包/域/35＋4契约及既有角色决定；不允许修改来源SIDE后重新签名绕过当前冻结输入。
- 五条主路共享C2三次边界共52系数；扣除上游、出生及末端位置等式后32自由坐标。末端五边界的一阶/二阶共10个方向可独立表达；这只是控制能力，不是可行性证明。
- 原25边界/19通行路径、35个实际区间和4个有限端点观测继续保留。出生前的独立观测不再通过母边界别名冒充一个实际分支；本轮没有重做来源扫描或改角色。
- 24个固定口部边缘位置对完整同身份原SHP part求距，全部不超过既有0.35m，最大0.349993434m；两条进口边缘余量仅约6.6微米。这不是端部附近整区间的保真证明，更不是通行路径或动态验收。

`mapforge/repair_web/coupled_event_state.py`：

- 一份146维共同状态：主路32自由坐标，六条转向各19。父路先在同一快照求端部，六路用该快照派生，不分别拟合后再拼接。
- 每条转向限定五个长解析原语（三个核心回旋线加两端原语），每个≥6m；横断面只有对应五个三次区间，**没有旧核的半端段结点，也没有按源点逐点增加段数**。
- 横断面端部系数按公共端部状态代数消元；内部两边缘及车道中线的世界位置、方向、曲率接续、末端闭合、连续区间最小宽度和前向正则性均显式计算。三次横断面在参考曲率变化率跳点允许二阶补偿，不能把横断面C1误称为世界G2自动通过。
- 旧父路单独`solve/compile`以及新共同状态的`solve/compile`入口均拒绝调用；原件/整段形状/面域/实际XML布局准入与一次试验登记未完前不能生成候选。

这不是第一次拥有共同模型：早期整图模型已经存在。本轮落实的是**新批准七路范围、固定位置/共享导数的端部条件，以及不含半端段短width的转向表示**，不能宣传成已完成整图共同求解。

## 3. 真实数据接口试验，不是新地图

证据：`out/node4-coupled-event-contract-20260917/`。只生成JSON，没有XODR。

固定测试：将边界3末端一阶导数增加0.0001，位置、其余端部导数及六路独立坐标不动。

- 109/110/111三条转向的输入端部和相应横断面系数发生联动；106/107/108不变。
- 六路出口完整端部状态不变；实际公共导数变化与请求之差约1.1e−17。
- 六个测试坐标的参考/横断面均为五段，最短约7.036m；≥6m是现有研究表示底线，**不是自动驾驶质量标准**。
- 未调用源拟合、非线性优化或端点校正。测试坐标按继承总长等参数构造，仅用于接口响应；不是旧图的无损投影，也不是已可用的初始地图。其参考末端最大位置残差约8.49m，内部曲率接续仍有非零残差，全部保存在JSON中，没有隐去或写成PASS。
- 单独用合成直路验证全部接续残差为数值零；世界曲率另用坐标四阶差分独立核对。这些测试仅说明求值器行为正确，不代表真实七路形状通过。

原XML零操作返回原字节；没有新XSD/esmini结果，不得引用旧消费端PASS为本轮背书。

## 4. 短记录不能藏在“长曲线”说法里

主路提案使用10个既有语义站位，独立三次跨度最短8.3320107m，未实际改写文件。按保留laneSection预测写出，会出现35个width记录，其中5个仍很短：

- s130–131.0543：两条现有车道各1.0543m；
- s150–151.6089：三条现有车道各1.6089m。

这5个区间在旧文件已经存在，新提案没有新增对应短区间。它们来自出生前既有语义分段，而不是新自由结点；代数测试确认跨s130/s150只是**同一长三次多项式换原点表达**，没有独立短段的形状自由度。

但当前仍只是布局预测，没有实际写出/逐系数回读；不能自动豁免最终记录长度门禁，也不能把语义section随意删除以少计短记录。后续须同时报告独立区间、语义重表达及实际最短记录，明确实际消费者行为。这件事仍是E2写出布局准入的一部分。

## 5. 下一步按同一事件收敛，不再逐路补丁

当前状态为`SHARED_PORT_INTERFACE_NOT_SHAPE_ADMISSION`，不是停止等待本次范围批准：用户已批准，不重问。

下一步只完成**这个共同模型的完整可行性契约**：

1. 将已有25/19及35＋4来源对应、六路完整via和两端普通尾段按原身份接入；普通尾段0.35m与主路研究包络、via指标分别计算，movement单列，不能重新剪裁降低误差。
2. 在完整分出—横移—趋稳区域检查来源走廊、宽度、额外波动/反向量、世界曲率及原s178病例；原R2四项研究护栏仍在其原空间域检查，不被更大域总分稀释。完整面域/交叠及实际写出布局一并准入。
3. 上述通过后，登记**一份**完整结构、算法和总预算，再运行一次七路共同候选和实际XML同SHA复验；失败保留原因，不换目录重开预算。不能先逐条解六路再冻结拼装。

静态形状与明确通行路径/速度工况继续分开；原源限速和旧动态FAIL保留。MAP同等级修形、Web应用闭环和整图验收仍未完成。

## 6. 验证与保护

新增26项检查通过；连同来源绑定、固定依赖、E1、原接触/延续/角色、过渡域及端部系数消元，共**124项相关回归通过（28.36秒）**，不是全仓库或地图验收。

本轮154项源/代码/证据绑定；旧142项证据不改。旧8765/8766没有重启或写入，不操作未保存撤销记录。未commit/push，无新来源角色批准，无新地图发布。

结束核验：154项本轮绑定、142项上一轮绑定、两个旧Web项目各47项绑定逐一校验无漂移；3个JSON证据的产物哈希一致，目录内没有XODR，`git diff --check`通过。只核验磁盘文件，不将此说成读取或迁移了旧页面的内存撤销链。
