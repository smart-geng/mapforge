# L01：唯一目标检查与停止裁决（S2）

2026-09-15。**S2已执行一次，结果拒绝；没有新XODR，没有Web应用，原鼓包未修复。** 本文件记录失败的具体性质，不把接口或测试通过算作地图修好。

## 固定的输入与执行边界

- 承接S1契约`661e581edb2ed6fffb28125faddbb13044175ffd42ed7380bc5db5ecdbda259a`，r6原XML SHA仍为`5d1cec2baea21200fbdcf78ca9267bfe7bb94e9cf441539d759370fffdaaea98`。
- 同一road11、边界3、s157.6089–201.7074107；边界0/1/2/4、范围外、端口/参考轴/TOPO/源速度不变。
- s178目标取原边界`IBD_LANE_BOUNDARY:2023041110502726490` record0/part0在同一站位的坐标，所需横移−0.125363205696158m。不换源身份，不裁剪鼠标目标。
- 一维控制空间中位置方程确定唯一系数状态：没有剩余形状自由度，不运行优化器。既有曲率/变化率/总变差/反向量研究护栏均保留，未删除、未调整或升级为行业标准。
- 预登记在`profiles/repair/node4-l01-unique-local-target-v1.yaml`。单一执行槽`out/node4-unique-local-target-s2-20260915-r1/`在求值前写入STARTED；脚本拒绝重用或换目录重置该次预算。

## 实测结果

`evaluation.json`状态：`UNIQUE_TARGET_REJECTED`。仅一个非零目标、零优化调用、零迭代；检查约0.33秒（包括报告落盘的脚本阶段约0.48秒），未使用90秒/160次预算追加试验。

通过本次检查的部分：

- 精确位置误差约4.44e-16m。
- 原件整段最大误差0.749997700754m，仍在原L01的0.75m门槛内；并非整图保真通过。
- 未选中/范围外多项式变化计算为0；原事件等式残差约7.11e-14。
- 实际整段宽度无超出数值容差的负值：零宽出生处最小约−8.88e-16m，既有Bernstein宽度护栏也通过。没有改源宽度或移动相邻物理边界来补偿目标。

拒绝项均发生在**被选中的边界3**：

- 曲率峰值0.0560599606 → 0.0574643406 /m，增加约2.51%。峰值位置s163.6089。
- 曲率对世界弧长变化率峰值0.0178968166 → 0.0185008432 /m²，增加约3.38%。最坏位置同为s163.6089的一侧。
- 曲率总变差0.2988802587 → 0.3105387220 /m，增加约3.90%。
- 对原件方向的反向量0.5225742647 → 0.5439974216m，增加约4.10%。

以上四项是原有“相对r6不退步”的研究护栏，不是OpenDRIVE XSD条款，也不是新的动态安全结论。静态几何与明确工况动态分开验收的用户决策不变；没有修改源限速，没有把缺工况改为通过。

## 已看图核实的形状问题

图：`out/node4-unique-local-target-s2-20260915-r1/inspection/unique-target-review.png`。橙色为原SHP，蓝色为实际r6 XML，红色为被拒绝的系数状态。**红线不是写出的XODR，绘图采样也不是新增几何短段。**

已实际打开检查四个面板：所有物理边界、边界3放大、世界曲率及来源误差。邻线保持原形；选中边界的鼓包有所降低，但s167.628682858处仍偏离原水平边界约0.459597m，目标之后出现下凹。命中s178一个点并没有完成整段修形。

源误差的全域最大值出现在s157.011155151，位于本次编辑范围之前；它由冻结区保留，不能将本次最大值基本不变解释为“全部形状都未变化”。完整极值和源ID保留在`evaluation.json`与`inspection/inspection.json`。

此次没有通过全部局部检查，编译器未写出此状态，因此没有新XSD/esmini/实际XML修形验收。旧文件、旧消费者证据和旧FAIL全部保留；不以系数图自证地图合格。

## 代码、回归和留存

- `mapforge/repair_web/unique_edit.py`：S1绑定复核、一维精确位置、原件整段极值/宽度/真实曲率/研究护栏检查、条件性实际XML回读；拒绝状态不给XML。
- `scripts/check_unique_local_edit.py`：同一来源/契约重建核对，预登记单次执行槽、输入绑定及拒绝留存。
- `scripts/inspect_unique_edit.py`：只读已保存系数和实际参考XML叠图及极值位置，不重新选择候选。
- **68项相关测试通过**（18项S2、35项S1、9项结构诊断、6项世界形状）；不是整仓库/整图验收。新增测试覆盖唯一方程、不可控/非唯一/非法目标、基准及控制空间漂移、零操作原字节、超时拒绝、已保存裁决读回。
- S2的65源/代码绑定无漂移；原8765/8766各47绑定无漂移。九处来源角色、原件/CRS/原速度/ID/TOPO未改，未提交。

## 停止裁决与下一项设计选择

**停止当前“固定范围＋固定一维表示＋精确位置＋现有护栏”的试验。** 这不是数值未收敛：位置方程没有其他自由量可供优化；该唯一状态实际违反登记护栏。它也不是原件错误或一般几何问题无解的证明。

不重开r2，不再调整权重、种子、迭代数，不扩大到邻边，不降低源容差、不删除研究护栏来制造成功。不进入S3/Web。

建议下一步按v0.4备选做**保持原形的受控长区间增自由度设计评审**：仍锁住未选中边界和作用域，研究少量长结点插入是否能在原形精确保持、最小跨度≥6m和最终width复杂度受控的前提下，为位置之外提供形状控制。需要用户确认进入这项表示变更设计；本轮没有插结点、没有生成新结构，更不保证变更后一定能修好。

其他选择是明确变更编辑范围，或单独评审现有形状护栏的合理性；两者都不能由工具自动执行，也不能将取消护栏等同于修复鼓包。下一次“继续”应围绕已确认的设计选择，不自动重跑本失败目标。
