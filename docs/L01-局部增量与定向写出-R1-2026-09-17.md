# L01：局部增量与定向写出 R1 实施记录

2026-09-17。**R1表示/编译一致性验证已完成；真实修形目标未求解，原鼓包未修好。** 本轮生成的是明确标记的微扰测试文件，不是新的合格地图。

承接[保形长区间设计v0.5](L01-保形长区间增自由度设计评审-2026-09-17.md)，范围仍为road11边界3的s157.6089–201.7074107。唯一新增简单结点s194.60370535，邻边0/1/2/4、参考线、口部及范围外不动。边界3是两车道间的共享边界，不是最外侧边界4。

## 已实现

- `mapforge/repair_web/local_refinement.py`：固定SHA/源身份的`prepare_local_refinement`，两维锁端C2局部增量，原XML分段多项式基准，完整区间求值和定向writer。
- lane−3的width减去同一边界增量，lane−4的width加上它。其他物理边界直接保留原形；不调用旧公共结点编译器，不新增其他车道width/laneOffset记录。
- 零操作原字节回放；非零仅允许显式`R1_COMPILER_PROBE`用途和匹配的契约SHA。没有Web/CLI生产导出入口，报告始终`export_allowed=false / map_accepted=false`。
- 写出后用实际XML检查全部多项式系数、未选曲线、范围外、端部及内部C2、两邻车道宽度、完整源段、整个XML的ID/TOPO/速度/标线/来源等语义。未识别width扩展拒绝适配，不静默丢弃。
- 参考/源包/基矩阵等均为不可变字节绑定；旧输入、旧S2执行槽及8765/8766未修改。

`compile_probe`只用于本阶段编译验证，不是未来R2完整形状门禁。不能因为拿到它的XML，就将失败的修形状态推广为用户可应用或可发布的地图。

## 真实原件上的固定微扰实测

脚本`check_local_refinement_writer.py`，证据目录`out/node4-local-refinement-writer-r1-20260917/`。

基准SHA仍为`5d1cec2baea21200fbdcf78ca9267bfe7bb94e9cf441539d759370fffdaaea98`。只执行一次预先固定的系数微扰`[1e−6, −1e−6]`，**不是s178原件目标，也不是缩小目标后谎称修复**；零拟合、零优化、零真实目标候选。

实际落盘`probe-only.xodr`，SHA：

`b74b67fda3e373956a09df0e6b914f2463917e6f33259dc995b95489a4535f27`

同一SHA的结果：

- geometry +0、laneSection +0、laneOffset +0、width **+2**，只在两邻车道各增加一条；新切出的width区间各7.10370535m。
- 未选/范围外物理边界最大读回差约**1.36e−16m**；合成边界与写出XML全多项式最大差约**9.18e−16m**。
- 原件事件区间最大误差**0.749997700754m**，通过既有L01 0.75m来源检查；这不是整图保真结论。
- **OpenDRIVE 1.5M XSD PASS**；独立esmini子进程核对road11车道边缘/中心共**2,076个位置，PASS**，最大位置差约3.92e−14m。未验证整图路径、面域或动态安全。
- 90项输入/代码/证据绑定复核无漂移；包含旧8765/8766项目文件与各自47项绑定。

旧范围内2.6547m的语义切分记录仍在；本次没有消除全部旧短记录。不能将“没有新增短独立区间”说成“整图所有记录都很长”。

## 没有隐藏的失败

微扰测试件的边界3对来源方向的反向量为**0.522574990178m**，原r6为0.522574264697m，增加约7.25e−7m，超过原研究护栏的数值余量。该项明确保存在`shape-observations.json`及`status.json`，**不能放行成修形候选**。这不是原件位置误差，也不是新的行业标准。

`R1_WRITER_VERIFIED_NOT_REPAIR`只表示编译和独立读回正确。研究形状护栏失败与R1写出一致性是两件事；允许留下诊断测试件，不等于绕过R2形状门禁。

r6旧源限速压力报告保持原SHA `1c7cacc1f82f411575834e07500c390a25933c376817294f56d3822fe078c2aa`：**30 FAIL、20 BOUNDED**，本轮未重跑。明确通行工况动态仍未评估，绝对CRS未验证，原限速未改。没有接入旧Web、没有替换用户地图、没有提交。

## 回归

新增28项测试，连同原表示设计/S2/S1/结构/形状回归，**106项通过（18.12秒）**。命令：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_local_refinement.py tests/test_long_edit_refinement_review.py tests/test_unique_edit.py tests/test_edit_scope.py tests/test_outer_edit_structure_review.py tests/test_line_boundary_shape.py -q
```

覆盖零操作原字节、两个独立控制方向/正负组合、实际XML和XSD、C2有限支撑、SHA/源身份/错误用途、非法参数、恶意或误改的邻边/范围外/速度/路口/头部/短记录/NaN/扩展字段、源/宽度超限拒绝。测试明确禁止调用原求解器、公共compile或S2执行函数。不是全仓库测试，也不是地图验收。

## 下一步

进入设计中的**R2：新表示下同一完整目标的一次有界检查**。先登记新契约和唯一初值/算法/总预算，再在位置等式之外的一个剩余形状变量上共同检查完整来源、宽度、曲率/变化率/总变差和反向量；不换源目标、不扩范围、不增加第二个结点、不放宽旧阈值。

R2尚未登记或执行，本轮编译微扰不冒充R2。通过后才有真实修形候选，随后做同SHA消费端、原件叠图/近景人眼复核和独立Web事务闭环。SHP完整外缘、路口联动、MAP同等级支持、整图验收仍按L01→L02→L03→L04推进，不把R1当目标已完成。
