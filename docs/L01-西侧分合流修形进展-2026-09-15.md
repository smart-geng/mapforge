# L01 西侧分合流修形：首个共同边界内核

> **后续实现**：见[保形约束及逐边界不退步](L01-保形约束与逐边界不退步-2026-09-15.md)。已实际写出新研究候选；反向量降低但仍鼓包/旧限速压力FAIL，不是合格地图。

> **已收到确认**：用户同意静态地图与明确通行工况分开验收。[当前决策及实现](验收决策-静态地图与通行工况分离-v1.md)覆盖下面“尚未答复”的历史状态；原37FAIL和两份固定模型不可行检查不改，不重新追问。当前静态形状仍有缺陷，明确工况求值仍未完成，原候选不是交付。

> **最新继续点：同日第二轮已接真实车道动态约束，并完成局部/整父路可行性筛查。两种固定Line模型均未通过动态必要条件，没有新XODR。已向用户请求确认“静态地图验收与明确路径/速度工况动态验收是否分开”，尚未收到答复；没有修改原验收口径/门槛/源速度。详细证据见本文件末尾第二轮。**

2026-09-15，基线提交34dd88f之后。**有新候选，未完成L01，更未完成整图目标。未提交，未替换8765/8766用户会话。**

## 事件范围卡

- 原投诉区域：node4西侧road11，参考站位100–201.7074107m，两次车道出生/增宽。
- 来源仍是已登记SHP数据、reconstruction-input.json和run.json。25条原边界记录/19条原车道路径完整留在scope.json；原始车道路径没有被边界中线替换。
- 两个零宽事件沿用既有批准：`2023041111104128071/start`（lane -3，s=131.0543）；`2023041111104060474/start`（lane -4，s=151.6089）。没有新授权、没有扩大九处来源角色范围。
- 前者左右原边界：`2023041110483940807` / `2023041110483938848`；后者：`2023041110492387249` / `2023041110492385251`。来源ID由原关系和目标occurrence绑定，不用最近线猜身份。当前IBD事件声明的left是参考轴较低t侧，读取时检查顺序；不能推广为所有图商的SIDE规则。
- 共同修改该范围内0至4号边界，width从相邻边界之差写出，laneOffset来自同一个0号边界。两端位置/一阶/二阶导数锁定；参考线、路口口部、原TOPO/速度/ID/标线/扩展保持不变。这里不是L02口部联动。

## 实现与结果

新内核`mapforge/repair_web/outer_event.py`不修改旧会话冻结的model.py。原UI的C2位移叠加会保留旧曲率跳变；本内核直接对事件中的所有边界联合求解，零宽出生边界和其内邻边界共享C2条件。

固定一条211.707m参考Line；54个三次B样条系数，经端点/出生等式消元为24个自由变量。独立形状区间最短6m；XML在既有语义laneSection处只是精确重表达同一多项式。没有按源顶点生成短参考段，但**不能据此宣称整图已经符合用户的非碎段/驾驶质量要求**；旧密集横断面记录仍保留。

源包络并非只验抽样点：对每个原始直线段和最终三次边界差求极值，超限极值作为新约束回求，最多8轮；不会因此增加几何结点。本次7轮，最差0.7500000074m，在原0.75m门槛及1e-7数值容差内，已无实用余量。

本轮只做三个结构布局假设，没有扫描种子：最粗布局不可行；加入源增宽终点的布局仍不可行（诊断性最小额外包络0.400584m，**未用其放宽门槛**）；第三布局给约9.7m的第二个增宽分配两个6m过渡区间，几何可行，但最终动态仍失败。前两轮数值在本说明保留，早期临时solve.json曾被下一轮覆盖；最终r3目录与后续脚本改为新目录、禁止覆盖。

证据目录：`out/node4-outer-event-l01-20260915-r3/`。

| 同一个最终文件的检查 | 实际结果 |
|---|---|
| XODR SHA256 | `e1ff744b8ad3cfbcc025357d80a89668fa17b045d596ccf9d405b8d9db555843` |
| 独立最终XML原件回读 | `PASS_LOCAL_EVENT_NOT_MAP`；与求解器独立解析width/offset，源段内精确极值 |
| 西侧原三处曲率跳变 | 消除；两个出生事件的世界边缘位置/切向/曲率闭合 |
| 区间外边缘最大变化 | 0m；其他道路和非几何语义不变 |
| 整图内部边缘失败 | 南侧road12/lane4/s30仍失败；不是所有地图门禁的总计 |
| 复杂度 | geometry 94→94；width 814→819；laneOffset 359→354；laneSection 104→104 |
| XSD / esmini实际读回 | PASS / PASS；2052位置，2个近零宽查询跳过；最大位置差3.9241e-14m。esmini对EQC的+R/+lat_ts/+no_defs发出不支持提示，不能视为CRS认可 |
| 原60km/h动态区间检查 | **FAIL：50区间中37 FAIL、13 BOUNDED**；最大观察横向加速度11.1327m/s²，曲率变化对应横向需求率55.6020m/s³，远超既有2.5/1.0目标 |
| 全源端域、面域、完整movement、MAP、绝对CRS | 未完成，本次不能证明 |

亲自查看`source-before-after.png`及`whole-map.png`：外缘跳变改善，但lane -3增宽后仍有鼓包；曲率连续不代表变化缓慢。最差动态仍在road11/lane -3、源`2023041111104132424`，s=160.9542–173.3754范围。**这份候选不能交付，也没有接入Web作为“修好”选项。**

当前控制意图是约束下的软目标，不能伪称拖到哪里就严格落在哪里：请求0m时实际控制站位移-0.061879m。接Web前要给出实际可达范围/实际读回位置，并处理硬目标冲突；不可套旧UI的`初始位置+请求位移`假装实际结果。

## 验证及保留边界

`pytest tests/test_outer_event_repair.py tests/test_repair_workbench.py -q --tb=short`：25通过（12新例＋13旧例），2条依赖弃用警告。新增覆盖来源授权/零宽、源路径不删、短独立形状段拒绝、完整源段包络、真实XML回读、区间外/非几何篡改、重放一致、动态失败及禁止降原限速。**新内核尚未接预览/应用/撤销/保存重开的Web事务，因此L01交互验收未完成。**

两旧项目各47冻结哈希复核均0变化。8765进程63616、8766进程55044仍监听；没有保存、撤销、重启或覆盖用户当前状态。原始资料与阶段归档未改。

## 下一步（不再补曲线端点）

1. 在同一个事件共同状态内加入形状过冲控制和实际车道动态约束/可行性判断，而非几何求完之后才知道动态失败。保持源包络、出生条件及源速度，不能只加平滑正则宣称通过。
2. 若固定参考轴/出生域导致三组约束冲突，定位具体绑定和不可达控制范围；禁止再靠短段、提高误差或降低限速换PASS。将此结果作为后续共享轴/端域方案依据，而不是无界重跑旧全局扫描。
3. 合格局部候选才能接入现有事务契约的新副本：真实XML位置预览→原子应用→撤销/重开→独立导出；原8765/8766继续不动。
4. L02→L03→L04原优先级不变，整图目标仍active。

数值实现核对：[SciPy SLSQP](https://docs.scipy.org/doc/scipy/reference/optimize.minimize-slsqp.html)、[HiGHS线性可行性接口](https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.linprog.html)。使用解析导数与等式零空间、线性可行点初始化、完整源段极值复验；求解器的success不是地图验收。

重跑（必须指定不存在的新目录）：

```powershell
.\.venv\Scripts\python.exe scripts\check_outer_event_repair.py --out out\node4-outer-event-NEW
.\.venv\Scripts\python.exe scripts\plot_outer_event_repair.py out\node4-outer-event-NEW
.\.venv\Scripts\python.exe scripts\validate_repair_export.py out\node4-outer-event-NEW\candidate.xodr out\node4-outer-event-NEW\consumer-request.json out\node4-outer-event-NEW\consumer.json
```

几何求解被拒绝时不产生candidate.xodr，不继续最后两条命令。此命令仅研究副本，不是生产转换入口。

## 第二轮：动态联合求解与固定模型可行性（待验收口径确认）

### 新代码和执行结果

新增`mapforge/repair_web/outer_event_dynamics.py`：复用上一轮同一个边界状态与源/width约束，实际车道中心由相邻边界共同得到；绑定原source_lane的60km/h，不新增默认速度。全部laneSection、曲线和speed分界的左右极限进入求解；不把零宽出生车道从动态检查中排除。

有符号动态量及对一阶/二阶/三阶横向导数的解析Jacobian已接SLSQP；整区间仍由既有`continuous_offset_dynamics.audit_span`独立复核，源包络仍用原线段极值复验。分析验证点不是新增XODR几何段。上限为6轮、每轮120步；没有重新扫描1676列或更换随机种子。

在非线性求解之前增加一个**必要条件松弛**：原源包络采样约束＋采样非负宽＋原C2条件，先用线性规划估计可行斜率范围，再由原2.5m/s²、1.0m/s³目标推导二/三阶导数的必要界。不用更严格的Bernstein非负控制点条件证明所有多项式无解。线性松弛不可行时立即停止当前表示，不能把失败优化器重试次数当工作量。浮点LP结果不是形式证明，更不证明所有地图表示都不可行。

| 有界检查 | 结果 |
|---|---|
| `out/node4-outer-event-joint-20260915-r1/`：只加加速度必要界，再联合非线性求解 | 15步后拒绝；源误差0.764248m越界，动态26FAIL/24BOUNDED；没有写XODR |
| `out/node4-outer-event-joint-20260915-r2/`：补上曲率变化率的必要界 | 54变量/24自由度，50动态区间/150检查点；源松弛可行、动态必要条件不可行；非线性步数0 |
| `out/node4-outer-event-joint-whole-parent-20260915-r1/`：一次明确的整父路支撑域替代 | 修改域扩至0–211.7074107m，68变量/38自由度，68动态区间/204检查点；仍不可行，非线性步数0；并非“只因修形区间过短” |

松弛诊断额外归一化导数余量分别2.841987、1.776487，仅用于定位冲突，**不是允许放宽的阈值，也不是可以直接换算的推荐降速比例**。整父路活跃冲突集中于lane-3的s=151.6089/163.6089/173.3754等位置的三阶导数；不得据此声称只有这些点需要验。

两种表示都仍固定原参考Line、原出生站位、原端点导数和源误差条件。曲线参考轴、可动源域、其它函数族或不同通行路径的可行性没有被否定，但继续盲调这两个固定模型已无依据。

测试：`test_outer_event_dynamics.py`8例新增（解析导数、最终XML动态对拍、结点三阶导数双侧、原限速拒绝漂移、真实数据必要条件拒绝、可行合成直线正例）；与上一轮12例共20通过。正例也必须达到整区间BOUNDED，不能把“全部拒绝”的空实现算通过。

本轮收尾回归：上述20例＋旧修形事务13例，共33通过（2条依赖弃用警告）；连续动态区间检查16例另行通过，总计49项相关测试。两个旧项目各47冻结哈希仍0变化，两个原服务进程63616/55044仍监听。未提交，未改用户编辑状态。

### 文献/标准核对及待确认问题

1. [ASAM OpenDRIVE 1.7 §9.5.5 Lane speed limit](https://www.asam.net/fileadmin/Standards/OpenDRIVE/ASAM_OpenDRIVE_BS_V1-7-0.html)将lane speed定义为最高允许速度；它不是实际行车速度记录。标准允许非负车道宽度，并没有要求车辆必须沿每条零宽新生车道的几何中线以最高限速贯穿。这是地图语义与本项目动态压力测试假设之间的区别，不意味着自动降低速度就算通过。
2. Zhu、Schmerling、Pavone，*A Convex Optimization Approach to Smooth Trajectories for Motion Planning with Car-Like Robots*（CDC 2015，[作者官方全文](https://stanford.edu/~pavone/papers/Zhu.Schmerling.ea.CDC15.pdf)）：把固定速度下的形状优化与固定形状下的速度优化区分后交替求解，明确动态约束不能仅靠C2连续替代。本工具借鉴约束建模，不照搬点列重采样写路，也不运行它的速度优化去篡改源MAX_SPEED。它是轨迹规划论文，不是SHP→XODR转换正确性的直接证明。
3. 新图`source-role-comparison.png`（整父路检查目录）直接重读原边界和原车道路径，没有展示生成的“理想路”。第一/第二幅显示两个已批准零宽对象的原路径与边界中点明显不同，第三幅显示后续完整车道二者基本重合。绿色原路径也只是地图中的路径观察，**不冒充实际车辆时间轨迹**。

已通过问题工具请求用户确认，原问题含义如下：

> 是否区分“静态地图保真/平滑验收”和“明确通行路径＋速度工况动态验收”？原限速、原误差门槛和既有FAIL全部保留；缺少工况证据仍BLOCKED，不自动降速换PASS。另一个选项是保持所有几何中线全程原限速的现有门禁。

**截至本记录写入尚未确认。** 因此旧G11策略与本轮失败状态均不改、不自动豁免新生车道、不宣称静态通过就可用于自动驾驶交付。九处来源角色批准也没有被重问或扩展。

收到确认后：若同意区分，先版本化记录静态门禁、速度上限语义、动态工况输入、缺证据BLOCKED及原固定限速压力测试的保留方式，再接新修形副本；不能直接把37FAIL改成PASS。若保留旧门禁，则必须评审另一种共同参考/源域模型或确认约束可行性，不重复本轮固定模型。L02/MAP/整图验收/绝对CRS仍未完。
