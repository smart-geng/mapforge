# 地图格式转换工厂 · 会话交接文档

> **2026-09-15 UI v2：借鉴RoadRunner/QGIS，改善“选线、拖动不直观”。** 青色分界线直接点选、近邻候选消歧、双箭头控制柄、偏心相对拖动、一键放大、画布应用/取消和快捷键；540px窄屏实测无横溢出。13 Python＋8 JS测试通过，真实浏览器拖动/取消/应用/撤销重做/保存刷新/导出跑通。新副本`out/node4-repair-ui-v2-20260915/project/`服务8766；**8765旧服务及用户未保存撤销状态未覆盖/未重启**，新副本仅继承当前已应用几何，不含旧完整撤销链。测试位移已撤销保存为零意图原SHA，测试导出969dd5f3…仅为过程证据。47绑定未变，仍7条受限共享边界，外缘/口部/MAP未开放，整图仍BLOCKED。详见[交互v2](docs/本地修形台-选线拖动交互v2.md)。本轮不改几何核、不增短段、不commit。

> **2026-09-15 当前继续点：可操作的本地修形纵切已实现，整图仍未修好。** `scripts/serve_repair_workbench.py` 启动 node4 SHP 原件/XODR 叠图；7个受限共享分界线手柄，四个长三次增量区间，复用已有width，新增几何/width记录0。真实浏览器已跑通拖动、应用、撤销/重做、保存重开、越界拒绝、候选导出。页面导出在 `out/node4-local-repair-20260915/project/exports/e747544bed22452181a87e7dda8064e6/`；XML SHA `5e3b4c3f09c8161fd0dfb68e8281b6e4fae83aaf490cc2aa221a5f9c5593259d`。XSD和esmini修改道路2028位置读回通过；内部边缘仍原4项FAIL，源保真/动态/面域/CRS未完。外缘/口部/拓扑/参考轴锁定，MAP尚未接入本页，不能宣称整图/完整E0已完成。47项绑定未变，九处批准不扩，未commit。详见[本地修形台使用与证据](docs/本地修形台-E0受限纵切.md)。
>
> 下一步收敛到原投诉的外缘/分合流修形，先明确同一物理边界和零宽事件联动，再开放手柄并按相同事务实测；不要恢复无限自动拟合或扩大完整GUI平台。下文“没有Web”的结论是历史状态。

> **2026-09-15 收敛路线：现成编辑器 ORBIT 第一关已否决，仍无合格新 XODR。** 用户同意先验证现成编辑器、失败再考虑最小本地修形台；不继续扩大上一轮自动求解研究。已在独立环境用固定 ORBIT `8bd191af` 核心跑完 node4 的 SHP/MAP 派生失败样本的无编辑导入、保存重开、导出及独立 XML/esmini 选点复核。SHP 94→2193、MAP 54→1366 个全 line 原语，宽度/站位丢真；esmini 复核 road12(SHP)约284m、road10(MAP)约217m的往返边缘差。道路/连接数保持不等于几何保持。
>
> 证据 `out/node4-orbit-evaluation-20260915/`，详见 [ORBIT准入试验记录](docs/实施记录-现成编辑器ORBIT准入试验-2026-09-15.md)。这不是新图修复，也不是GUI拖动验证；未运行GUI自动realign、未安装GUI。第三方源码不入主线、不打补丁，不改原件/九处批准/速度。下一步不再适配ORBIT；按用户批准的收敛计划，现成商业工具无可用授权时，仅做一个真实本地长曲线编辑—保存—重开—导出—独立复验纵切，不先扩完整GUI平台。下文自动求解“下一步”是历史路线，不能未经说明又恢复长时间数值研究。

> **2026-09-14 最新：全路口长曲线动态检查层已实装，仍无合格新XODR。** 最新证据`out/node4-whole-dynamics-r1-20260914/`，详见[实施记录](docs/实施记录-全路口长曲线动态区间检查-2026-09-14.md)。从同一完整5父路/24转向状态检查实际世界车道曲率及弧长导数；用Bernstein区间包络检查长曲线内部，不增加几何段。已复算原件及九处批准，281哈希不变，初值与r4逐项全等，拟合0、写图0、约74.75s。没有新图，不冒用旧截图。

- 924个声明检查区间（父路732、转向192）全部参数域有结果：591 BOUNDED、333有实际超限见证；不是原件全部写出域已覆盖。最小检查区间约0.000746m仅是已有分界的交集，不是短参考段或新width。实际仍5普通参考原语＋120转向原语，转向最短6m。
- 5普通道均有源限速下的动态超限；转向13条完整链限速统一，11条混有40/60km/h，仅给最大限速包络诊断，不能当真实目标速度区间或已批准设计速度。恒速目标仍2.5m/s²和1.0m/s³，后者是横向加速度变化率，不是总惯性jerk；不改源速度。单侧光滑区间的浮点数值包络不是形式安全认证或接缝验收。
- 新增19项测试已通过，包括真实系数逐见证复算/BOUNDED区间≤0.1m加密复查；不是XML/esmini/MAP验收。当前动态是共同状态检查接口，**尚未成为完整优化器残差**。下一步先实际写出域/原件所有权及11条混速链速度映射、面域与动态残差，再按原有限候选共同求解；不重复全列差分、旧固定C/CZ或冻结父路逐条补解。旧7.526348m域问题、MAP/CRS/movement/Web未关，不重问已批9处，未commit。

下文全量1676列r2仍为有效历史数值证据；本轮没有重新扫列或优化。

本轮最终相关回归 **194 passed / 66.64s**（含新增19项），compileall/diff检查通过；最终281项输入/原件/批准/执行代码哈希仍未变。不是全仓或地图验收。所有本轮进程已结束。

> **2026-09-14 全量变量和整图联合步验算完成，仍无合格新XODR。** 最新 `out/node4-whole-joint-step-r2-20260914/`，详见[实施记录](docs/实施记录-全量变量与整图联合步验算-2026-09-14.md)。本轮零拟合、零写图，未commit；原件/速度/九处批准不变。

- 检查已从旧9列扩展到**全部1676自由列**：左右与平均差分三个实际矩阵均1724×1676，5个非路口cut槽仍固定0。507等式/1217组不等式，父路基准51084条证书保留；不是全量光滑Jacobian认证。240列/431行列项有左右差分分歧；77列仅单侧可行，缺失侧显式掩码，不补成零方向。
- `trial_vector`先更新全部改变的父路，再一次性更新所有受影响转向，两端来自同一状态，失败不污染旧快照。`WholeSourceStepModel`保留两侧预测区间并实际检查整个联合变化，不能用平均差分或原本通过来免检。区间只是工作预测，不是非线性包络或已实现SQP-GS；未来仍需完整约束求解器。
- `SourceCoefficientStencil`只在一个未变轴/口部/布局快照的**系数差分**中计算全部仿射见证，精确端口基函数零才免算无关转向；轴/口部/knots仍完整重建原件。每新基准重建缓存，不能恢复旧固定C/CZ优化。完整矩阵调用要显式`coefficient_stencils=True`；r1旧慢路径到110列主动停止，保留为部分检查，不补零。10次真实系数对比最大差4.89435e-14。
- 预登记的一次**5父路＋24转向联合微扰**已实际计算，全部残差与整图独立重算差0；无新增不等式违例/预测漏报只针对这个1e-5微扰。仍有507组不等式失败，最大混合等式18.943201、最小混合余量−307.213411，**当前几何不合格且未优化**，不能标米或说边缘改善。没有新地图/截图，不拿旧v1.71冒充修复后。
- 全差分计算约722.4s、全任务830.078s。278输入/原料/批准/代码哈希不变；v1.71旧图SHA不变。172相关回归＋3真实终态复核通过（合计175不同测试），compileall/diff检查通过；非全仓/整图/MAP验收。所有本轮进程已结束。
- **下一步不能再只扩大求导抽查**：全自由列已有结果；把实际写出端域/区间、全源所有权、父路/转向动态、道路面域接到这一共同状态，再按既定最多3个完整结构候选预登记求解。须处理来源组见证与转向非光滑约束，不能将中央差分直接交给光滑算法假装问题消失。原7.526348m旧域分配、MAP/CRS/movement/Web及同SHA文件/esmini验收均未关闭，不改门槛/速度、不加短段、不重问已批准9处。

复跑用 `scripts/check_whole_source_joint_step.py`（四个位置参数与下文旧检查一致，必须新输出目录；非零研究退出不是地图交付）。旧r5的“仅检查9列”为历史范围，不能继续当作当前覆盖范围。

> **2026-09-14 全图来源绑定/约束接口修复，仍无合格新XODR。** 最新证据`out/node4-whole-derivatives-r5-20260914/`，详见[实施记录](docs/实施记录-全图约束残差与求导检查-2026-09-14.md)。本轮未拟合、未写地图，不推广CLI，未commit；九处既有批准不变，不重问、不扩权。

- **修复了共同模型的系统性左右错配**：父路按实际边界族生成世界端口，但转向此前把Profile SIDE直接当行驶左右。现在由两端**同一原边界ID/族身份**绑定工作左右，必须唯一且两端一致，不几何猜连通、不改原SIDE/边界点。24转向均证实工作左右与原声明字段需要交换；96个端口边缘对原边界最大0.020568767m。仅是源端对齐，**不是连接路闭合或最终XML接缝通过**。
- **修复零长度源尾段的反弯证据跳变**：106原角预算曾因极短片段有无而变0.402291°（约0.140426缩放角，不是0.14m）。现在从完整原线段读取当前切面单侧切向，零长度尾段也保留；同三状态源预算稳定，真实目标反弯违例仍在。movement路径仍与物理中心分离。
- `SourceConstraintEnvelope`用各源身份全部证书余量的最小值输出固定行，不删任一原检查；源width零长过渡的见证归相关已有源车道组。正则式`.8−κt`过零有限。`WholeSourceConstraintSystem`提供507等式/1217组不等式、1676自由列（1681状态中5槽固定0）。父路原51084检查项逐项留账；可变证书分区不增加几何段。
- **只复用真正无变化的源矩阵**：轴/口部/knots有任意改变即重建原件约束、全部关联端口和原尾段；系数单独扰动才可复用相同坐标矩阵。两次与整图独立重算的全部残差差为0。完整快照汇总同步，不能携带旧总残差。
- 距离约束用`(B²−e²)/(2B)`消除零距离范数尖点，B由原核提供，正负/活动边界导数与原门槛一致；原核余量保留。**不是放宽保真、不是光滑Jacobian证明**。9列×两个步长检查后仍有2项步长不一致：北κ变化对应102/103中心反向距离中位数（row552/607）。另外左右差分仍有分歧，不能把“仅2项步长不一致”写成全量Jacobian只剩2处问题；未验证全部1676列。
- r1–r5均是**同一固定初值**的接口/语义检查，不是5个结构优化候选，拟合预算0。r5最大混合尺度等式18.943201；原核最小余量−14.298756；等价平方后的最小混合余量−307.213383，尺度不同不能当误差变大/变小。初值仍拒绝。275原件/输入/决定/执行代码哈希不变；旧v1.71地图未动。
- **下一步**：处理完整模型非光滑见证/中位数切换的线性化与受限步验算，仍需全量自由列、全源所有权/实际写出外端和区间、动态/面域约束，然后按全局方案有限登记共同求解。旧C/CZ、冻结父路逐转向、逐点短段和降低速度/容差仍禁止。7.526348m旧多要素域分配、MAP/CRS/movement/Web、同SHA写出/esmini未完成。本轮无新图，不以原件对齐代替地图验收。
- 本轮最终相关回归 **161 passed / 59.45s**（包含r5真实证据3项），compileall与diff检查通过。最终重新核对275项原件/决定/输入/代码不变；v1.71研究件SHA仍`e05dcb85db2c913081cfb1de29d88a918c4e2df019c79684f9553ad02d27a5b0`。旧r4证据测试将Python修订显式与原始哈希分开，仍严格拒绝原件/Profile/批准漂移，不伪称重跑旧编译器。没有后台求解作业遗留。

下文全模型r4是此前阶段；来源端口“已共享”不能再沿用其未归一的SIDE→工作左右路径。

> **2026-09-14 全5道路＋24转向的共同几何状态已接通，仍未交付、未输出新XODR。** 最新证据 `out/node4-whole-source-model-r4-20260914/`。本轮用户在三处明确范围确认问题后回复“继续”，已仅新增西2处/南1处批准；不要再问这9处角色，也不要再跑旧双父/逐条转向拟合。

- 新决策 `profiles/repair/node4-west-south-zero-width-source-roles-v1.yaml`保留原问题与答复，精确绑定全接触哈希 `88dab542381e54b7b5e89c274b164561e925b0efe9758bd15feda4164cff5c34`。只增加2023041111104060474/start、2023041111104128071/start、2023041810470164021/start；北2/东4原批准原样重放。9处均为原边界决定物理道路、完整原车道线保留为通行路径另验；其他原件/TOPO/速度/CRS/MAP不扩大。
- `OriginalParentBoundaryBlock`用父路起点切线作为原点的可逆计算坐标；不把原Arc改写成Line、不采样旧地图作为真值。完整5父路原件模型、共享长三次边界已接入。确定性Greville初值只在既定控制点数量上读取原边界，不做逐原点插值、不调用局部优化；初值违反源接触导数约束必须保留为失败。
- `SourcePortDomain`从实际road/lane联系与完整原TOPO链确定端口，不按左右车道编号/邻近距离配对。取消把不同方向外端强行裁成同一断面的前提；完整原件仍在父路约束中，路口尾段对全部关联转向显式留账。最终实际XML的全源域分配、外端覆盖和写出width区间仍须独立核验，旧7.526348m未分配问题**未因此关闭**。
- `WholeSourceGeometryState`在同一个**1681维状态**中评估5父路＋24条连接路：1081父路坐标/共享边界状态量＋600转向量。其中无路口连接一端的5个cut槽位固定0，不是新增外端裁剪自由度。全部转向使用固定3核心＋2端帽的5长参考原语、每段≥6m；普通共享width原区间最小依次为5.513318/5.507379/5.593378/5.552347/5.629201m。上述是研究模型预算，不是ASAM或自动驾驶通用平滑合格线。
- `MovableMovementSupport`随同一父状态，在真实Line/Arc法向平面上重新切分原始尾段；完整via不裁、全部原件保留，源尾段不固定在旧生成口部。已批准movement路径不能经连接路原中心合成重新变成物理中心约束：物理中心按已知原分段检查，完整movement单列待验。保真缓存不能复用旧源域结果。所有边界、中心与转向拓扑仍需最终文件读回。
- **实算不是求解成功**：一次完整初值评估含507项等式、51948项不等式；最大混合尺度等式残差18.943201、最小不等式余量-16.916222，不能把它们简单标成米。全部初始转向仍不合格。当前状态 `WHOLE_SOURCE_AND_SHAPE_EVALUATED_NOT_ACCEPTED`；没有数值优化、没有新XODR。西road11原点y仅扰动0.00001m时，106–111六条依赖全部更新，其他不动，验证了一次真实联动，不是闭环验收。
- **后续仍有工程缺口**：共同几何评估器不等于完整优化器。先把随轴/口部/区间变化的原件约束变成可保持结构的残差/Jacobian，补齐全源归属/写出外端与区间、父路与转向动态、面域约束，再按全局方案预登记最多3个完整结构候选与求解预算。不得把旧固定C/CZ的GN-QP直接套上、冻结父路分别解转向、增加逐点短段或降低限速/容差。完整写出/同SHA验证/esmini、MAP和CRS、movement/Web仍未完成。
- 相关回归**123 passed / 66.88s**，另真实全模型重放**3 passed / 6.81s**；271项原件/批准/输入/执行代码哈希不变。本轮只写JSON研究状态和新增3处决策，不改原料、旧批准或v1.71研究图，不推广CLI，未commit。r1仅源基底、r2端口、r3首次完整核为阶段记录；最终用r4（含路径角色分离与真实联动检查）。

本轮准备检查入口（新目录；返回非零表示不是地图交付）：

```powershell
.\.venv\Scripts\python.exe scripts/prepare_whole_source_model.py out/node4-global-model-preflight-20260914/final-input out/node4-whole-source-role-binding-20260914 profiles/repair/node4-west-south-zero-width-source-roles-v1.yaml out/<新全图检查目录>
```

以下来源角色“待确认”为上一轮历史，已由上述本轮3处明确决定解除，不能重新当作阻断。

> **2026-09-14 全图来源角色接入：仍未交付、没有新XODR，停在西2处/南1处解释待确认。** `out/node4-whole-source-role-binding-20260914/`已经从完整5道路/24转向输入重读原件、编译291要素/240端点；北2处和东4处既有批准准确继承，新增授权0。不能再带着未批准角色运行整图拟合，也不重复询问已批准6处。

- 实现 `compose_reviewed_source_roles` / `replay_source_role_packet`：旧批准必须先通过原始精确版本验证，再逐项比较原车道全部记录、左右边界、完整原点/部件、CRS投影、来源预算、道路归属及冲突证据。输出是可独立重放的派生记录，不签造新用户批准；改原点/边界/速度/宽度或重算哈希伪造完成均拒绝。旧v1直接批准保持原结果。
- **仍待确认三处，都是原始start端**：西road11 `2023041111104060474`（1.566211m）、`2023041111104128071`（1.767185m）；南road12 `2023041810470164021`（1.828769m）。直接从SHP/DBF重读核对：显式S_WIDTH=0、原左右边界同节点同点收合，但原车道线端点与收合点偏离上述距离。完整原记录/详细放大叠图在 `pending-originals.png`，已亲自查看三处。
- 上述距离只证明“这两个原点必须代表同一零宽物理端点、且各误差≤0.35m”这一角色绑定不相容，**不是一般OpenDRIVE无解、不是证明原地图错误**。建议用户仅对这三处确认：原左右边界定义物理道路，原车道线完整保留为通行路径另验。未得到明确答复前不写新角色决策、不删源点，不改变TOPO/速度/CRS/MAP。
- 本轮拟合预算0；61项原件/历史批准/输入/执行代码哈希不变。`report.json`状态 `SOURCE_ROLE_DECISION_REQUIRED`；完整模型构造在未决角色处拒绝。既有研究XODR不变，不以源角色复核通过代替平滑地图验收。
- 本轮相关回归 **97 passed / 63.70s**，覆盖旧v1与派生v2角色、真实原件重读、全图未决阻断、共享边界、可动布局与完整端口。不是全仓、全图或MAP验收。测试 `test_whole_source_role_replay_real.py`明确校验北2东4集合、西2南1集合、240端点、291要素，以及原件哈希不变。
- 获批后仍须接齐5父路与24连接路完整形状核、事前登记有限结构候选及预算，再共同求解并复核同XML全源/平滑/面域/动态/esmini。原7.526348m多要素域分配、MAP、CRS、movement/Web等没有关闭；不能承诺“只差这三处审批就完成地图”。未推广CLI、未commit。

复核入口（用新目录，研究状态非零退出）：

```powershell
.\.venv\Scripts\python.exe scripts/prepare_whole_source_roles.py out/node4-global-model-preflight-20260914/final-input out/<新目录> --review out/source-role-input-v151 profiles/repair/node4-zero-width-source-roles-v1.yaml --review out/node4-east-source-v167 profiles/repair/node4-east-zero-width-source-roles-v2.yaml
```

以下为此前连续坐标层及数值试验历史。

> **2026-09-14 连续坐标层已接入，仍未交付、没有新XODR。** 最新进展见[共享可动参考轴与固定结构布局](docs/实施记录-共享可动参考轴与固定结构布局-2026-09-14.md)。`SourceKnotLayout`冻结结构/系数列，`MovableSourceParentState`共同调整轴、原件端域相对口部、共享长区间及边界系数；每次重算原件约束，不缓存旧固定C/CZ。`WholeSourcePortState`完整端口接口已实现，但11/12/13和全24条形状核尚未接齐，不能启动旧双父求解。

- 本轮真实全接触：5普通道路/24转向、291要素/77事件、240源端点，结构问题0、9项既有角色冲突保留，不扩大北2东4之外授权。两个批准组件北183变量、road30为78变量；仅5项/组件有限扰动，不做拟合、不宣称源约束或整图PASS。
- 最终证据 `out/node4-movable-source-layout-r3-20260914/`（JSON检查，无XODR）；188相关测试通过。原7.526348m域分配/动态/面域/MAP/CRS/Web等未关闭，旧研究地图不动，未推广CLI、未commit。

> **2026-09-14 全局重建切换：仍未交付；本轮没有拟合/生成新地图。** 用户要求停止局部补丁。当前继续入口为[全局方案的当前执行章节](docs/全局重建与约束式交互修形方案.md)，优先于下文v1.74“下一步拒步/初值”的旧安排。

- 全路口原件准备入口已落地：5普通道路、24原始转向、0固定对端；精确Line/Arc源域、完整来源链、链TOPO核查与源速度区间读回。新模式显式参数，旧模式/历史读回不变；新编译器增加原方向元数据，不改原速度。
- 最终输入 `out/node4-global-model-preflight-20260914/final-input/`：120原车道、171原边界（69共享），转向24/24；普通源速度有交集453项一致、976项无交集、0不一致。38映射原料/Profile/输入哈希不变，重新打开原件复核一致；这只是研究来源准入。
- **仍有91个原区间/81要素合计7.526348m未分配**，不是一个7.53m道路孔洞。角色审批、几何/动态、面域、CRS、MAP、Web仍未完成；西2南1不扩大，北2东4不重问。未修改或重新验收旧XODR。
- `--whole-junction-id 1`在初始化前拒绝旧双父输入：缺父路11/12/13、转向108–111/116–119。证据 `out/node4-global-model-preflight-20260914/old-model-rejected/whole-junction-preflight.json`；没有启动求解。
- 本轮129相关测试通过；输入/接口通过不是模型可行性。下一步登记覆盖全部父路、切分位置和共享边界自由度的有限结构候选，先核完整模型，不继续跑旧局部种子。保留旧拒绝证据和v1.71研究地图，不推广生产CLI、未commit。

---

以下v1.74为历史数值试验与拒绝证据，不作为当前执行顺序。

> **2026-09-14 / v1.74：双父路＋16转向共同求解已实施，仍未得到合格整图。** 两次实际写出都经24转向、两父路和esmini完整复核后明确拒绝。最新563变量试验已结束，仅接受一步；蓝缝、尖弯和内部曲率跳变仍在，没有交付。

## v1.74 当前工程与拒绝证据

- **保留件不变**：`out/node4-all-source-tail-readback-v171/node4-review.xodr`，SHA **e05dcb85db2c913081cfb1de29d88a918c4e2df019c79684f9553ad02d27a5b0**，仍是研究件，不是交付。未修改CLI默认输出，未commit。
- `solve_source_junction_group.py`消费v1.73双父输入，北road10与road30的209个父路独立变量和全部16条转向同时变化；首批模型合计509变量。依赖完整清单是**100/101/102/103/104/105/106/107/112/113/114/115/120/121/122/123**。未改道路逐字节核对，原traffic graph不变，不能跨父路状态挑单条成功曲线拼装。
- **原件约束不再只作为软惩罚**：`source_feasible_gauss_newton.py`用Clarabel解有界Gauss–Newton QP；两父原8860＋2940线性不等式限制每一步，回溯重新检查实际非线性残差。正对角变量/行/目标缩放处理病态数值；零空间消元产生的约1e-15残余行，只有整个步长盒上的表达幅度≤1e-12才不送数值QP，接受前仍按原行逐项核查≤1e-7。不是丢原件点或扩大几何容差，也没有全局收敛证明。来源优先、曲率能量的后续分层目标**尚未运行**。
- 试验留痕：`...joint-v174/`软惩罚10次源约束违例约0.321、编译器拒绝写出；`...hard-source-r2-v174/`未缩放QP不收敛；`...scaled-source-v174/`被数值零行误报不可行。上述均不选用。数值处理修正后`out/node4-north-exit30-stable-source-v174/`实际8步接受，残差45518.099→4841.975，两父源不等式通过，但**16条构造全部拒绝**。
- **这份实际拒绝图**：`out/node4-north-exit30-stable-source-v174/node4-joint-diagnostic.xodr`，SHA **fdce07be271006fa8309b7351ffa71fcbc56cb4b8cd56e8d84e690ea7d1218a4**。最终证据在`review-packet-r2/`和`FINAL-REVIEW-STATUS.json`，不能引用旧`review-packet/`的漏域结论。24实际转向中16几何失败；全源转角10失败（104/105/108/110/112/113/115/119/120/121）；144个esmini接口18失败，最大接缝**4.867314m**，方向差**3.525494°**。104偏移坐标前向因子降至约**0.000181555**，内部边缘曲率跳变约**1.857e6/m**。这是明显退步，不能用残差下降或XSD通过称平滑。
- 两父独立原件读回：北完整双向最大约**0.350000/0.341235m**，road30约**0.349645/0.349914m**；内部G2/99与32源速度记录分别通过，但**两父源速度动态近似均FAIL**。东侧完整原件组合约**0.348917/0.348945m**，只是采样子项通过。road30仅拥有两条原车道的90.968m域，旧生成外延不是源真值，也不以缩短西侧整体范围换保真PASS。
- **已实际看图**：上述同SHA的esmini全貌/路口/斜视、24转向原件叠图、两父完整域与端部图；蓝缝、外缘台阶及转向形变仍在，未自动判PASS。修复`review_asymmetric_dependents.py`的20格`zip`截断：之前多出4条被漏掉，连带东尾段误判。现在绘图容量随实际库存增长、独立清单必须等于实际24条，最终封存拒绝漏检。旧20/22并集验收计划已被**全24实际movement库存**替代。
- **针对失效新增两项保护**：共同求解初值及每个接受步检查正宽和偏移坐标非退化（均≥0.1）；端口固定模型的选择改为检查父路允许系数空间中的斜率自由度，而不是只检查初值是否`t'=0`。解析回归证明：初值平口但允许斜率变化时，旧无端帽模型可令两边缘G2精确、中心曲率却不连续；使用允许该变化的固定端帽模型可同时衔接。只是此建模问题的证据，不代表所有路口缺陷已解决。
- `out/node4-north-exit30-closed-source-v174/`在第一轮共同导数计算时主动停止，原因见`STOPPED.md`：尚未采用上述按允许自由度选择模型的修正。没有完成优化或交付。`...capability-source-v174/`在101初始化拒绝、`...restored-source-v174/`通过100–103后在104拒绝；都未启动新的共同优化、无新地图。101诊断和闭合修正在`out/node4-101-capability-seed-diagnosis-v174/`与`...restored-seed-v174/`，最终初始化残差约2.3e-14，5原语最短6m，仍非地图验收。
- **104紧转弯的模型/流程诊断**：五段全到6m下限后仍约1.615m闭合残差；两核心＋两端帽四长原语试验降至约0.111m，尚未闭合。新`regular_ribbon_seed.py`只要求共同初值的端边缘jets/正宽/非退化，通过同基底受限QP构造，内部G2和闭合留给真正的共同变量求解，不在冻结父路的预处理里要求已经全部解决。`out/node4-104-regular-joint-seed-r2-v174/`明确`OPEN_REFERENCE_JOINT_INITIALIZATION_ONLY`，不能当独立道路用。原约束/最终阈值不变。两核心不保证任意G2条件都有解，文献及实现限制见实施计划。
- **最新整组已经结束并拒绝**：`out/node4-north-exit30-regular-domain-v174/`显式启用`--compact-port-families`，563变量（209父路＋354转向）同时求解全部16条。104选两核心＋两端帽共4原语，其他为4或5原语；实际每段≥6m，三次宽度区间≥3m。两次步长尝试仅第一步接受，第二步无可接受下降步，约217.906秒后结束。残差2328742.953→2071189.013仍很大，两父11800源不等式逐项通过，但16条严格构造全拒绝。来源优先尚未运行。
- **最新实际拒绝件与完整证据**：`...regular-domain-v174/node4-joint-diagnostic.xodr`，SHA **8864c17b9a2a5f9aa3de27162405ecd655c05aac98cae4f2042abf5446e701dd**。最终`FINAL-REVIEW-STATUS.json`与`review-packet/`覆盖实际24条；16条几何失败，**19条全源转角失败**（100/101/102/103/104/105/106/107/108/110/112/113/114/115/119/120/121/122/123）。144个esmini接口18失败，最大位置差**0.321295543m**、方向差**1.015572855°**、曲率差约**3.08e-8/m**；**内部**边缘曲率跳变仍最大**20.498706/m**。接口曲率小不代表内部平滑，不能混为同一指标。XSD1.5M通过，CRS相关`+R/+lat_ts/+no_defs`提示仍在，未宣称坐标已获验证。
- 北源实际双向最大**0.349341249/0.346805023m**，road30**0.208918192/0.205972604m**；两父内部G2与99/32源速度记录通过，但源速度动态近似都FAIL。东完整源组合约0.348917/0.348945m、无未分配尾段，只是来源采样子项。已亲自检查24转向原件叠图、北/road30完整域和端部图、esmini全貌/路口/斜视：104/105/122/123内部有明显尖弯/反弯，路口外缘台阶和各进口蓝缝仍在，未选用新件。
- **下一步不是增加8次到更多次**：正宽/局部偏移图非退化不足以保证整体世界曲线平滑。当前GN-QP只对父源线性域施加硬步长约束，转向非线性活动约束仍主要靠残差和回溯；需要先保存逐试探步的拒绝原因、活动约束/道路和状态，再以这些证据改进受限可行性恢复（非线性活动约束线性化、近可行状态的校正/更好的初值筛选）。不能凭第二步拒绝就断言某项是唯一根因，也不再冻结父路独立解104。优先比较**同一父状态下**满足正宽/非退化的已有世界G2种子与正宽QP种子，而非无条件使用远离G2的初值；真正进入共同求解后仍释放全部父/转向变量。相关算法尚待实现和验证，不能宣称全局解已找到。

复跑要使用新目录；研究命令非零结束不应改成成功：

```powershell
.\.venv\Scripts\python.exe scripts/solve_source_junction_group.py out/node4-north-exit30-joint-input-v173 out/<新双父目录> --method hard-source --initialize-references --compact-port-families --max-evaluations 8
.\.venv\Scripts\python.exe scripts/prepare_source_group_review.py out/<新双父目录> out/<新双父目录>/review-packet
```

随后必须同SHA全24转向的geometry/shape/full-source-turn-budget、两父`review_source_parent_component.py`、esmini三视图并人工看图，最后`seal_source_group_review.py`封存。本轮不改原件、源速度、CRS和批准角色。北2东4不重问；西road11两处/南road12一处没有扩大批准。11/12、全幅来源/面域、源速度动态、MAP、CRS、movement路径与Web仍未完成。

本轮收尾：相关回归**263 passed / 57.47s**，覆盖源角色/原件域、共同导数、两核心长模型、端口斜率、QP原约束、正宽初值、完整库存及拒绝封存；compileall通过。不是全仓或整图/MAP验收。最新试验输入和最终证据**298项哈希复查无变化**。全部本轮求解/复核已结束，无后台作业留运行。保留全部失败与初始化目录，不重复运行覆盖；两个拒绝图各自封存同SHA证据，不混用。

---

以下v1.73及以前为历史：

> **2026-09-14 / v1.73：真正的共享父路—转向求解已实现并实跑，但首个整图试验仍拒绝；没有完成交付。** 新找到并解除了一项固定出口的来源冲突。北路＋road30的16转向输入已经准备好，尚未对这16条共同求解。下文v1.72及以前均为历史。

## v1.73 当前断点：先解除固定源端口矛盾，再扩展共同求解

- **保留件不变**：`out/node4-all-source-tail-readback-v171/node4-review.xodr`，SHA **e05dcb85db2c913081cfb1de29d88a918c4e2df019c79684f9553ad02d27a5b0**。它仍是研究件，不是交付。不选用本轮`out/node4-simultaneous-source-prepared-v173/node4-joint-diagnostic.xodr`（SHA **efe9d228997108283b03c71d4ab0b50f4639a50077dcfe102e8947f79a5cee69**）。实际文件及同SHA独立检查、截图已封存于`FINAL-REVIEW-STATUS.json`，状态REJECTED_NOT_DELIVERY。
- **已实现真正共同变量**：`coupled_source_junction.py`将北路152个独立源系数与12条转向205个独立参考/横断面变量放入同一357维问题。北路164完整系数的12个线性等式用零空间消元，8860原件不等式保留；两端位置、方向、曲率每次从同一个源状态派生。连接路端部边缘系数代数消元，内部世界G2和车道中心端口另约束。稀疏Jacobian用精确父路线性映射×局部6维端口数值导数，并已与真实几何核的整体变量有限差分对照。**不是冻结父路后12次分别优化。**
- **本次只跑可行性阶段**：非平口源模型v1.72＋旧`out/node4-geometry-shared-port/shared-state.json`作初值（不是来源真值），5次TRF/LSMR求值、约188秒。平方约束残差133.319769→0.456796，父系数最大变化0.080809m，原源不等式违例约7.1e-15。但12条全部未过严格构造条件，优化器未收敛；**来源优先优化及光顺优化阶段尚未运行**。不能拿残差下降当几何已合格。参考仍固定长模型，不加逐点短段。
- **独立实际XML复核**：北/东20转向并集仅9条几何子项PASS，失败**100/101/102/103/104/106/107/114/115/122/123**；全源转角失败**102/108/119/120/121**。旧单转弯诊断虽零报警，不能覆盖全源失败。esmini144接口中8项失败，最大位置差**0.004481683m**、方向差**0.003533057°**、曲率差**8.350644e-5/m**；全图内部边缘仍含11/12等12条失败，最大曲率跳变约0.109144/m。XSD1.5M通过不抵销失败。
- 北源实际写出完整双向最大**0.349999999/0.349812235m**，内部G2和99源速度记录读回通过；东侧完整源组合**0.348917160/0.348945471m**通过当前采样子项。都不是连续证书或全图验收。本轮已亲自看20转向原件叠图、北全域/口部/原收合区及esmini全貌/路口/斜视，蓝缝和边缘缺陷仍在；未以截图自动判PASS。
- **查明一个固定条件矛盾**：104接road30/-2的左边缘端点距对应原件**0.577774917m**；105接road30/-1的左右分别**0.577357320/0.577774917m**。在road30端点保持不动的条件下，不可能同时满足精确闭合和0.35m原道路尾段预算。仅是这一固定条件的几何矛盾，不是一般OpenDRIVE无解、也不是来源角色修改授权。`diagnose_fixed_source_ports.py`保留精确原折线距离证据；新主驱动已加入求解前阻断，实际复跑`out/node4-shared-source-preflight-v173/`在优化前拒绝，未再盲跑5/20次。
- **road30原件重建已完成孤立子项**：`out/node4-exit30-source-v173/`原始33要素/1439顶点，角色冲突0；其拥有两条源车道2023070414510746824/2023070414510748078。新增“无冲突时保持所有原件物理约束”的接入方式，没有伪造用户批准记录；任何真实冲突或端点库存缺失仍拒绝。`--source-direction with_s`仅研究编译器显式启用、校验唯一原方向，不造对向车道，不削弱默认双向域检查。
- 孤立件`out/node4-exit30-original-cubic-v173/road30.xodr`，SHA **d032f76b43a0c28c6a1a03594d11909051ddc43a5239d9127ed34f36ed333a1a**：**一条90.967794m Arc**，57源系数、16宽度区间≥**5.648099m**。完整双向原件采样最大**0.213918326/0.210902240m**，XSD/内部G2通过；原两车道60km/h、32写出速度条目不降速。恒速平面动态近似PASS但有jerk WARNING（最大0.753751m/s³，ay约0.646383m/s²），不是车辆仿真认证。**只覆盖这两条明确源车道域，不代表整个西侧道路复原**；旧road30约174.987m，禁止把旧外推长度当原真值，也不得拿缩短整图外延冒充保真成功，外部原件范围仍须总体核验。已看原边界实际叠图。
- **下一共同求解输入已准备**：`out/node4-north-exit30-joint-input-v173/report.json`，其`unconnected-source-parent-input.xodr`（SHA **28ae5b14b15f89ed03f171c7bd3d3b4bb00bcaec0fe173d37fad0cd8774c5477**）只是INPUT，**连接尚未重建，不能给用户当地图查看结果**。北152＋road30的57＝209父路独立变量；8860＋2940源不等式。实际XML端口与源系数映射逐项相等、拓扑ID不变；全部依赖为**100/101/102/103/104/105/106/107/112/113/114/115/120/121/122/123**，其余固定端口未检出同类>0.35m矛盾。此检查不保证整个可行域有解。
- **下一步不要再跑旧12条驱动**：扩展`solve_source_parent_junction.py`消费上述双父输入，将209父变量和全部16条转向变量真正共同求解。新增112/113不在旧20条报告内，必须按实际XML/原件显式准备固定长模型，不能漏掉或假称旧PASS。端帽分支固定后验证有界初值、原件/原尾段/世界G2/全源转角；先可行再保真优先。两父同时写出后，几何复核需覆盖旧20与新增112/113的并集22条，并独立复核road30完整源域及外部范围。**16条共同求解、来源优先阶段尚未实施完成**。
- 本轮相关回归**228 passed / 58.50s**，另拒绝封存/全源预算测试**9 passed / 3.76s**，compileall通过。是相关工程测试，不是全仓/MAP/整图验收。双父输入248项绑定与首轮拒绝13项封存证据最终核对未变。所有本轮作业已结束，不改原件/角色/源速度，不推广CLI，未commit。北2东4不重问，西2南1未扩大；road30无冲突路径不等于批准西road11的角色。11/12/全幅面域/整体动态/MAP/CRS/movement/Web未完成。

研究复跑（均用新目录；返回非零表示非交付，不能改成成功）：

```powershell
# 旧北12条驱动现在应在固定road30原件端口检查处拒绝，不再启动无效优化
.\.venv\Scripts\python.exe scripts/solve_source_parent_junction.py out/node4-north-source-port-replayable-v172 out/node4-all-source-tail-readback-v171 out/node4-geometry-shared-port/shared-state.json out/<新前置检查> --max-evaluations 1
# '-'只表示零冲突时完整保留原件，不是批准任何角色变更
.\.venv\Scripts\python.exe scripts/build_ordinary_source_road.py out/node4-exit30-source-v173 - 30 out/<新road30件> --source-direction with_s
.\.venv\Scripts\python.exe scripts/prepare_joint_source_group.py out/node4-all-source-tail-readback-v171 out/node4-north-source-port-replayable-v172 out/node4-geometry-shared-port/shared-state.json out/<新road30件> out/<新双父输入>
```

首轮共同求解保存的是加入固定端口前置检查之前的代码快照；独立读回包明确记录代码变化，不冒充由后来代码产生。拒绝封存保留实际XML及报告hash。后续代码修改要显式记录迁移，原件/配置/批准仍不能迁移成新授权。

---

> **2026-09-14 / v1.72：整图仍未完成。本轮北侧源道路重建通过几何子项，但全部关联转向重建的整图试验明显退步，已经拒绝。** 保留v1.71研究件，不推广默认CLI，不提交git。下文v1.71及更早均为历史。

## v1.72 当前断点：不能再把“全部重算”称为“共同优化”

- **保留的研究件仍是**`out/node4-all-source-tail-readback-v171/node4-review.xodr`（SHA e05dcb85db2c913081cfb1de29d88a918c4e2df019c79684f9553ad02d27a5b0），它也不是合格交付。本轮拒绝件是`out/node4-north-source-dependents-v172/node4-diagnostic.xodr`（SHA **f1c9eb9405339855e980978617dfc4f06f11f69374f97d27d42e7aa5d7b31d51**）。`FINAL-REVIEW-STATUS.json`绑定实际XML、完整源/转角、北侧读回及esmini截图，明确不选用。
- 查到历史`constrain_flat_connector_port`对口部所有边界强加`t'=0`。这是项目假设，不是SHP真值或OpenDRIVE强制条件。取消该假设只是本轮**研究对照**，并非已经找到所有问题的根因。仍从同一原件、同一两处批准角色出发；没有修改默认CLI或直接改原地图顶点。
- **北侧独立几何件**：`out/node4-north-source-port-replayable-v172/road10.xodr`，SHA **4d3ef94de27dd23a75e82eeec1877bcf1c2b508e1de371906c79660d639d68c3**。一条**96.702154m Arc**参考线，164个共享三次系数，16个宽度区间、最短**5.513318m**。实际写出原件→边缘最大**0.350000000484m**，边缘→原件最大**0.349679250m**（0.02m求值网格，非新增写出原语、非连续证书）。实际XML内部G2、XSD1.5M和99个源速度记录读回通过；另有源状态的2处原零宽物理接点重放通过，不混称esmini端点读回。**源速度动态报告仍FAIL**，movement另验未完成。这不是可直接交付的独立道路或整图。
- 输入重建：旧`out/source-role-input-v151/`落后于完整端点库存实现，因此先从原v145地图重新准备`out/node4-north-source-v172/`。contact SHA **80075c6373744eb67e04629fcdba8e0f4055942b05bf487ae5d165c736e24994**，118源要素/3009原点，原scope/domain/run哈希未变。旧北2角色逐项核对原支持后迁移，新增授权为0。修复保存状态漏记`previous_source_directory`，独立重放现在可以按原迁移链加载，不能绕过“当前原件与旧包不一致”的保护。
- `rebuild_north_source_ports.py`按实际源ID和现有车道ID安装同一北路，不再重复应用旧rank映射；改变任一父端口必须覆盖全部**12条：100/101/102/103/104/105/106/107/114/115/122/123**。其他road逐字节不变，原traffic graph校验不变。每条仍3–5参考原语、最短≥6m；102初始化失败并保留旧曲线，明确`geometry_rebuilt=false`，不能算重建成功。12条全部尝试，仅101/103/114/115通过构造条件，另8条拒绝。
- **重要边界**：上述是“固定一份新父路后，把所有依赖分别求解”，**没有实现父路参数与连接路参数同时优化**。文件内明确`simultaneous_parent_connector_optimization=false`；所有失败曲线仅为同父诊断，不能将不同父状态下的成功曲线拼回去。
- **实际XML独立复核**：20条北/东关联转向并集，15条通过既有几何子项，失败**100/102/104/122/123**。构造拒绝的105/106/107在较宽的文件误差子项通过，也不升级为接受。全源反向转角仍失败**100/102/104/108/119/120/121/123**（8条）；不能用旧单转弯检查只报104/123来隐藏其余。东侧完整原件组合正向最大0.348917160m，但边界反向**0.455386982m > 0.35m**，同样拒绝。
- esmini144个边缘/中心接口中**12项失败**，最大位置差**2.347225m**、方向差**2.241133°**、曲率差约**0.00413043/m**；内部边缘失败11/12/100/104/122/123，最大曲率跳变约**0.981004/m**。XSD通过不能抵消这些失败。已实际查看20转向原件叠图、北侧全域/口部/收合原件图，以及esmini全貌/路口/斜视；蓝缝、北口西侧凸起仍在，整图不能选用。
- 受控初始化增加`long_source_seed.py`：有界TRF闭合、源中心引导的SLSQP、投影边缘前向性检查，长度仍≥6m、最多5参考原语；仅是初始化，不是可行或光顺保证。真实122两次试验`out/node4-north-bounded-seed-122-v172/`、`out/node4-north-nonfolded-seed-122-v172/`都拒绝。后一试验独立原件P95约**2.479m**，比旧曲线约0.908m更差；新增反向转角约12.447°仍失败。实际前后叠图已看，未混入上述全组诊断或保留研究件。单个局部求解失败不是一般OpenDRIVE不可表达的证明。
- **下一步工程应改变问题组织**：把北侧共享三次源状态的允许端口自由度、12条连接的长参考线/横断面放进同一受限问题；父路完整来源约束和原端点事件始终有效。保真作为优先目标，世界曲率能量不能靠将误差推向门槛获胜。端帽数量变化是当前表达模型的离散分支，必须先以有界可行初始化验证；不能只重跑冻结父路或增加迭代数。具体实施顺序见[实施计划v1.72](docs/实施计划-整路联动重建与编辑闭环.md)。此共同优化**尚未实现**。
- 原件/源速度/角色未修改，北2东4不重问，西2南1未扩大批准；不以这些待决事项阻塞已批准北/东的工程。MAP/CRS/整幅面域/源速度动态/movement/Web未升级。所有本轮求解和独立复核已结束；最后相关回归**137 passed / 42.01s**，包括同身份安装/模型分支、初始化、源角色与源速度、端口/世界G2、全源预算及同XML拒绝封存。不是全仓、MAP、动态或整图验收。

复跑必须用新输出目录，保留拒绝证据：

```powershell
.\.venv\Scripts\python.exe scripts/build_ordinary_source_road.py out/node4-north-source-v172 profiles/repair/node4-zero-width-source-roles-v1.yaml 10 out/<新北侧件> --previous-source-directory out/source-role-input-v151
.\.venv\Scripts\python.exe scripts/rebuild_north_source_ports.py out/<新北侧件> out/node4-all-source-tail-readback-v171 out/<新全依赖诊断> --workers 3 --max-iterations 60
.\.venv\Scripts\python.exe scripts/review_asymmetric_dependents.py out/<新全依赖诊断>
.\.venv\Scripts\python.exe scripts/review_north_rebuild.py out/<新北侧件> out/<新全依赖诊断>/node4-diagnostic.xodr out/<新全依赖诊断>/north-independent
.\.venv\Scripts\python.exe scripts/review_turn_shape.py out/<新全依赖诊断> out/<新全依赖诊断>/shape-review
.\.venv\Scripts\python.exe scripts/review_source_turn_budget.py out/<新全依赖诊断>/shape-review/report.json out/<新全依赖诊断>/shape-review/full-source-turn-budget.json
.\.venv\Scripts\python.exe scripts/capture_geometry_review.py out/<新全依赖诊断>/node4-diagnostic.xodr out/<新全依赖诊断>/esmini-review
.\.venv\Scripts\python.exe scripts/seal_parent_rebuild_diagnostic.py out/<新全依赖诊断>
```

这些命令用于复现研究，不是推荐重复同一失败试验。research脚本主动非零退出且明确REJECTED是预期状态，不要改为成功交付。

---

> **2026-09-14 / v1.71 当前结论：仍未完成整图交付。** 本轮补齐全源转角、原道路尾段的严格误差约束及端口代数消元；100/109/116的额外反弯通过独立复核，全源转角报警12→9。100/116的P95反而增大，不能只凭报警减少说“全面更好”。

## v1.71 当前复核件与下一步

- 研究件：`out/node4-all-source-tail-readback-v171/node4-review.xodr`，SHA **e05dcb85db2c913081cfb1de29d88a918c4e2df019c79684f9553ad02d27a5b0**。在v1.70同一父路状态只改100/109/116；其他道路、source ID、拓扑和速度逐字节不变。原104/106/117/123的几何/全源转角检查保持通过。未替换CLI默认输出，未commit。
- `source_turn_slacks`要求左/右/中心三个原始字段齐全；原S弯保留已有反向转角预算，不再以`single_turn=False`豁免。构造新增≤0.5°，独立实际XML诊断仍≤1°，不放宽门槛。封存`seal_geometry_review.py`现在**强制**同SHA全源预算，重算字段判定、核对完整road清单及失败汇总，不能用旧单转弯报告代替；最终仍`REJECTED_NOT_DELIVERY`。
- 稳定化：固定归一化求值网格的数量/端点/中点，网格密度根据搜索盒的原语长度上界确定；不是添加几何节点。缩放变量、局部步长盒、可行性筛选防止失败迭代替换可行锚；这是受限SQP工程防护，不宣称全局收敛。端口12个横向位置/一二阶导约束用三次基底首末系数代数消元，32维表达仅20独立优化变量。内部世界G2及参考终端仍显式约束，不用抹平接头掩盖。
- **一次实际拒绝促成的修正**：`out/node4-all-source-fair-readback-v171/`初版116消除了多余反弯，但东侧完整原件反向误差由0.349升到**0.525422m**，拒绝选用。原因是连接路只受via的宽松最大误差约束，忘了它接管的普通道路尾段仍属于0.35m严格来源域。现把原TOPO两端的实际边缘尾段直接加进联合问题，构造最大≤0.349m，最终仍0.35m；原始点保留、来源归属不改。
- 最终独立复核：100/109/116最大双向P95 **0.727157/0.226857/0.714561m**；额外累计反向转角最大 **-0.756517/0.500017/约0°**。三条各4参考原语，最短≥6m；各7宽度区间，最短≥3m，无逐源点短拼接。内部曲率差分别约9.60e-15/6.66e-15/2.20e-15每米。100/116原P95约0.274/0.208m，现为通过门槛的折中候选，**保真明显退步，需继续优化，不能当作已满意交付**。
- 东侧完整原件组合：源→文件最大**0.348917160m**，文件→对应原边界族最大**0.348945471m**，无未分配尾段；是0.1m采样检查非连续证书。4个已批准零宽事件、190源速度区间、XSD1.5M及144 esmini接口通过。231项绑定输入/代码核对未变化。
- 全20关联转向既有几何仍19通过、122失败；全源转角仍报警 **102/103/108/114/115/119/120/121/122**。已实际查看3条同尺度原件前后叠图、esmini全貌/路口/斜视：其他方向的蓝缝、外缘缺陷仍在。road11/12内部曲率跳变仍约0.109144/m；整幅面域、源速度动态、MAP/CRS/movement/Web均未完成。
- 122两次新试验都拒绝：`out/node4-all-source-fair-122-v171/`与`out/node4-all-source-tail-122-v171/`。后者虽压低反弯，实际源误差图仍接近1m；构造右边界反向P95 **0.974359m**，实际内部曲率差约**4.77026e-5/m**。仍4长原语，失败文件没有混入本轮复核件，不再靠增加次数或短段宣称解决。
- 新只读诊断`source-port-diagnosis.json`（位于后一个122试验目录）查全20转向：122从13连到10，北侧父路右边缘端口方向**89.089554°**，原边界末段方向**96.426931°**，差**7.337377°**；端点距离0.347124m已接近严格来源上限。这是父端口/来源耦合的线索，**不是无解证明，折线末段也不是已认证的平滑切向**。下一步应把北侧父路端口自由度与所有相连转向一起纳入原件约束，而非继续冻结父路只调122；不得单独移动连接路或自动扩大源角色。
- 下一步还需改进保真/光顺目标的优先关系：目前能量权重会把100/116误差推近门槛。应在全源、尾段、端口、G2、转角和长基底约束保持下做来源优先的受限优化，并明确动态约束；不能通过放宽容差/改源速度来掩盖。
- 相关回归 **180 passed / 36.12s**，不是全仓或MAP验收。所有本轮求解作业已结束；旧6处角色不重问，西2南1仍未获新增精确批准，本轮没有修改原件/角色/限速。图和机读封存见`comparison/`、`independent-review/`、`shape-review-sealed/`、`esmini-review/`及`FINAL-REVIEW-STATUS.json`。

复跑：`scripts/joint_fit_source_connector.py <同父复核目录> <新目录> --road <ID> --fair-world --resume-current-code`；随后同基底`restore_joint_connector.py`、同父`assemble_fixed_parent_trials.py`、全组`review_fixed_parent_replacement.py`、`review_turn_shape.py`和**强制**`review_source_turn_budget.py`、事件/真实截图/`seal_geometry_review.py`。保留所有失败目录，不覆盖历史证据。

---

以下v1.70及以前为历史：

> **2026-09-14 / v1.70 当前结论：仍没有整图交付。** 已把实际世界边缘/中心的曲率变化与反弯预算接入联合求解。104/106/117/123四条在既有几何和新增全源转角预算复核中均通过；116只通过旧单转弯适用范围，右边缘全源转角仍失败。不能把“5条通过旧单转弯筛查”说成五条完整光顺完成。

## v1.70 联合光顺的实际写出与扩展复核

- 当前**研究复核件**：`out/node4-world-fair-five-readback-v170/node4-review.xodr`，SHA **3564339af5221cd56871878bc06805a618781fafec2e76566495afa00e3e3fba**。five指改动5条转向，不是5段参考线。仅替换104/106/116/117/123；父路、其他转向、源ID/traffic links/速度保持不变。122保留旧v1.68，失败的122试验没有混入。不是CLI默认输出，未commit。
- `world_curve_fairness.py`直接计算`reference+offset`世界曲线的曲率变化能量。已有区间的多项式曲率分子求根，分出转向同号区间并累积反向角；16点Gauss积分按实际车道弧长积分。归一化求根/加密检查都不增加写出自由度。这是数值检查，不是区间算术连续证书。3核心默认行为保留，`--fair-world`仅研究脚本显式打开。
- 联合问题仍保留全原件/独立via、精确端口、内部世界G2、正宽、防折返及少段约束；原单转弯范围构造额外反弯≤0.5°，独立筛查仍1°；原门槛median/P95/max≤0.35/0.75/1.5m不放宽。目标同时含来源与世界曲率变化，不仅t''或参考曲率。`initial_coefficients_supplied`记录错误已修正；初始可行也执行光顺优化，不因已有候选跳过该阶段。
- **独立复核**：20关联转向中19条通过既有保真/G2/宽度/接口，仍只有122失败。104/106/116/117/123最高双向P95分别约**0.568/0.726/0.208/0.627/0.675m**；几何各3或4原语，最短≥6m，宽度6或7区间，最短≥3m。这些误差有的比旧件增大，不能说“完全复原”；保真与光顺权衡仍需全源检验。
- 104第一次写出内部曲率差**1.238e-7/m**略超1e-7，确实被拒绝。随后同基底精确约束投影只改变系数约4.67e-8，重新写出和复核通过。其余候选也经过独立投影/读回，不采用求解器success作通过依据。
- **旧适用范围的shape报警6→1，但这不是全形状通过**。查看完整原件叠图后补上S弯/复合曲线检查：保留原累计反向转角预算，额外>1°报警，不要求原S弯曲率同号。当前全源补充检查检出**100/102/103/108/109/114/115/116/119/120/121/122**共12条，不能把“不适用旧规则”当通过。116右边缘原2.269°、新7.559°，仍须修复。证据`shape-review-sealed/full-source-turn-budget-final.json`；旧单转弯报告保留用于解释差异。
- 已实际查看5条同尺度前后原件叠图及esmini全貌/近景/斜视，蓝缝、其他方向边缘缺陷仍在。最终图`comparison-final/comparison.png`标题改为**所有字段新增反向转角**，不再只显示单转弯适用字段而隐藏116右侧。XSD1.5M/144esmini接口通过；东侧完整原件组合误差0.348917160m/边界反向0.348917886m不变，4个原零宽事件/190速度区间通过。`FINAL-REVIEW-STATUS.json`明确REJECTED；新增`REVIEW-ADDENDUM.json`绑定全源预算报告（SHA 908408a09b690d5f2a9c85b58f5cd977930e90b994629adcf9f7daf308534e82），明确12条报警不能被旧shape字段覆盖。
- `assemble_fixed_parent_trials.py`允许合并不同转向的同父候选与其精确投影子链：逐层核对输入地图/报告hash、全部父路及未改转向、非几何信息，拒绝跨父状态或重复转向。历史实现变化须显式`--allow-current-code`记录旧/新hash；原件/配置/批准文件变化仍拒绝。此迁移是重新复核，不把旧结果冒充当前代码生成。
- **122试验仍未解决**：`out/node4-world-fair-122-v170/`四原语联合光顺虽把反向角压至约1.3–1.5°，但右P95约0.94m/端口及内部残差失败；`...122-readback-v170/`保留独立拒绝。5原语模型预算辅助已实现和单测：只把≥12m核心无损拆为两个≥6m长区间，整个初始化曲线不变，再释放少量自由度，最多5原语/8宽度区间。v1.68种子没有≥12m核心，首次初始化诚实拒绝。前一诊断种子的五原语对照`out/node4-world-fair-122-five-core-v170/`也已结束并拒绝：80次迭代无可行状态，实际内部曲率差**0.026022626/m**、独立0.02m源叠图最高P95约**0.858m**，最大反向角约**16.443°**。已看该图，不选用；SHA **700f8d6efd921b710790d05149398d75186d830204006ff6a649afea98b6ae5b**。不存在“多加一段就已解决”的结论。
- 新增西2南1仍为**待明确确认**，“继续”没有被当作扩大精确源角色批准。北2/东4不重问，不改源/速度/CRS/MAP语义。road11/12、完整路面、全源形状、源速度动态、movement另验、MAP及Web仍未完成。
- 最终相关回归 **164 passed / 35.58s**；compileall/diff检查通过。研究复核件绑定的**227项输入/代码**复查无变化，输出SHA一致；原件、角色配置与速度未改。无仍运行的本轮求解作业，未commit。不是全仓/MAP或整图验收。

下一步：先将全源转角预算（包括原S弯的既有转角，不是强制单号）纳入求解与最终门禁，避免只优化旧6条；求解器需采用约束保持/信赖域等稳定化，不靠反复放大无界横断面系数搜索。完整来源、父端口依赖与少原语预算不放松。五原语尝试只验证一个有限模型，失败不是一般无解证明。

复跑均用新目录：`scripts/joint_fit_source_connector.py <输入复核目录> <新目录> --road 122 --fair-world --resume-current-code [--core-count 4]`；候选仍须`restore_joint_connector.py`、`assemble_fixed_parent_trials.py`、`review_fixed_parent_replacement.py`、`review_turn_shape.py`、`review_source_turn_budget.py`与真实截图复核。不要重跑旧目录覆盖失败证据。

---

以下v1.69及更早内容为历史：

> **2026-09-14 / v1.69 当前结论优先于以下历史记录：没有平滑合格的新地图。** 新候选虽通过20转向的既有误差/G2子项，实际看图及新增反弯检查仍失败，明确拒绝。四向原件已一次查全，只有新增西2南1角色待决定；北2/东4不重问。不推广CLI、不改原件或源速度、未commit。

## v1.69 当前断点与本轮证据

- 拒绝诊断：`out/node4-fixed-parent-v169-final/node4-review.xodr`，SHA **15805f99f8501ac518253c6019cf8dbc6da6b8babb67b2e4888a426f57ed5574**。目录名final不是验收结果；最终`FINAL-REVIEW-STATUS.json`为**REJECTED_NOT_DELIVERY**。旧v1.68不覆盖，新件没有选为更优输出。
- 122参考线/宽度联合32变量，第一版0.5m检查得到via右P95=0.749m，独立0.05m读回0.754387m仍失败。改0.1m构造网格和0.02m收紧余量后贴线通过，但内部曲率跳变0.000128158/m；再在同一长基底投影到精确端口/世界G2约束，系数最大改动0.000104，不移动父路或整个连接路补缝。求解器success/投影成功均不自动赋予验收。
- 最后20关联转向的完整来源/独立via/正宽/无折返/内部G2/接口子项通过；122右P95源→文件0.699043m、反向0.726814m、via单独0.742894m，最大0.817448m，门槛仍0.35/0.75/1.5。122仍4原语(6.025288/13.502436/6.016385/6.000089m)、7宽度区间≥3.000044m，不是逐原点拼线。
- **实际看图后否决**：122边缘产生原图没有的反弯。新增`review_turn_shape.py`对原件明确单转弯范围，比较实际XML世界边缘航向与原段航向的累计反向转角，0.02m检查、不增写出段。**104/106/116/117/122/123**共6条被拦。122右原源约0°，新件约70.720°，旧v1.68约10.600°；新中心约42.377°、源0.394°。更小P95不能抵销反弯劣化，新件拒绝选用。其余曲线不因未报警就等于完美。
- 新检查仅用于源净转角≥30°且源反向累计≤0.5°的范围，新增>1°报警；是研究阶段保守诊断，不是标准条款/连续光顺证书。原S弯或直线返回需另验，不强制同号。证据`shape-review-sealed/report.json`和`reverse-turns.png`；同规则旧件复核`out/node4-v168-shape-review-v169/`也拦同6条。
- XSD1.5M/144esmini接口仍通过；东侧完整来源组合最大0.348917160m、边界反向0.348917886m不变，4处零宽实际XML事件及190速度区间通过。esmini完整/口部/斜视截图均已实际查看，仍有西南口部蓝缝与旧外缘缺陷。road11/12内部边缘、整幅面域、源速度动态仍失败；MAP、CRS、movement另验、Web未完成。
- **一次原件清查**：`out/node4-all-source-v169/`覆盖四向120源车道/240端点、171边界、291要素/6290原点，24原TOPO转向MATCH，9处零宽全入库存。北2/东4旧批准只列出，不重复批准或自动迁移。新增仅西`2023041111104060474/start`(1.566211m)、`2023041111104128071/start`(1.767185m)，南`2023041810470164021/start`(1.828769m)；直接pyshp核实原零宽、SIDE、同节点同坐标，原图已看。新增请求在`pending-review-final/pending-roles.json`，**尚未批准**。全包contact SHA **2030a2aefb440c203ac4278214f52f9fa55ff492ca4dbc250eeb8c782086c6b4**；细节见资料盘点第十一节，不代表全部SHP地图已查全。
- **下一步工程**：将真实世界边缘/中心的曲率符号、允许拐点、曲率变化能量与完整来源/精确端口/正宽/防折返/少原语约束放进同一联合求解；不能只最小化点误差或参考线曲率。新反弯门禁已实现，**此光顺联合修复尚未完成**。原始文献及具体路线见实施计划v1.69。122等反弯是工程责任，不要求用户调参。
- 西南整路重建需新增3项的精确角色决定：原边界作物理道路，完整原车道线作为通行路径另验；此前不得删除或降权这些原路径以求解11/12。获批后绑定各源包并重建所有依赖，不混用不同父路状态。原已批准6项不重问。
- `require_fixed_parent`核对未选road/header/junction逐字节不变，所选连接的source ID/速度/traffic links不变；父路一改需全依赖。`research_code_revision.py`仅显式记录仓库实现.py版本变化，原件/配置/批准/输入地图变化仍拒绝，不伪装旧代码hash为当前。没有仍运行的本轮求解作业。
- 本轮最终相关回归 **138 passed / 34.74s**，包括固定父路替换保护、原件端点全量清查、同基底G2约束投影及反弯检查。此结果不等于整图/MAP/全仓验收；新地图仍为拒绝状态。

复跑诊断（新目录；最终状态始终由独立检查决定）：

```powershell
.\.venv\Scripts\python.exe scripts/joint_fit_source_connector.py out/node4-fixed-parent-v169 out/<新联合试验> --road 122 --resume-current-code
.\.venv\Scripts\python.exe scripts/restore_joint_connector.py out/<联合试验> out/<新投影试验> --road 122
.\.venv\Scripts\python.exe scripts/review_fixed_parent_replacement.py out/<对应同父基线> out/<试验> out/<新全组复核>
.\.venv\Scripts\python.exe scripts/review_turn_shape.py out/<复核> out/<新形状检查> --allow-current-code
```

---

以下v1.68及更早内容为历史：

> 更新日期：2026-09-13。项目已落位 `F:\MapFactory\`，当前方案为 **v1.68 东侧不齐端部已写出，完整源尾段联合覆盖通过采样复核；20关联转向19条通过，整图仍拒绝**。新复核件见下，旧v1.65不覆盖。下一步是122转向及11/12普通路与面域，不再重复东侧四处角色审批或共同端面研究；未获其他车道或MAP语义授权。
> **完整交接见 [docs/交接文档-mapforge.md](docs/交接文档-mapforge.md)**（架构、验收方法、复跑命令、遗留事项）。
> **遗留工作总排期见 [docs/遗留工作全面规划-2026-08-14.md](docs/遗留工作全面规划-2026-08-14.md)**。
> **新会话请先读本文件，再按第三节的待办继续工作。**

---

## 〇、当前实现状态与实时交接

### v1.68 当前复核件：东侧来源已覆盖，整图尚未通过

- 文件：**`out/node4-east-north-dependents-v168/node4-review.xodr`**，SHA **9110dda090c9a97c7e57e1361b43cfa34927d5623f0a35877d3844c995dc95c4**。仅研究复核，**没有推广默认CLI、不是合格交付**。保留所有旧输出与原始资料，四处精确角色批准未扩大，未commit。
- 东侧不再强迫双方向同起点。共享连续laneOffset锚定实际长侧边界，外端**24.667293m**仅写真实存在的一侧，不补长零宽伪车道。单条road13共**190.409591m，一条Line参考原语**；共享三次状态296系数，33个写出宽度区间，最短**5.552347m**。结构性侧向起点进入共同基底，斜端厘米级尾帽跨该站仍是同一多项式，不增造短自由度或逐点参考线。
- 口部选择共同长区间的合法截面，原始末端不删除：6条源车道链的剩余尾段通过原TOPO分配给12条东侧关联连接。保留北侧v1.65同一父路状态，与东侧一起重建**20条关联转向的并集**，没有混用不同父端口候选。源40/60kmh分段写入lane.speed，源连接限速60保留。
- 独立文件读回：`independent-review/east-full-source-aggregate.json`包括全部road13原边界与物理中心顶点/原段（0.1m加密），源→文件最大**0.348917160m**；同一原边界族的文件→源最大**0.348917886m**；尾段无未分配项。**反向数值只针对物理边界，不包括通行路径或中心的反向证书**，四movement仍另验。孤立component的完整来源检查曾最大6.909m失败，正是尚未接管尾段，不能用孤立件当完整来源PASS。
- `independent-review/report.json`：1.5M XSD通过；esmini144个边缘/中心接口通过（最大约0.004914mm、0.0000172°、曲率差4.45e-9/m）。20条转向逐条文件/source/宽度/防折返/条带/接口检查，**19 PASS，122 FAIL**：右边界P95源→文件0.880459m、反向0.906917m，最大0.977527m；门槛仍median/P95/max≤0.35/0.75/1.5m，未放宽。参考原语3–5条、最短6m，宽度仍固定长基底，不是源点拼线。
- 整图内部边缘失败已从11/12/13缩到**11/12**，最大曲率跳变0.109144/m；截图仍见西/南口部蓝缝与旧方向缺陷。已实际查看完整SHP叠图、20转向叠图、esmini全貌/外端/路口近景。**接口PASS不等于面域/控制器PASS**。北侧父几何为原样保留的历史状态，不冒称当前重新完整重建了北侧源模型；MAP未重跑，源速度动态/CRS/Web未完成。
- 原件/配置/当次实现与输入输出绑定219项hash；新独立复核重新建立当前东侧模型、核对model SHA并读实际XODR，明确记录相对component运行后的代码变化。早期`out/node4-east-asymmetric-v168/`因XML声明UTF8兼容问题失败，修为UTF-8后新目录`...-utf8/`成功，不覆盖失败记录。该component仍不是完整地图。
- 单Arc坐标轴替代122的对照（`out/node4-single-arc-122-v168*`）**拒绝且未选用**：紧0.70m构造管域不可行，1.45m构造上限候选独立源误差更差且中心接口曲率失败（约8.34e-6/m）。最终验收0.35/0.75/1.5未改；这不是一般单Arc无解证明。脚本保留作负例，不进入默认路线。只对齐两边缘并不保证换参考轴后的中心G2，也不能拿参考原语更少替代实际验收。
- **下一步**：在这组固定来源和已接通的写出/联合读回链上处理122的曲线基底与共享端口条件，同时展开11/12的完整原件重建；父端口若改变必须重建全部受影响转向。随后验整个路面覆盖与外缘，才能推广MAP/七路口/Web。不能再把东侧“没有可写出域”当当前阻断，也不能挑单条成功替换不同父状态。

复跑（均新目录，最终报告仍决定拒绝/复核状态）：

```powershell
.\.venv\Scripts\python.exe scripts/build_ordinary_source_road.py out/node4-east-source-v167 profiles/repair/node4-east-zero-width-source-roles-v2.yaml 13 out/<新东侧目录> --line-axis --asymmetric-domain
.\.venv\Scripts\python.exe scripts/rebuild_source_dependents.py out/node4-east-asymmetric-v168-utf8 out/node4-world-g2-regular-shared-parent/node4-review.xodr out/<新联动目录>
.\.venv\Scripts\python.exe scripts/review_asymmetric_dependents.py out/<新联动目录>
.\esmini\bin\odrviewer.exe --odr .\out\node4-east-north-dependents-v168\node4-review.xodr --density 0 --ground_plane off --camera_mode custom_fixed --custom_fixed_camera "0,-50,600,1.5708,1.45"
```

图：`independent-review/east-aggregate.png`、`all-turns.png`及`esmini-review/`。本轮最终 **111项相关回归通过/28.60s**，compileall及diff检查通过；不是全仓/整图/MAP验收。补做`check_asymmetric_written_events.py`：直接解析最终XML的4处原零宽左右边缘位置/切向/曲率均通过，190个写出车道速度区间逐项等于原40/60事件；219项绑定hash复核不变，证据`independent-review/written-events.json`。这次是真实写出事件复算，不再只读源状态系数。没有仍运行的本轮作业，未commit。

### v1.67 当前断点：东侧完整形状可解，写出器的共同端面假设仍需修正

- **已批准范围**：上轮用户同意东侧三处零宽角色解释，本轮独立核查后用户答复“同意，仅补这一处”，仅新增`2023061915261945676/start`。新`profiles/repair/node4-east-zero-width-source-roles-v2.yaml`精确列出四处并绑定新contact内容SHA **f45c41aa5fbd53117fe3ab00c3f82a1412b932d9bca81657245c4bcb12b2379b**；原v1/北侧决策不覆盖。原路径保留为movement另验，不删除、不扩大到其他车道/MAP。
- **修复漏检**：原角色检查嵌套于TOPO过渡事件，漏掉包外端。现`source_contacts.py`检查全部保留源车道的两个端点，包含固定/连接支持，且不凭零宽增造TOPO。独立pyshp读取本包57车道/114端点（其中road13自身33车道），明确零宽恰4处；第四处左右边界原节点`2023061915241735288`完全重合，原车道线起端偏离**1.862619785m**。准备包`out/node4-east-source-v167/`仍142要素/3178原点、29事件、24原转向MATCH，无源结构遗漏。不能将这次检查称为整个SHP数据集的全量清洗。
- **最终共享源状态**：`out/node4-east-shared-line-v167-all-tapers/`使用1条Line参考轴、289个共享三次系数，独立宽度区间最短**5.552347m**。完整边界同坐标轴偏差上界**0.350000m**，29条物理中心完整来源保守上界**0.172898m**、未覆盖区间0；C2残差2.28e-13，最小宽度约-4.39e-15m（数值零）。四条movement保留另验。不是逐点短线，不是世界双向文件误差证书，不是动态或整图PASS。
- **补齐第二层漏传**：此前虽查到了外端零宽，但`physical_continuations`仍只从TOPO事件产生G2。现用原SIDE/同节点/同坐标零宽证据单独增加物理端点G2，不合并重叠边界family、不增造traffic link。重新求解后，`review_ordinary_source_state.py`重建当前模型、校验模型SHA，并直接读标量样条系数算四处世界位置/航向/曲率差，4/4通过（最大约1.78e-15m/1.78e-15rad/8.87e-16每米）。这是源状态读回，**不是XODR或esmini读回**。
- 最终复核在`out/node4-east-shared-line-v167-all-tapers/replay/`：59项当前输入/配置/代码/状态文件hash复查未变，报告SHA **733269973693c6be699e977adbe70f283e0c4bf297a2587a5a3eb8328e0564b9**。物理端点检查在主求解后增加了非重合源节点拒绝检查，复核报告明确单列旧/新代码hash；重建模型完全一致且当前约束复算通过，未把旧代码hash当当前。
- `out/node4-east-shared-line-v167-approved4/`和单Arc对照`out/node4-east-shared-state-v167-approved4/`均是**补齐外端G2之前的诊断**，不作为最终平滑状态；保留不覆盖。Arc的277系数结果仅说明当时模型可解。
- **未生成新XODR的具体原因**：原两方向外端相差约24.6m，路口口部原端点也不齐。当前`transverse_domain/compile_road`强制双方共同端面和每区间双方向，Line方案在口部会丢掉约**0.928215m**原边界端长（超过同一0.35m二维预算）；Arc方案外端也失败。检查在写出前拒绝，完整源状态/原点均保留。此处是写出模型限制，不是一般OpenDRIVE不能表示，也不是再调122初值可解决。
- **下一步工程，不需再问四ID**：允许真实单侧存在区间；用同一共享参考轴表达两侧源域。口部普通路与连接路的来源覆盖应按原TOPO联合分配，所有原尾段仍进入整图双向验收；不得裁断原料、补长距离零宽假车道、拿连接填色代替道路、把短原点间距变参考原语。若需切road，必须结构性分段且参考线/车道接头合法、来源台账完整；先验证1.5M与esmini兼容性，不能直接借1.8新元素。当前这一写出/全依赖联动**尚未实现**。
- 新源速度事件按原始节点与Profile字段映射保留40/60km/h分段，只写`lane.speed`，不新增长度原语或宽度区间。当前真实共享状态已消费，写出层合成测试通过；**新真实XODR属性和动态仍未验收**。
- 相关回归包括北侧真实重建、外端漏检/G2传递、反向数字化、未知宽度、节点错配、三处不自动变四处、源速度事件、直接系数真值测试（最终计数见下方复核结果）。旧v1.65文件SHA仍 **65796005fd40644c296282bd902ba46753a2370219da35fedbdc8f2cc3e370d8**，11/12/122失败结论不升级。已看原件四处近景、最终Line全源叠图与口部图，**没有新esmini截图或新地图交付**；未commit。

复跑（新输出目录）：

```powershell
.\.venv\Scripts\python.exe scripts/build_ordinary_source_road.py out/node4-east-source-v167 profiles/repair/node4-east-zero-width-source-roles-v2.yaml 13 out/<新目录> --line-axis
.\.venv\Scripts\python.exe scripts/review_source_role_conflicts.py out/node4-east-source-v167 --output out/<新原件复核目录> --decision profiles/repair/node4-east-zero-width-source-roles-v2.yaml
.\.venv\Scripts\python.exe scripts/review_ordinary_source_state.py out/node4-east-shared-line-v167-all-tapers out/<新状态复算目录>
```

这些研究命令不能声称交付，状态看报告。完整源状态在最终Line目录的`shared-state.json`；复核报告/六视图为`replay/report.json`和`replay/source-state.png`。旧contact包因补全端点库存会和新编译器内容hash不同；旧命令不能再声称fresh-match。`rebind_unchanged_source_decision`仅在原scope/domain、TOPO、完整来源和既有冲突证据不变时显式迁移原批准，新增冲突仍阻断；原历史包不重写。见[实施计划v1.67](docs/实施计划-整路联动重建与编辑闭环.md)与[资料盘点第十节](docs/资料盘点-金凤示范区数据与标准资料.md)。

本轮最终回归：**102 passed / 35.52s**；覆盖`test_physical_continuations`、`test_source_contacts`及real、`test_source_roles`及real、`test_source_contact_fit`、`test_source_role_conflict_readback`、`test_east_source_role_scope`、`test_source_speed_events`、`test_source_transition_domains`、`test_source_cubic_export`、`test_ordinary_source_replay`。compileall和diff检查通过，未commit，无仍运行的MapFactory求解作业。只表示这些回归和源状态复核，不是MAP/全部路口/完整仓库测试或新地图交付。

### 历史v1.66：东侧源角色问题在求解前阻断（已由v1.67接续）

- 按上轮下一步展开road13完整原件及12依赖转向，准备包`out/node4-east-source-v166/`：57源车道、85物理边界ID、142完整要素、3178原始顶点；29源事件（26普通延续、2零宽出生、1零宽消失）。原TOPO路口24转向仍MATCH，来源结构无缺失。此处是输入准备，不是新几何。
- 新发现的3个具体ID不在北侧已批准两ID中：`2023041111113051690`起宽0、路径端点离边界收合点**1.768531m**；`2023041111104112823`起宽0、**2.343632m**；`2023041810460339677`末宽0、**1.757130m**。
- 新`review_source_role_conflicts.py`不调用Profile几何重建或拟合器，直接pyshp读原DBF/SHP记录号、SIDE关系、全部原始点与边界端节点。三处边界两端点坐标完全相同且节点ID相同，独立原记录和准备包逐项一致；不是将原0当缺失，也不是最近线配错。完整原路径和端点近景已实际查看。
- 仅在“车道线端点必须与零宽物理中心绑定为同一点”的条件下，双方各0.35m预算不相容：所需共同半径至少0.884266/1.171816/0.878565m。**不判定哪个源图层错误、不证明一般道路重建无解，也不证明这就是122偏差的全部原因。** 需要新增源角色决定后才能完整联动road13；不能先把这3条中心约束自动降权/转movement来求解。
- 核查证据：`out/node4-east-source-v166/independent-role-review-final/report.json`及`raw-source-conflicts.png`；contact内容SHA **d0ecca3b7554da7f6d257853e3e7ead45a2e408f8e524da5e41ba3172cfb15d3**。初版独立检查图保留在相邻目录作历史，不覆盖。原件事实同步资料盘点第九节。
- 待用户明确：是否**仅新增这3处**沿用北侧处理——原左右边界定义物理道路，原车道线不删除、改列为通行路径观测另行验证。若获确认，写精确绑定该版本的新决策文件，再实施road13共享边界/端口及全部依赖转向联动。若不确认，保留现合同并等待可信源修正；不重新跑同条件局部优化冒充进展。
- 本轮无新XODR、无新esmini渲染、不改源/旧文件/源限速/角色；旧v1.65文件SHA仍 **65796005fd40644c296282bd902ba46753a2370219da35fedbdc8f2cc3e370d8**。129相关测试通过，未commit。MAP、其余方向、整幅路面、动态、CRS、Web仍未完成。

复跑独立原件核查（输出目录必须新建）：

```powershell
.\.venv\Scripts\python.exe scripts/review_source_role_conflicts.py out/node4-east-source-v166 --output out/node4-east-source-v166/<新的复核目录>
```

### v1.65 保留复核件：同一北侧状态的12转向重建，11条通过，整图仍拒绝

- 保留复核件：`out/node4-world-g2-regular-shared-parent/node4-review.xodr`，SHA **65796005fd40644c296282bd902ba46753a2370219da35fedbdc8f2cc3e370d8**。这不是默认CLI新交付，也不是自动驾驶可用证明。旧v1.64文件未覆盖。
- 根因修复：原连接横断面只由端点决定、内部强迫`t'=0`，短转弯鼓包；现由完整原左右边界及中心共同拟合，允许横断面二阶导数在参考曲率变化率接头处作正确补偿，实际左右边缘/中心保持世界G2。固定结构基底，不按原点加自由度。新增折返门禁`1-k*t>=0.1`（内层连续Bernstein界，写出后独立四次多项式极值）；只检查正宽度会漏掉小圈。修复参考线/宽度站位浮点往返造成的伪微区间，未放宽曲率跳变门槛。
- 原v1.64共同零横向斜率端口在旧算法下失败；本轮在**该同一共享父路状态上重建全部12条**，没有拼接不同父路状态的成功连接。写出的连接参考线为**3–4个原语，最短6m**；宽度**6–7区间，最短3m**。不是逐点短线，也不能说完全不分段。北侧仍单96.702154m Arc、16宽度区间，最短5.513318m。
- `review-validation.json`：XSD PASS、ID/link diff为空、esmini144接口PASS（最大0.931290mm、0.001165°、曲率差4.41e-9/m）；12关联连接的内部边缘、正宽、防折返及0.05m采样条带检查通过。**完整来源门槛仅11/12通过**，仍是各方向median≤0.35/P95≤0.75/max≤1.5m，原via另外统计，不能冒充最大0.35m。
- 122（road13东进口→road10北侧）仍拒绝：右边界源→文件P95 **0.854202m**，文件→源P95 **0.870733m**，完整最大 **0.945610m**；数值变小不等于通过。32变量参考线+宽度联合求解和独立可行性恢复也做了，`out/node4-joint-world-g2-feasible/`仍拒绝、未选用；迭代停止/恢复器success不当约束成立，更不据此宣称一般无解。
- 在最终文件上重新读取北侧源模型：`north-source-readback.json`完整源边界/物理中心→文件最大0.350000m，反向边界0.349812m（采样审计，不是连续双向证书）。52项来源hash不变；12连接按真实SHP保留60km/h，速度属性PASS，动态均FAIL。没有借降速、改源、扩大角色或改变MAP宽度过关。
- 实际查看了`all-connectors-readback.png`、esmini完整俯视和路口近景。当前转向没有早先的局部小圈；**西侧两幅路长不齐、口部蓝色空隙、其他方向边缘仍未修好**。整图内部边缘失败仍在road11/12/13。旧拒绝基线有千米级数值漂移，叠图只按完整源和当前文件定视域并标注旧线超界；验收计算没有裁切。
- **下一步不要再只调122初值**：把road13完整原始边界、端口与road10口部纳入共享变量，并重建所有被改变的依赖转向；已有`joint_connector_fit.py`是连接路内部32变量联合，不是整图联合。随后重建11/12并验整幅面域，才扩MAP/七路口/Web。MAP本轮未重跑，旧结论不升级。
- 113项相关回归通过；新解析真值测试替代了误用旧负宽候选为真值的试验夹具。未commit，无运行中的mapforge作业。保留各拒绝试验作诊断，不向默认输出推广。

复核/观看（不跑车）：

```powershell
.\.venv\Scripts\python.exe scripts/review_source_connectors.py out/node4-world-g2-regular-shared-parent --parent-state out/node4-geometry-shared-port
.\esmini\bin\odrviewer.exe --odr .\out\node4-world-g2-regular-shared-parent\node4-review.xodr --density 0 --ground_plane off --camera_mode custom_fixed --custom_fixed_camera "0,-50,600,1.5708,1.45"
```

本轮完整重建命令：`python scripts/refit_source_connectors.py out/node4-geometry-shared-port/node4-review.xodr <新目录> --parent-trial --world-g2 --roads 122,123,104,100,105,101,102,103,106,107,114,115`。`--parent-trial`强制全部依赖一起重建；参数续算校验父版本hash。图像用`capture_geometry_review.py`重新生成，绑定实际文件SHA。执行细节及数学约束见[实施计划](docs/实施计划-整路联动重建与编辑闭环.md)。

### 历史：v1.64 北侧写出成立，整图仍有明确失败

- 用户在“先完成一个真实路口几何，不再用恒速60舒适性阻塞所有工程实施”的提议后答复“是的”，随后要求继续。**这是几何优先实施授权，不是动态PASS、源限速修改或MAP名义宽度解释授权。** 上轮goal的BLOCKED是历史工具状态；本轮已按用户新指令恢复工程工作，不重复问速度。
- 新`source_cubic_export.py`直接编译共享三次边界，非五次曲线采样回写。保留原TOPO、边界身份和共同区间，车道宽度是相邻物理边界之差；一条双向road、参考线左右展开。最终保留研究件`out/node4-geometry-cut-aligned-long-widths/north-road.xodr`：**一条96.702154m Arc参考线；16个独立宽度区间，最短5.513318m；164系数**。不能把“一条参考线”说成“宽度不分段”；显式`--minimum-width-span 5.5`是本轮宽度复杂度设置，不是默认主线升级。
- 实际修复①：原Line编译统一断面丢失斜切源端点，完整文件读回误差曾1.020597m。端点决定单Arc坐标轴，并把52条写出端点约束计入同一0.35m二维预算后，**原始边界/23条物理中心到文件的完整顶点及加密原段检查最大0.3500000005m，反向边界0.349679m**。这是有采样步长的文件审计，不冒充连续双向证书；2条已批准movement仍另待验证。原点未删、未外推原料、未放宽预算。
- 实际修复②：道路延长后，端口`source_lane`还指向旧记录，106/107的来源走廊错误多计了约6m已由普通路覆盖的段。现在按原TOPO链与新端口实际支持域选择原记录，保存old/current来源映射，不猜拓扑。实际修复③：`flat_join_spline`用含大常数的残差相减构造线性矩阵导致有效数字丢失；改直接线性算子、物理单位检查和迭代精修，101重建后不再沿用断开的旧连接。
- **速度纠错**：只读旧v1.45的连接路含约11/12kmh生成限速，而原始SHP为60。不能“保持旧文件不变”就宣称保源。新`node4-source-speed-review.xodr`的12条关联连接路全部按原SHP恢复60，写出属性检查PASS；历史候选15kmh动态结果不再是权威验收。北侧原限速一直60；其余旧道路未当成本轮重建成果。
- 最终保留复核件：`out/node4-geometry-cut-aligned-long-widths/node4-source-speed-review.xodr`，SHA **366a6979c941f276e8a92f86e7944efc04b2b35d3d73c541cbf2368a820d86b2**。XSD PASS；esmini144接口PASS（最大3.823352mm、0.013215°）；北侧184内部边缘接缝通过（曲率跳变约2.0e-15/m）。**12连接仅7条通过既有几何候选条件**，不是都满足0.35m最大误差：连接用原median≤0.35/P95≤0.75/max≤1.5m门槛。100/104/105/122/123仍拒绝，最坏贴源约5.006m。
- 整图内部边缘仍FAIL，来自尚未重建的road11/12/13，最大曲率跳变0.109144/m。北侧自身源60的ay约10.597、jerk56.265仍FAIL；12转向恢复真实源限速后动态也FAIL。整图局部坐标可读，不代表绝对CRS通过（esmini仍报不支持部分geoReference参数）。**仍为REJECTED_REVIEW_NOT_DELIVERY，无默认管线推广、无正式交付。**
- 图已实际查看：`north-readback.png`、`north-esmini.png`、`esmini-review/esmini-three-views.png`及原尺寸路口图。北侧表面连续；路口仍有尖角，旧方向口部仍有蓝色空隙/西侧两幅路长不齐。不能拿接口PASS当路面/边缘/控制器PASS。
- 另试`out/node4-geometry-shared-port/`：共同零横向斜率端口不加变量、仍贴源，但只3/12连接候选通过，部分三原语横断面出现严重病态放大；整组**拒绝、不选用、不截图美化、不推广**。不要挑其中单条成功覆盖保留复核件，造成不同上游状态混用。
- 86项相关回归通过；compileall/diff检查通过。源材料及旧v1.45 SHA `abf59437b504c8b3fd6faa8cb439632906b7a5b7538a00b48bf6ad477709b9c9`未改，未commit。检查报告`review-validation.json`含52项输入hash及本轮复核代码hash（不是声称整个运行环境的完整封存）。
- **下一处真正闭合点**：让同一上游状态的全部12转向同时满足完整来源/非负宽/曲率接续及段数预算，尤其短转向104/122/123；不能逐条换用不同端口候选。之后重建11/12/13并检查整幅路面覆盖，才扩MAP和七路口。端口联动已经接到实际文件，剩下不是继续生成LP研究报告。MAP来源宽度、两movement、绝对CRS及Web仍未完成。

复跑（显式研究输出，新目录；旧正式输出不覆盖）：

```powershell
.\.venv\Scripts\python.exe scripts/build_source_geometry_candidate.py out/node4-geometry-replay --source-end-axis --minimum-width-span 5.5 --junction
.\.venv\Scripts\python.exe scripts/build_source_geometry_candidate.py out/node4-geometry-replay --verify-only
.\esmini\bin\odrviewer.exe --odr .\out\node4-geometry-cut-aligned-long-widths\north-road.xodr --density 0 --ground_plane off --camera_mode custom_fixed --custom_fixed_camera "0,72,240,1.5708,1.5707"
```

当前代码已修101的数值问题且从求解前就恢复原限速；保留证据包是主运行后重试101、另写恢复限速文件的历史。重新运行以新目录新报告为准，不能保证优化器数值轨迹逐字节一致。研究CLI成功退出只说明报告生成，**交付状态以报告中的拒绝项为准**。

### 历史：v1.63 收尾复核（已被上面用户授权的几何优先工作接续）

- 2026-09-11：从当前只读原件重新构建普通路径图，与v1.62证据包逐项一致；重新构建必要条件方程并检验保存的10组原/对偶证书，全部数值校验通过。三条链在1m检查下的jerk必要下界仍为4.286743627、1.638831761、1.295433956m/s³。这里没有复用旧实现hash冒充当前实现，也没有生成曲线；v1.63最终包204项输入及输出hash复核未变。
- 从v1.62首次请求到v1.63及本次复核，普通分合流的设计验收工况仍未确认。v1.63已完成可独立推进的源端部覆盖修复；当前0.35m完整贴源、恒速60km/h、ay≤2.5和jerk≤1的组合仍有来源级必要条件冲突。不能擅自降速、放宽贴源或扩大两个已批准角色；这些阈值是当前工程合同，不是OpenDRIVE规范本身。
- MAP的laneWidth是否允许解释为名义值并显式推断变宽也仍未确认，现有常宽门禁不变。不能自动借用SHP边界或改变来源身份。两个待确认项分别约束SHP和MAP，不将它们混成一个几何算法问题。
- 长期目标现已标记为 **BLOCKED（未完成）**，当前无后台作业，未commit、未生成新XODR。本次只做只读复核和交接状态同步，不新增算法版本，也不重复运行同条件优化。
- 恢复条件：项目方明确普通分合流的设计验收工况（源60km/h限速字段仍原样保留），或给出可信源修正/批准的来源解释；MAP继续前另需明确宽度合同。收到确认后仍须完成共享可写出整路、全部连接、独立读回和截图验收，**不能承诺仅改变验收条件就已完成地图**。本条状态优先于下面各轮历史的goal active记录。

### v1.63 当前断点：完整中心观测已补齐，动态仍不合格

- 新`source_center_support.py`把普通TOPO上的完整中心观测对应到已有连续物理路径，外端按世界二维距离扣除纵向占用后的同一0.35m预算约束；不外推边界、不删点、不按最近异车道匹配。Line/固定Arc共享模型约束及独立审计均接入。
- 原14条记录端域未验收问题：73个完整来源区间、12个外端预算、1个约2nm原接触预算，未解决区间0。23条物理中心观测完整原点/原段覆盖通过；2条已批准movement观测仍待单独验证。是来源到中心的保守证书，不是反向/面域/整图全部通过。
- 原156系数/41长区间/最短18.13m不变，新增几何段0。旧参数完整对应误差上界0.363338m；新约束求解后再做12次相同60kmh动态改进，最终来源上界0.35、宽度数值零、C2约2.27e-13；2cm ay4.228166/jerk6.511206仍FAIL。初始仅来源可行曲线曾jerk34.24，不当修复成果。
- 最终`out/shp-center-coverage-v163-refined/`完整叠图/两分合流近景/四外端近景已看，仍有波动及约0.35m端点偏差。204输入hash、原件重建/初值重求/保存终态复算返回VERIFIED_SOURCE_CENTER_RESEARCH_NOT_MAP；137相关回归/37.62s、compileall/diff通过，不是地图PASS。报告SHA `b7d264aaa3ebf6eb2c10689d5ce69c8d430a40fd9bbe48924ecf86851d37f080`。
- 无新XODR/esmini，旧v1.45 SHA不变，未commit、当前无后台作业、目标active。v1.62设计工况冲突仍待答复，源限速不改；MAP宽度/两movement/可写出共享整路与全连接/旧负宽/反向误差/CRS/Web未完成。后续编译必须保留本轮同一预算，不能重回裁端点。详见[证据、数学依据、限制与复跑](docs/复核补充-完整中心观测与斜切端部预算-2026-09-11.md)。

### v1.62 当前断点：普通原始路径本身与当前恒速验收合同冲突

- 去掉分叉C2及中心观测的诊断对照仍冲突；不能继续认为只移动分叉事件即可修复。新`source_path_graph.py`按原TOPO恢复7链/25源车道，5条物理中心链做独立必要检查，含已批准movement角色的2链仍需另查，不删源不扩大批准。
- 新`source_path_jerk_bound.py`不使用边界中点、分叉等式或曲线基底，允许端点在0.35m内移动，用欧氏管域的必要外包、精确曲率关系及Taylor余项求jerk下界。2m/1m仅检查站，不生成曲线或XODR。当前恒速60kmh、ay≤2.5、jerk≤1下，链2/5/6的1m检查下界为4.286743627/1.638831761/1.295433956；其他两链只未被排除，非PASS。是单调平面来源路径合同的数值冲突，不证明一般地图无解。
- 链6有原始站距0.000187928m导致极小系数，初版Optimal但对偶残差0.38，已拒绝。正数等价行缩放后保留全部原点、在原始方程验算；初版2.03/2.08下界弃用。10证书最坏原/对偶/间隙6.30e-15/4.55e-13/1.01e-13；最终`out/shp-source-contract-v162-certified/`原件重放为VERIFIED_SOURCE_CONTRACT_NOT_MAP，199输入hash一致，0 unavailable。
- 源图五条链已逐条看，明确纵横非等比例，没有拟合地图。128相关回归/6.90s通过，非整图验收；旧v1.45 SHA未变，未commit，目标active，当前无后台作业。报告SHA `e55898ad57bc894cd30926566bf4e8a9c0c76a1101fc7038acab89c00c140dab`。
- 下一步需确认这些普通分合流的设计工况：原限速60必须保留，但专家原文将2.5/1.0列为业务可调工程初值。不得据曲线倒算低速、放宽源容差或将普通链自动改movement；若坚持当前合同需可信源修正或另行批准解释。MAP宽度待答复不重复问，旧负宽/源端域/全部连接/CRS/整图/Web仍待办。[完整推导、原ID、证据、论文与复跑](docs/复核补充-原始车道路径与设计工况冲突-2026-09-11.md)。不要以同模型无限加迭代代替合同确认。

### v1.61 当前断点：补齐源接触带后，旧“自由必要条件可行”不再成立

- `source_transition_domains.py`按原TOPO/物理边界及原始顶点索引补18普通接触带，按车道累计1.584826813m、最长0.389847304m；不增加几何变量或段、不猜laneLink。Line/Arc共享宽度、优化器及2cm动态复算均接入；2生长事件不转普通通行边，14中心端部缺口仍待办。
- 旧v159自由站值在原18929行最大违例8e-10，在新882行有210超限；新自由必要模型3411变量/19811行数值冲突0.000395714253。均匀/事件长五次仍冲突0.003673931253/0.002853175959。是固定图/接触/来源/中线/速度条件诊断，**不能继续拿旧自由可行当只换基底的理由，也不证明一般地图无解**。
- 重算历史156系数实际曲线：来源0.35、宽度数值零、C2通过；原60kmh正常区间ay4.226154587、jerk6.666361917仍FAIL。9.492332548原始查询落在7e-15m近重合结点带，另记数值诊断并图中标紫点，不作为可靠道路区间值；早期图直接取记录端点另一分支导数的显示错误已修复。
- 最终`out/shp-transition-coverage-v161-final/`存报告/旧参数读回图/过渡图/200输入hash；实际图已看，仍有波动。123相关测试/7.01s、compileall/diff通过，不是地图PASS；无新XODR/esmini，旧v145 SHA不变。`v161`和`v161-replay`是修正显示/数值诊断前快照，不当最终证据。
- 下一步先核条件冲突支撑和固定站位/相对展宽/物理中线与通行轨迹模型，再做来源支持内合法事件与可写出全联合；不可退回短段、漏检、删源或降速。MAP宽度待答复、绝对CRS/Web/整图等门禁不变。[详细证据、代码、复跑与限制](docs/复核补充-源接触过渡带覆盖与必要条件修正-2026-09-11.md)。未commit，goal保持active。
- 最终重放已完成：VERIFIED_COVERAGE_RECHECK_NOT_MAP，200输入hash一致；报告SHA `e7a6f359a3c3f6e0d0c1392d8b2dc530e0c0770ad62414db8816098e35aeab16`。当前没有后台作业，最终图片已核查；只有研究检查完成，不是目标完成。

### v1.60 当前断点：可写出表示内核已接入，单Arc仍不能通过完整来源

- 新`arc_source_chart.py`解析Line/Arc双向投影和原线段法向交点，二阶导包络计入同一0.35m预算；不能把弯曲坐标中的原直线当成投影端点连线。所有原件/角色/速度不变，倒序坐标图保留真实原顶点序号。
- `ArcSourceBoundaryBlock`以一个参考原语、全局共享长分界、三次边界系数表达原25车道/33边界/11族；幂基精确换算可用，但完整源端域/道路写出及全部连接未完成。新增世界曲率/导数与Jacobian支持；Line必要导数检查禁止误用于Arc。
- 连续航向/曲率＋全部共享系数的有界变量投影24求值、22唯一状态：21来源拒绝、1数值ERROR，不把后者当无解。66/74系数、最短15.10879m；已访问最低完整模型额外松弛0.77261046m，仍拒绝，非获得0.77m误差的新地图。没有真实动态重跑或新XODR/esmini。
- 仅78原顶点+C2必要检查通过，但完整原段/物理中心/宽度采样必要检查额外松弛0.76840503m；不是只由Bernstein保守性引起。当前`out/shp-arc-source-v160-reproducible/`存源模型/所有参数/拒绝叠图/194输入hash。全源及两近景已看，红色诊断明显有波动，不是修复成果。88相关回归通过，不当地图PASS；旧v1.45 SHA不变。
- 下一步必须处理相对展宽、合法事件及完整端域/动态覆盖，再联立少原语轴/世界G2共享断面/全部连接；不继续拿单Arc调参当完整全局方案。MAP名义宽问题仍待答复，门禁不改；SHP旧104负宽/60kmh、CRS/Web仍待办。早期v160不完整包与代码快照不可当最终证据。详见[实现、真实拒绝原因、标准、复跑](docs/复核补充-弯曲参考轴与可写出三次边界-2026-09-11.md)。
- 收尾：最终`reproducible`重放为VERIFIED_RESEARCH_NOT_MAP，194输入hash无差异；报告SHA5086fdbc6ffa261e448ac104223de8d66f4e4401f06573f95c75dcf3ea66c0b7。88测试/7.67s、compileall/diff通过，最终图片重看；所有本轮作业已结束，未commit，goal保持active。

### v1.59 当前断点：源事件对齐有改善，固定长五次模型仍受限

- 新`source_jet_relaxation.py`把33原边界接触统一成必要条件模型：精确曲率恒等式+来源割线界+Taylor余项，无小角度假设，3411检查站导数变量不写为曲线。自由松弛未排除可行性；限制回171变量均匀基底后数值冲突0.0036633275。是条件性数值诊断，不证明源数据或全路网无解，也不是获得实际曲线。
- `source_event_basis.py`将两个物理分合流事件41.25377707/59.38377666m纳入长基底布局，25车道/33边界/11族保留，171→156变量、46→41区间、最短18.1299996m；不是原点拼接/新增微段。全原边界0.35m、非负宽和G2保持，2cm原60km/h ay7.218821→4.226155、jerk16.285130→6.504621，仍FAIL。新基底必要条件也冲突0.0028531753，继续同模型加迭代不是下一步。
- 最终`out/shp-source-jets-v159-release-review/`已重新构建源模型和读回参数，状态DIAGNOSTIC_VERIFIED_NOT_MAP；191项原件/配置/代码hash一致。旧v1.52参数也重算，旧SHP XODR不覆盖。新全源/两近景图已看，仍有直段波动；五次中间边界没有可写出编译，不产XODR/esmini，不当完整来源/整图PASS。
- 本轮修复必要上界漏乘(1+M²)²，45°圆弧负例已覆盖。早期`v159-initial/basis-compare/event-fit/final`诊断无效；`verified`为最终防伪补测前版本，仅`release-review`对应当前全源码hash。70相关回归/4.14s、compileall/diff通过；非全仓/全图。无仍运行作业，未commit，目标active。
- 下一步是可写出解析参考轴/共享边界/源支持内合法事件及全部连接联立，含长直保护与同一0.35m总误差预算，不能把用满容差的五次再随意近似成短width。14中心端域、两通行观测路径、旧104负宽、完整普通路和连接还未闭合。MAP宽度问题尚无答复，门禁不改，不重复询问已批准的node4两处角色；CRS/node18/Web仍待办。[完整证据、公式、文献与复跑](docs/复核补充-SHP必要条件与源事件长基底-2026-09-11.md)。

### v1.58 当前断点：联合入口已打通，宽度解释不能替来源作主

- `--joint-initialization`要求coupled+restore+rotating，普通路标为JOINT_SEED_ONLY；长基底投影后只保证结构等式/非负宽，全部连接从同一口部初始化。三回旋线/长普通基底不变；搜索来源残差可暂非零，最终完整门槛不变，失败不写混合图。负宽种子投影已修复，默认研究几何未推广。
- 当前`out/map-joint-v158-seeded-{NODE5,node16,node13}/`三案已结束，均无最终XODR。NODE5最大正残差1.0552653274（北lane1），node16为1.2634887936（东lane2及西/北），node13最后接受状态3.3167788976（南lane3/2）后线性子问题失败。不是统一米误差或全局无解证书；initial目录不可交付。
- 新`map_observation_contract.py`只读全部近距离候选/原TOPO/关联边界法向截线；缺交点或多义不补宽不绑定ID。NODE5北9原点/7点对、node13南19/16均有至少一路局部TOPO候选，非唯一对应。原件图已看，横向夸张有标注，非XODR截图。
- node13南lane3点1的350cm字段附近唯一候选中心仅差0.000083m，原SHP起止宽5.65→6.7m，局部截宽6.3515m。NODE5北lane2点0距0→2.35m渐变候选中心1.19mm，但该点截线缺失，不能宣称实测0/2.35m。消息层送审稿只给宽度定义/1cm，没有全程常宽站位解释。**修正旧node13“宽度差即真实失真”的无条件说法，仍不恢复整图合格。**
- 已异步询问是否允许MAP把laneWidth作名义值并显式推断变宽；尚无答复，不把预选项当授权、不改默认常宽门禁。当前t宽保留合同并非物理净宽真值；不要自动借SHP身份、删点、压窄或降速。SHP原60kmh、五次编译/负宽、node18异常、事件站位/CRS及Web仍未完成。
- [完整证据与复跑](docs/复核补充-联合初始化与MAP宽度语义证据-2026-09-11.md)。最终三个包各154、两个对照包各166项hash一致、没有遗漏当前Python实现；合并122项/23.25s回归PASS，compileall/diff通过，不等于地图PASS。旧SHP v1.45及旧node13 SHA未变，无仍运行作业，未commit，目标active。后续改核心代码后不得拿本轮旧hash冒充当前实现。

### v1.57 当前断点：连续轴已实现，但显式宽度揭示了来源建模缺口

- `rotating_source_tube.py`重投影全部原点/原段，每个Bernstein见证独立保留且用解析联合导数。修复先取最大值再分别求导造成的并列见证不一致；NODE5四轴+275边界系数+55连接参数共334变量，仍长基底和三回旋线。只支持无附属对象的单Line轴，事件站位未联立。
- `out/map-joint-v157-bundle-NODE5/NODE5.xodr`中心/动态/66接口通过，但截图及原laneWidth审计发现北侧约1.5856m、源为3.5m，故不合格。旧v1.55/v1.56 node13同SHA也有最大2.694m宽度差；**不能继续称完整保真通过**。旁加REVIEW-OVERRIDE，保留旧地图和原报告。
- 新`map_width.py`/默认`MAP-explicit-width`必需门禁、原字段读取、解析宽度极值、长基底精确宽度和旋转支持宽度约束已接入。常宽是保守转换合同，不是原标准的全域测绘等宽保证；缺值不当3.5事实，尚不支持的坐标图返回UNAVAILABLE阻断。
- 最新`out/map-joint-v157-final-{NODE5,node16,node13}/`三案均在普通路初始化拒绝，无最终XODR。固定轴先通过才进入连续轴联合的入口限制仍在。诊断`out/map-width-model-v157-final-*/report.json`显示仅原点约束时NODE5/node16线性可行、node13南侧仍失败；不得把删去整段证书的反事实写成成果。
- 下一步先建立原点/点间推断/标量宽度的明确合同，再打通联合初始化和合法事件站位；保持全源身份和差异报告，不自动删除点、改宽度、降速或增碎段。node13南侧精确近站位原点冲突线索已落档。SHP五次模型未编译、旧负宽/60kmh、node18异常原点及Web仍未解决。
- [本轮全部证据、文献、命令](docs/复核补充-连续参考轴与MAP显式宽度门禁-2026-09-11.md)。历史`bundle-node13`因发现宽度漏洞主动中止，非无解；旧导数试验不能当当前代码复现。未commit、目标active。
- 最终161项相关回归/469.28s通过（含真实SHP/MAP），不等于地图验收；compileall/diff通过。三个最终重建包各153项hash、三个诊断包各152项hash与当前实现/原件零差异，无遗漏当前实现文件。旧SHP SHA未变，无仍运行的本轮作业。注意width当前是参考轴t向宽度，下一步需区分物理净宽度量，不能把常宽合同当坐标不变标准真值。

### v1.56 当前断点：整路恢复已运行，参考轴仍需成为联合变量

- `restore_joint`实现全共享状态LP最小超限+平顺QP+非线性复算；来源/宽度/结构/长度和形状硬约束不放松。先消去精确等式修复HiGHS Unknown，再加按变量尺度的近端正则改善病态；QP即使Solved也必须过原始残差检查，失败不采用LP顶点、不返回残缺几何。
- `--source-mouth-axis`由原Lane.points末30m支持估计单Line方向，全部原顶点/远端/身份保留，真实出口优先、缺出口同进口左右镜像不变。它是固定来源方向的研究选项，**不是轴/事件已联立优化**。`--restore-joint`必须配合`--coupled`。
- 当前`out/map-joint-v156-source-axis-NODE5/`、`out/map-joint-v156-source-axis-node16/`最大归一化残差降至0.000833035/0.107975341，20步仍拒绝，无最终XODR。前者主要107/110动态及101/104/109/110端点，后者主要103/105/112。不是米/百分比，不能据此宣布几何PASS或全局无解。
- `out/map-joint-v156-control-node13/node13.xodr`复现v1.55 SHA `33f976733666074b8a0ca2a965e2538af3bede7f8e46b735a822885cb1e7ba67`，2cm ay2.49680731/jerk0.999，来源/速度/XSD/内部边缘/G8/G11及90个esmini接口PASS；连接仍15km/h缺省研究工况、绝对CRS未核，BLOCKED且不推广默认。叠图/三视角已重新看，不把辅助铺面当测绘路缘。
- 三个当前包各149来源/配置/代码hash零差异，126相关测试/29.97s PASS，compileall/diff通过；旧SHP v1.45 SHA未变。早期restore/nullspace/proximal试验对应旧代码，不拿旧hash充当前复现。没有新SHP成果，60km/h/旧负宽/Web未解决；未commit、无仍运行作业、目标active。
- [本轮完整证据/论文/命令](docs/复核补充-联合可行性恢复与来源参考轴-2026-09-11.md)。下一步参考轴/合法事件站位进入依赖闭包，每次改轴重建全部源约束与全部连接，不再只换固定方向初值。node4西侧折返及SHP跨链对照已在v1.40/v1.41记录，不重复当新发现、不自动删点换身份。

### v1.55 当前断点：node13 整路/全部连接联合通过，尚不能推广全部地图

- 新 `spikes/joint_corridor_connectors.py`、`connector_shape.py`；`scripts/rebuild_map_joint.py --coupled`把所有普通道路边界系数与所有连接长度/内部曲率同时求解。node13为310变量/4普通道路/15连接。普通路和全部关联连接统一重算，不独立挪端点；仍固定Line坐标轴、laneSection事件站位及长基底，尚非一般全局模型。
- 最终 `out/map-joint-v155-verified/node13/node13.xodr` SHA `33f976733666074b8a0ca2a965e2538af3bede7f8e46b735a822885cb1e7ba67`；源/速度/XSD/G8/G11/内部边缘PASS，esmini90/90接口PASS；2cm ay2.49680731/jerk0.999，最短原语5.00000549m，原始顶点max0.349999738m。普通速度照源，连接缺省15研究工况，不能说全部60通过。最大共享口部横移0.226660m，整路贴源预算未变。
- 修正近直行跨lane连接被5°限制误杀：允许前进锥内必要的一次S形，普通30–150°转弯仍禁止反向曲率；检查二次航向内部极值。掉头保留既有±π分支及精确模角，不允许绕整圈。冻结曲线完整ID集、内部G2/父端点检查和原子写出都有负测。
- 七案各149原件/配置/代码hash独立复核一致。NODE5/node16/node3第一共享线性化子问题PrimalInfeasible；不证明全局甚至该非线性模型无解。node4 road10、node17 road10/12/13先被整路模型拒绝。node18原点异常维持原件，既有可信修正问题尚未收到答复。
- source-overlay/esmini三视角及完整路口近景已看；没有看到此前的大断口/反摆，但外轮廓有推断辅助铺面，不代表实测路缘。CRS绝对依据仍缺，所有交付BLOCKED，默认转换器/SHP原件/旧v1.45未改。失败包initial种子不可当最终成果。
- [完整方法、限制、复跑和下一步](docs/复核补充-MAP共享整路与全部连接同步求解-2026-09-11.md)。下一块把合法事件站位与必要参考轴自由度接入相同共享状态，先定位node4/17完整源约束冲突；SHP继续真实共享物理边界及全部连接，Web仍未实现。不要退回自动降速、单连接调参或细小拼接。
- 138相关回归PASS/467.93s；最终补近π测试后96定点PASS/19.16s及29联合测试PASS/1.92s（重叠不相加）；compileall/diff通过。batch-summary SHA `a36d5702dc08b1060557fe1759dec276cd183771a02d32bc32800ef6b09b680f`。未commit，目标active，无仍运行作业。

### v1.54 当前断点：源覆盖/边缘闭合已改进，固定口部转弯仍失败

- 新 `scripts/rebuild_map_joint.py`原件起步，单 Line 坐标图整路长三次边界，绝对 0.35 m 原始中心线包络；不继承 legacy-envelope。源稀疏段两端跨 laneSection 时仍完整约束，2 m 只分检查区间、不增加结点。全部普通路解完后联动重建所有连接，任一几何失败不落最终混合图。
- 修复单 Line 远端覆盖全部已纳入 Lane.points（node16 原点原先在起点外 0.714182 m）；同时修复叠图不含 laneSection 首末点导致的显示小缝。原点、路口端、源速度/ID、原件均未改。整段证书仅覆盖共同支持，不冒充全域证书；原件 G8 与所有原始顶点 0.35 m 检查另外执行。
- `three_clothoid_minimax.py`固定三段/端点 G2，最终最短5 m、10%相对长度、普通转弯曲率不反向；仅研究管线调用。初版node13虽数值PASS但用3 m端部反摆，已推翻，不能交付旧 `map-joint-v154-source-ends`。最终 `out/map-joint-v154-fair/`中 NODE5/node13/node16 有候选，其余四案拒绝；表、SHA、复跑、全部限制见[复核记录](docs/复核补充-MAP整路源约束与不反摆连接-2026-09-11.md)。
- 三份最终：XSD/G8/来源/速度/内部边缘PASS、esmini中心及两边缘234/234 PASS，原始顶点最大约0.35 m；G11-D仍FAIL。node13 road109 ay2.559752/jerk1.023998；node16 103/105/109/112最大3.857019/2.205325；NODE5 107/110最大2.458957/1.237383。不是新合格地图，默认转换器未推广。
- `turn_space_bound.py`固定口部、零端曲率、单调转向的必要位移界：node16 103/105缺约1.178/3.666 m，说明此限定模型不能靠多加小段解决；不证明原地图全局无解。下步把动态/形状残差反馈到共享口部与源支持内事件站位，联动全部连接，不能继续只换单条连接初值。
- node3连接110未达本轮5 m；node4普通10、node17普通10/12/13仍模型拒绝。node18北Lane1第二点离邻点弦约1719.94 m，已通过非阻塞问题索要可信修正，未收到；保持原点，其他路口工作不等待它。SHP v1.52、Web待办未完成。
- 最后7案各147输入/实现哈希及旧v1.45 SHA复核一致；三份源叠图、9视角和NODE5补全全图/北腿图已查看。收紧前125项回归PASS/454.90 s；最终96项定点PASS/4.26 s，compileall/diff通过。未commit、无仍运行作业、目标active。

### v1.53 当前断点：自动降速漏洞已移除，真实接口/动态仍失败

- 直接重读用户专家原文：路口movement设计速度与进口/来源限速不能混用。但此前默认转换仍在MAP镜像、连接路、普通路，以及SHP连接和断面后处理中按曲率自动降低写出speed，旧测试甚至要求cap发生。现已移除**主线调用**；历史cap helpers保留给孤立研究脚本，不得重新接入默认转换。
- `dynamics_speed.py`区分限速事实和设计工况；单位m/s、km/h、mph，sOffset区间，零值不是缺失。`movement_design`必须完整绑定所有connecting驾驶车道和artifact revision；不允许覆盖ordinary，不完整/陈旧/重复引用拒绝。proposed提案仍FAIL且保留原限速压力指标，不能据曲率倒算工况。
- `scripts/recheck_speed_contract.py`只写新目录。已从原件生成`out/source-speed-v153-{shp,map}-node4/`：限速核对SHP309/309、MAP457/457（有源身份301），分别275/59输入和代码hash复核一致。两个XODR均XSD/G8 PASS；SHP G11-C/D FAIL、MAP G11-D FAIL；esmini中心/左右边缘分别31/144、84/96 PASS，不能当整图通过。
- 源限速工况2cm复核：SHP最大ay55.5523/jerk1542.1030，MAP ay11.5952/jerk61.0259；SHP jerk最坏在普通road13，MAP在普通road10。给全部转向显式15km/h待审提案也不能消除普通道路失败，不需要用户再选择一个更低主路速度来掩盖几何。
- 已看两份源叠图+各三视角esmini。SHP外边缘S→T P95 0.58545m、max1.82045m，仅配对覆盖口径；口部小突起仍可见。独立接口gap最大SHP0.298636m/MAP0.067050m。连接路最大4/3原语、最短6/8.1626m，未新增碎段，但依然不合格。
- 本轮两份是**默认转换诊断产物**，不是v1.52五次共享边界的编译输出、不是几何优化成果；`geometry_repaired=false`。旧v1.45 SHA abf59437b504c8b3fd6faa8cb439632906b7a5b7538a00b48bf6ad477709b9c9未变。继续J1+J2整路/全部连接联合；不要退回自动降速或单独换铺面掩盖接口。详见[复核记录](docs/复核补充-来源限速与设计工况分离-2026-09-11.md)。
- 最终73项定点/1.65s及12项真实SHP/MAP回归/169.14s通过；compileall和diff检查通过。真实SHP测试要求限速保持并诚实预期当前G11-D FAIL，非地图通过。无仍运行作业，未commit，目标保持active。

### v1.52 当前断点：边界几何子块可行，动态和XODR表示仍未闭合

- `physical_continuations.py`按原TOPO/SIDE证据把40个接触见证去重为26对物理关系，不靠几何最近选延续。北侧两叉分别是普通延续/渐变接触=1/1、2/0；两者继续G2，后者不能因有新生lane就豁免已有边界。源通行关系与XODR零宽laneLink分开。
- `source_contact_fit.py`增加显式degree=5研究档，原46长区间不变，内部结点重数3保持C2而非额外强制C4；33源边界/11长族/171系数、最小跨度16.1206m。整个源折线与候选间的多项式差求极值；Bernstein整段源界/正宽，第三导平方用Gauss精确积分。长三次起始零宽/零一二阶宽度的表达限制已由更高阶模型绕开，**不是缩短小段，也不是获得可直接写出的五次width**。
- `shared_boundary_dynamics.py`：全部25源lane共同支持内的物理中点，原速度60，796个1m/接头单侧样本；在硬source/width/G2内对实际ay/jerk做序贯凸minimax。纯LP会使非最差直段漂移，已加入第二层平顺QP；OSQP在该层超时不接受，改用已有Clarabel并检查原始单位残差。没有改变门槛/源速度。
- 最终`out/source-boundary-network-v152-final/`：整段边界max0.35m，宽度最低仅浮点舍入量，G2约束残差约1e-13；1m动态比从33.7372降至16.1932，**2cm复算ay7.2188208/jerk16.2851296仍失败**。全源/两局部叠图已看，直段仍有不必要弯曲，未当形状PASS。五次非PH曲线，不可直接转width或强行切小三次。
- 14条普通源中心端部共同支持不足、两条原通行路径另验、域外端策略、参考轴与全部12连接联合、写出再读回、MAP/Web仍待完成。不要用0.35m边界子块通过替代这些项目。下步需把长直保护/事件站位与可写出line/arc/spiral参考轴模型接入同一依赖闭包，总误差预算仍0.35m，不能先用满0.35后再无预算近似编译。
- 最终report SHA `2956a9558298961934235478a500b7614675b92e174d71d34938a6cfc9efb383`，图SHA `17c2ee384f25904d0ff566c3cd9d4aca8d35a97e11ba826fd61f22e34e81cfd4`。原v1.45 SHA `abf59437b504c8b3fd6faa8cb439632906b7a5b7538a00b48bf6ad477709b9c9`未变。当前研究代码已更新，旧v1.51和本轮01–05只作对应旧代码的历史证据，不宣称当前代码可原样验证旧hash。
- 219项相关回归/170.57s PASS，后加二层QP失败不接受LP顶点的测试后9项定点/4.96s PASS；非全仓/整图。复跑/保存参数重算和限制见实施计划。没有新XODR/esmini；未commit，目标未完成。

### v1.51 当前断点：用户已确认两处角色，固定轴长三次模型仍拒绝

- 用户对上轮“仅两处以边界为物理道路依据，保留车道线作通行观测”回复继续，已落实。`profiles/repair/node4-zero-width-source-roles-v1.yaml`精确绑定v1.50 contact正文SHA `88917afb1d17875109a60933ccba39d5dd6eb007d5b34fffc90b2a3539662840`，两个ID/起点限定，无全局开关。`source_roles.py`原点全留、其他角色不变、记录TRANSFORMED解释；原车道线的通行路径检查仍待做。无需重复向用户询问这两处同一角色原则。
- 新`spikes/source_contact_fit.py`把原节点二度接触合成长边界，分叉共用约束，北侧25源lane、33源boundary→11长族/79变量，最短独立跨度16.120613m；不是每个源要素/点一条曲线。完整边界逐源线段做Bernstein误差界，width整段非负。2m检查区间不新增结点/变量。默认转换器未接入。
- 固定源接触站位、固定Line坐标轴、长三次边界、所有分叉C2的本轮模型仍拒绝。充分包络需0.8269215m附加松弛；仅源边界1653采样、不含宽度/中心/动力学的必要条件也需0.8252669m。是**当前基底/配对/接触假设**的数值不可行，不是原地图全局无解。14条非改角色源中心的端部共同支持不足被明确记录，不静默外推或删除。
- 最终`out/source-boundary-block-v151-final/`：report SHA `b44ce40af64268f002d5ff75d5c0207c4eacc37f679a135e5923a65b6b49c6c0`，source-roles文件SHA `59f8690ad239e212c717e3ec11a0b4604b16abcdb057c4e2d7de799cc1d173db`；原件/决策/模型重建及拒绝参数重放一致，退出2。依赖的v1.50最终源包已只读复制到`out/source-role-input-v151/`，本轮成果都可随项目搬走。中间01/02/03不是当前代码重放证据。
- 红色最小松弛诊断图已查看全源和两局部；明显偏离/波动，明确REJECTED。它是允许误差的LP诊断见证，不是平滑目标优化后的候选，不据其波浪证明所有解都波浪。没有新XODR/esmini，基线v1.45 SHA未变，旧104负宽与60km/h失败未修。
- 规范/文献复核已入实施计划：ASAM区分旁侧新生车道与普通前后继；不能据此直接放松本项目G2或1.5消费约束。下一步需结合原边界角色明确continuation/contact/新生separator，不默认所有三叉边缘同导数；再选自由事件站位/内在曲率原语/参考轴与全部口部联动的候选模型。**不能只是继续移动均匀结点或缩短宽度小段**。
- 验证201项相关回归/140.98s PASS，含18新测试；最后增加必要条件PASS不等于接受的断言后6项块测试再过。非全仓/整图，MAP/Web没有新增实现。未commit、目标未完成，无仍运行验证任务。

### v1.50 当前断点：源接触已建，两处零宽点角色不可混用

- `source_contacts.py`和`prepare_source_contacts.py`：从原TOPO、原lane/boundary端点ID、原坐标共同建立源接触。Profile新增lane的`S_LANE_PID/E_LANE_PID`和boundary的`START_PID/END_PID`映射；不改原SHP。不得拿新的Profile去声称旧v1.49包的配置hash仍有效，新包已重建。
- node4北侧20事件（18延续、2零宽出生），40对物理边界接触；118原要素/3009顶点完整保留。编译器检查原件全集、part/顶点/所有者/弧长、scope与partition问题；不能少了一条固定源线仍称完整支持。端点反向数字化、零宽跨侧接触、异ID/缺映射、篡改后自签都有负测。
- 旧54个残余区间：39个/14.667004665m为包内源接触，15个/1.117668244m为包外原件接触。**未重画或填满任何道路**；原区间分配仍UNRESOLVED，完整观测支持不是写出几何覆盖。包外邻接仅只读证据，非整地图边界，也非扩域授权。
- 原`2023081117223317273`及`2023081117251327605`起宽0，路径首点距左右边界汇合点1.748232m/2.176391m。若硬绑为同一物理端点，必要公共来源半径至少0.874116m/1.088195m，超过0.35m。此为**条件冲突，不是全局无解**。原点全保留，角色未自动改；需明确是否以边界为物理道路依据、路径点另作为通行观测。不得擅自降权/删点来通过。
- 最新结果目录：`C:\Users\geng\.codex\visualizations\2026\08\21\01a02206-86a5-7041-afef-1cdb390ada9a\mapforge-source-contacts-v150\final`。`contact-model.json`文件SHA `ef7cec66ebb84ca8376cf8dbce59bed1d0ebaa6d851716cd007449fa30859a8c`，原件重放一致/退出2。`source-role-review.png`全源与两处放大图已亲自查看。基线仍v1.45 SHA abf59437b504c8b3fd6faa8cb439632906b7a5b7538a00b48bf6ad477709b9c9，无新XODR/esmini。
- 曾遇F盘满；仅本轮两份中间证据目录（10文件）经SHA校验移到上述C盘父目录，未删原件或历史成果；这两份中间包不是最终重放证据。复查F盘已恢复约71GB，本轮最终证据仍留C。详细复跑与最终测试见实施计划。
- **下一步**：先落实两处源角色和准备域外端策略，再以共享端点/完整支持接J1整路及12依赖连接联合求解、J2独立读回；不是继续逐条104试初值。当前C0接触不代表G2/平滑/少原语/60kmh合格；MAP/Web、旧负宽和整图失败不变。未commit、目标未完成。

### v1.49 当前断点：独立源域核对已落地，旧事件切口仍不能承担完整来源

- 最新`out/joint-source-domain-v149-final/`：`source-domain.json`、主准备包v2、`run.json`，另有仅源区间诊断的`interval-review.png`。基线仍v1.45 SHA **abf59437b504c8b3fd6faa8cb439632906b7a5b7538a00b48bf6ad477709b9c9**，没有新XODR/几何优化/esmini。
- 显式绑定原路口PID `2023041113170861627` 到旧junction `1`，不按几何最近猜ID。原件进口16/出口8、48 TOPO边、24完整路径；旧24逐路径MATCH。路口内道路按Profile `interior_values: [1]`，不要求整条via在路口面内。真实删104及junction行后旧结构仍自洽，新检查报24期望/23实有；多内链合法分支也有完整路径回归。
- `source_domain.py`：原始层行号/字段保留；完整路径枚举后再核旧图。当前精确Line法平面区间核对不支持曲线父轴（拒绝近似）、不跨multipart，局部eqc管线必须匹配。包含固定对端同ID在其他section的只读上下文，不扩大可变范围。
- 47源车道+71边界ID=118个要素/part、3009原顶点；15.7846729m累计源线仍未分配（中心6.52171594、边界9.26295697），48个要素有残余，最大单段1.2655565m。不是道路长度缺失、横距或铺面空洞指标。没有冲突所有者，但**归属记账不证明同车道保真或边界拓触通过**。诊断图已查看，北侧要素切换附近成排尾段可见。
- 原型审查修复：主准备包v2强制源域绑定，删除run标记也不能跳过；重放必须`fresh_reader()`重开原件，不信任旧Profile缓存；完整枚举解决多内链误拒绝。J0-1不带源域的legacy调用仍可作准备，不能声称J0-2或联合求解入场完成。
- **下一步**：针对这些原始要素尾段建立源支持内的事件站位与物理边界接触对应，保持每点参与约束；不能把旧断面裁切当源真值，也不能把未分配段随意并到相邻车道。完成J0-2剩余源角色/共享接触后，接整路+所有连接J1/J2。MAP原点异常、源速度60工况、旧104負宽问题和Web未完成项不变。
- 本轮不改SHP/XML/XSD、源速度、ID/phase、旧XODR、正式输出；仅代码、Profile分类配置、文档和新准备证据变化。未commit，目标未完成。测试结果见实施计划最终记录。

### v1.48 当前断点：J0-1准备包与提交边界已实现，先补J0-2后接联合求解

- [本轮实施计划/范围/命令](docs/实施计划-整路联动重建与编辑闭环.md)。新`reconstruction_scope.py`与`prepare_joint_reconstruction.py`；Profile新原始身份API保留Multipart/原行/关系SIDE。IBD配置显式`lane_identity: primary-with-supplement-records`，普通层优先但11个聚合层同ID原件仍完整另存，不自动拼接或删除。
- 最新`out/joint-reconstruction-input-v148-final/`：14可变端口、10固定端口、12连接、47源车道、71边界ID/23共享ID、40源链接校验；6连接的中间支持`2023081515293628015/2023081515293622479`补入完整来源。38输入文件/配置hash；47源限速均60，12旧连接写出速度有差异，不改速、不等于动力学通过。基线仍v1.45 SHA abf59437b504c8b3fd6faa8cb439632906b7a5b7538a00b48bf6ad477709b9c9。
- 源入场`ADMITTED_FOR_RESEARCH`；保存包重新读取原件结果一致；全作业`BLOCKED`/退出2，无XODR。验证**实际保存正文**，不是仅验证重新构建的临时包。未申报端口、固定远端、速度/来源/内部laneLink、junction表不一致均有拒绝测试。结构事务仍非完整独立ValidationBundle。
- **接下来明确做J0-2**：从原路口源TOPO独立检测旧图漏连、完整源区间分配、分合流角色/边界接触绑定。当前只是已存在连接的依赖闭包，不能拿12条当原源所有合法连接数。再拼接J1共享边界/参考轴/全部连接实际联合求解。不要回到逐条104调参。
- 本轮未改原件/旧XODR/限速/phase/ID；Profile配置有显式新增，旧报告的配置hash不可再当当前配置。无新esmini或截图，MAP与Web未实现，未commit，目标未完成。
- 最终135项相关回归通过/103.68s（含33项准备与事务测试及Profile真实数据测试），非全仓/整图。compileall、diff检查通过，38项当前输入hash一致。两名只读审查者发现的范围/提交漏洞与保存包校验漏洞已落测试修复；无仍运行的子任务或测试进程。

### v1.47 最新断点：Line优先和直线保护可表达形状，分合流在原60km/h仍拒绝

- 先读[完整原件、文献、模型、结果和复跑](docs/复核补充-完整源路径少原语与直线保护-2026-09-11.md)。新增`sparse_path_model.py`、`straight_transition_model.py`（均spikes）、`sparse_source_path_trial.py`和`review_sparse_paths.py`。基线仍v1.45 SHA abf59437b504c8b3fd6faa8cb439632906b7a5b7538a00b48bf6ad477709b9c9；**无新XODR，不把单路径当多车道地图**。
- `out/sparse-path-v147-reviewed-final/report.json`及`comparison.png`已读/看。right−3：1条97.1874m Line、max0.0344m。right−1/left+3：自由3段固定60时偏源0.6401/0.4518m；保护直线的5原语（最短12.10/13.54m）来源上界约0.3482m、G2通过，但60km/h jerk14.3205/8.8037，仍拒绝。其24.71/29.06km/h只是候选恒速指标，不改限速、非最优速度/安全承诺。
- 自由速度诊断3段虽指标较好，却把直段拟成波浪，已看图确认，不优先选它。原始LANE_LINK在零宽处分支仍不能当边界中点。下一步在物理边界/通行角色明确后，把直线保护的候选选择接入整路及全部连接共享约束；不是继续调每条104。
- 验证器增加点间1-Lipschitz来源折线上界、接头独立检查、低速诊断不能PASS。103项相关回归/8.75s PASS（含21新测试），不是全仓。21源文件/配置hash及完整源要素快照、原始TOPO/方向/11车道速度均核对不变；绝对CRS未获证。MAP、铺面及104旧负宽未解决；Web未实现；未commit，目标未完成，无仍运行的测试进程。

### v1.46 最新断点：依赖闭包与原子回退已实现，严格整路模型在两侧分合流处拒绝

- 先读[共享口部、失败约束和复跑](docs/复核补充-共享口部依赖与整路可行性预检查-2026-09-11.md)。新增`port_dependencies.py`、`shared_boundary_window.py`、`road_constraint_model.py`和`shared_port_preflight.py`。road10右侧边界3同时影响−3/−4及103/104/105；漏依赖、任一失败、旧revision、偷改接触关系均拒绝。**联合非线性求解尚未拼接，Web尚未实现。**
- 真实结果`out/shared-port-preflight-v146-final/report.json`与`preflight.png`已读/看；输入仍v1.45 SHA abf59437b504c8b3fd6faa8cb439632906b7a5b7538a00b48bf6ad477709b9c9。30m窗口切口s61.13755506旧边界偏源1.1034426204m，固定它必拒绝。整路76变量/70自由变量、释放口部、≥15m独立跨度、0.35m来源、分合流C2时线性不可行。
- C0/C1全部分叉反事实可行，但只降右侧仍被左侧并流拒绝；不将诊断降级写出。不是全局无解证明。下一步是两侧事件/参考轴/基函数模型选择后与全部依赖连接联合；不要重试104固定端或加小段。原路径与边界中线角色冲突仍不可混用。
- 82项相关回归/3.14s PASS，非全仓/整图。21原件/配置文件hash和23完整源车道快照入报告；绝对CRS未证明。未改源/限速/默认转换器，**本轮零XODR导出**，没有新esmini验收；未commit，无运行进程，目标未完成。

### v1.45 最新断点：全局路线落档，共享断面释放自由度，122局部通过

- 先读[全局算法/论文/Web约束式编辑设计](docs/全局重建与约束式交互修形方案.md)和[本轮真实数据、失败结果及复跑](docs/复核补充-共享断面自由度与全局路线-2026-09-10.md)。GUI方案升v0.5，纠正旧14图完成口径及“所有修形交商业工具”；**Web代码未实现**。用户要求停止逐条调参；下步真正联动普通路物理边界、SharedPort、全部依赖连接，不把本轮固定普通端状态的试验叫整图全局求解。
- 新`mapforge/ops/shared_section.py`：q=t′/(1-κt)跨断面共享，[t″]=-q*t*[κ′]，保持世界中心/两边缘G2而不强迫q=0；固定n+2区间、增加n−1共享方向变量，Bernstein系数约束整段正宽。零q等价/非零q全断面/多边界/充分非必要界/非法输入均有测试。默认转换器未接入。
- 固定参考A/B消除104负宽并贴近原via，但15km/h jerk29.898仍FAIL；联合参考后104仍来源/动力学失败，不宣称全局无解。122联合后独立PASS，最终仅替换122的`out/shp-shared-section-final-v145/node4.xodr` SHA **abf59437b504c8b3fd6faa8cb439632906b7a5b7538a00b48bf6ad477709b9c9**；via max0.245168m，精确最小宽3.343351m，2cm jerk0.982006；5参考段最短6m，7横断面记录最短3m。最终仍保留输入中的104负宽，不将诊断美化图混入接受文件。
- 144独立esmini中心/边缘接口及XSD1.5M PASS；全图G11 A/B/C/E PASS、D FAIL。源叠图及esmini全景/口部/斜视已看，西侧间隙、口部蓝缝及边缘台阶仍在；原件/速度/正式输出不变。esmini不支持部分geoReference参数，非绝对CRS证明；15km/h不是源60km/h语义通过。
- `spikes/shared_section_candidate.py`支持联合参考和按父SHA重放参数；不能覆盖已存在目标。数值构造余量-2.69e-9导致误REJECTED，允许1e-8构造舍入（仍预留1mm来源/2%动力学）；最终门槛没变，重新独立读回122通过，不手改旧报告。单图review标题裁切已修。69相关测试/9.61s PASS，非全仓非14图。无仍运行进程，未commit，未标记完成。

### 2026-09-10 最新断点：来源口部联动后17连接通过，144接口通过，整图仍FAIL

- 先读[本轮来源、图像、失败原因与复跑命令](docs/复核补充-来源口部联动与少段连接复核-2026-09-10.md)。最新冻结`out/shp-joint-review-v144/node4.xodr` SHA **ff0613ef8443ab36063363494c3ac13ab2bf91e1820c64ba202a42220cad327a**。独立复核17局部PASS/7FAIL；失败104/105/113/116/117/122/123。144 esmini中心/两侧接口和XSD1.5M全部通过，全图G11-D仍FAIL。
- 原始via与TOPO相邻支持分别约束，禁止长直线稀释转弯误差；普通road11/12末端各30m窗口来源max中心0.204/0.111m、边界0.350/0.168m，保持60km/h，2cm jerk0.979945/0.140206；不代表普通整路通过。父文件`out/shp-anchored-retreated-full-vertices-v144/node4.xodr` SHA c4fe1be07b8e4764fbdffcdbcb47fe4e7e9152872b2002b3185c638f16fe4acb；最终普通road与父文件子树一致。
- `joint_source_mouth.py`为有源支持的长窗口构造；`measured_connector_caps.py`省去不必要首尾段，110/111各3参考原语（最短6m），横断面固定5记录仍最短3m。另修数值闭合、0变量LP、余量选择错误、原始via独立约束和真实参数计数；`trial_xml.py`修试验根元素顺序并写后XSD检查，不能继承旧XSD PASS。重放`recheck_joint_shapes.py`支持显式`--refit-road`，默认不优化。
- **不可掩盖的失败**：104最小宽-2.811628m、边缘自交、源偏差5.524m；122源边缘3.055m、jerk2.347。`out/preview/shp-joint-review-v144/`24条叠图、全景/口部/斜视已逐张看，仍有西侧间隙、口部蓝缝、外缘台阶；不是只看统计PASS。新截图器`capture_geometry_review.py`无车/无地面，单次参数、不重复覆盖，输出SHA。
- 当前构造留1mm来源余量和0.98动力学目标，最终门槛未改；112曾被低误差旧种子替换新安全候选，修后2cm jerk1.000009→0.980011。15km/h附加检查不代表源60km/h连接通过。来源manifest站位/标线物体传播、铺面资格和绝对CRS仍未完成；所有试验BLOCKED，默认未提升，正式14文件未覆盖。
- 下一步先road10/13/30的物理边界族及固定接入冲突，联动104/122等。源锚点扫描后road13以总115m位置、109m窗口/≥15m独立跨度试验仍拒绝，详见`out/shp-source-anchor-long13-v144/node4.mouth.json`；不是全局无解证明。不要再盲移结点/缩短小段。MAP node4/17/18未完成项不变，本轮未重转。
- 最新80项相关回归PASS/2.86s、compileall通过，非全仓/非14图闭环。无仍运行进程，未commit，未标记目标完成。

### v1.43历史断点：实测连接共享断面原型已验证局部，仍有10条失败及整图缺口

- 先读[共享断面、结构口部与独立复核](docs/复核补充-实测连接路共享断面与结构口部-2026-09-10.md)。新增`spikes/measured_connector_caps.py`：完整raw via中心/两侧边界参与来源约束；5参考原语、固定7横断面区间，匹配普通路真实文件语义的中心/两边缘端状态，不用独立中心线+名义宽度。最短参考6m，width最短3m，不能说完全无横断面复杂度。
- 最新代码仅road108冻结`out/shp-measured-current-v143/node4.xodr` SHA **7949aba5dd77e8980abdc3b306263017c4041333a138259224a6edd2fe052b56**：原始中心max0.1932m/边缘max0.2876m，15km/h设计复算2cm jerk0.194459，内部边缘和独立esmini6接口PASS；其他连接保持输入，整图FAIL。`out/preview/shp-measured-current-v143/`已查看。
- 结构口部外移6m的全24条历史批次`out/shp-measured-mouth-fit-v143/node4.xodr` SHA **3668c39a1fc7771fd36eff9acf6ba5ae2df155e40e935df823534c8b128ade5c**，原始via不裁/源停止线不动；精确限制普通路，并把原TOPO唯一确定的相邻源链纳入连接。当前独立复核**14局部PASS/10FAIL**，不是优化器旧15CANDIDATE。PASS：101/102/103/106/107/108/109/113/114/115/118/119/120/121。144 esmini接口12FAIL（基线95），不是整图通过。
- **严重失败不能隐藏**：104最小宽-53.7168m、边缘自交，即使两端接续通过也不可用；122等仍有异常。`out/preview/shp-measured-mouth-v143/`24条对比与esmini三机位已亲自看，异常清晰，全部标REJECTED。非对称首尾段与新增一个全局横向自由度的能量最小化都未解104，结果在`out/shp-measured-cap-seeds-v143/`；`ribbon_fairness.py`未接入转换器。
- 独立复核脚本`review_measured_ribbon.py`保留所有源顶点、双向分别统计、精确宽度极值、重算2cm动力学、内部边缘单侧接头和采样条带自交，验证报告SHA。历史批次生成早于这些求解侧加固及悬空section laneLink清理；已用新复核器重审，**未用新生成器重转全24，不得称最终成品**。结构区间源清单/信号物体等传播未集成。源60km/h与旧文件较低的连接速度需区分，15km/h仅附加设计检查；未改源或文件限速。
- 53项相关回归PASS/2.83s，非全仓非14图。下一步应联合普通路完整口部来源/端状态与via，采用正宽、有界的少自由度模型；不要强迫旧拟合导数或继续盲加小段。SHP普通路分合流/铺面仍失败；MAP node4/17及node18原件问题不变。未commit，goal active，无仍运行进程。

### v1.42历史断点：独立物理边界图原型已实现，严格来源与驾驶约束尚未同时满足

- 先读[物理边界图、严格来源约束及复跑命令](docs/复核补充-物理边界图与严格来源约束-2026-09-10.md)。`spikes/physical_boundary_graph.py`只在原始已知零端宽事件处分开边界延续和通行laneLink；左右/行驶方向/生灭均有测试。保留原laneLink和源TOPO，缺失连接不猜同ID，复杂非零多对一仍不支持。长渐变合成例可在≥15m独立跨度通过来源/C2/构造动力学，不代表node4已解。
- 新入口`spikes/physical_graph_candidate.py`默认**绝对0.35m来源约束、保留口部原端状态**。旧误差包络仅显式`--input-error-envelope`对照；旧`road_boundary_family.solve_road`默认仍保留legacy，其他入口不能误称严格0.35m。失败/几何诊断退出非零、拒绝道路只是输入副本。正式原件未覆盖。
- 已跑node4：15m/旧误差包络仅road12/30局部动力学通过；6m仅几何/旧包络使10/12/13/30来源约束可行，但road10实际来源max2.047m、jerk25.589。不能以较好看当验收。严格0.35m、保留口部的最新几何诊断`out/shp-physical-strict-preserve-v142/node4.xodr` SHA **dc4acf4d87313d003ed04fa7f6eb48d321d726b6084c3da6ecce70434116af21**，481边界采样max0.350m，但2cm ay9.638874/jerk48.188128，FAIL。最严重为road10/-1，源`2023081117251325687`，s53.7401～53.7601m。参考轴1个97.138m Line，不等于没有横断面碎片：10section/213width，6m独立跨度仅诊断。
- 完整动力学的`shp-physical-strict-preserve-dynamic-v142`六个phase全部拒绝；平行口部严格版同样拒绝。**不是全局无解证明**，不能降速、删点、放宽验收。普通路几何与真实via联合仍需解决。
- 本轮发现并修掉试验接头回归：平行化普通路口部但不更新实测via，esmini144接口失败95→107，新增12中心接头；默认改`--mouth-mode preserve`后回95，无新增失败，**原95失败未解决**。报告留在输入/候选旁`.esmini-edge-interfaces.json`。esmini不支持部分geoReference参数的日志不掩盖，局部XY检查不代表绝对CRS通过。
- 已逐张查看`out/preview/shp-physical-graph-v142/`三机位、全图/四向来源图和北分流strict-preserve等图：假波动减少，但路口仍有台阶，西侧仍有间隙；不能随意填driving。最新43项相关回归通过/4.17s；非全仓、非完整14图。MAP本轮未重转，node4/17平滑与node18原点待核不变。
- **下步**：原始物理边界身份/接触位置和完整支持域验证，实测via及全部口部端状态联合求解，少控制点与来源/动力学同时约束；不要继续盲换均匀结点或把短段转移进width。无仍运行的验证进程，未commit，goal active。

### v1.41历史断点：SHP错误零宽已修，必须拆开物理边界图和通行TOPO

- 先读[源分合流复核与复跑命令](docs/复核补充-源车道分合流与错误零宽修复-2026-09-10.md)。**确定性错误**：node4北road10，原车道`2023081117282865661`分到全宽继续`2023081117251325687`和零宽支路`2023081117251327605`；旧中心距离贪心选支路，把全宽继续车道误收成0.0001m。默认reader新增端宽known标记，转换器保留显式0，TOPO候选按非零/宽度连续优先；不修改源TOPO，不将未选支路几何兜底接给无关车道。默认重跑读回全宽4.355399698m、正确predecessor=-1；支路0.0001m、无前驱。
- `spikes/shp_coordinate_candidate.py node4 --ordered-boundaries --output-tag shp-topology-fix-v141`已完成。`out/shp-topology-fix-v141/node4.xodr`是生成后的长轴试验，`out/shp-topology-fix-joint-v141/node4.xodr`只对road12/30联合求解通过（2cm jerk .980024/.406376），独立边界跨度≥15m；road10/11/13拒绝、全图G11与边缘FAIL。最终SHA `18e0b4a4a0d80ea53f7ee9df515537db01b4ca22a4c219f22161ce097195f356`。其余失败道路保留输入，不是解。正式输出未覆盖，长轴/边界QP未推广默认。
- 已查看`out/preview/shp-topology-v141/node4-north-split.png`（原件/修前/修后）、`node4-esmini.png`三机位和`node4-source.png`全域/局部叠图。假收口消除但仍有边界波动、口部台阶。叠图共同支持域外缘P95 .708791m/max1.824041m，不是全来源复原或精度提升证明。
- **下一步第一优先**：`road_boundary_family`目前跟laneLink建边界族、统一把新族与内侧边界绑C2；分流处旧外边界可能沿新支路外侧继续，新分界线才分出。这与车道中心通行关系不同。需基于原始边界/左右邻接/零宽事件建独立物理边界图，再求少控制点全局几何；别继续盲换求解器或均匀移动结点。对偶支持已加，`out/shp-topology-fix-joint-v141/node4.fit.json`可定位具体source/family。
- MAP原件新证据：`scripts/map_shp_bend_probe.py` / `out/preview/map-source-bend-v141/node4-west.{png,json}`，13局部原MAP顶点离某条SHP中心≤5.43mm，但Lane2索引4→5跨两条不同SHP链，9对局部点中1对无前向TOPO路径。未裁定哪份正确、未改点/ID。不能用贴近任意中心线证明同一车道准确。
- 非线性信赖域恢复试验已做，`spikes/nonlinear_feasibility.py` / `map_restoration_candidate.py`、`out/map-restoration-v141/node4.joint.json`：node4 phase0超限比17.786→17.705后50次拒绝；phase12局部停滞。始终保持来源/宽度/端状态硬约束，只有精确动力学通过才返回；不是全局无解证明。恢复及边界横向排序仅spike，正式门禁未放宽。
- 回归：两组不重叠 **50 passed**；真实 `test_direct_xodr_node4` **1 failed/103.69s**，在最后G11，未xfail/改阈值。新写出分流断言单独读取本次pytest产物PASS；compileall、diff-check通过。没有仍运行的验证进程，未commit，goal active。node18原点待核与其他既有未完成项不变。

### 2026-09-10 最新断点：node16完整来源及边缘验证通过；其余问题继续保留

- 先读[本轮出口覆盖、证据及复跑命令](docs/复核补充-MAP真实出口覆盖与坐标轴联合候选-2026-09-10.md)。默认MAP先以原始身份/走向匹配real出口，再合入双向全部来源支撑，node4西延10.592m、node18南延91.434m；不移动本路口接触端。真实出口不再按拟合参考轴`t>0`过滤（物理左右相对laneOffset），node17完整15来源恢复，G8 PASS但G11/边缘仍FAIL。
- `validate/map_source.py`已从原XML身份及连接独立推导可用邻居来源，即使漏写neighbor上下文也不能自证镜像合理。原件不变，速度下限不为过门禁而降低。坐标轴/整路族/解析铺面仍仅在spike中，未提升默认。
- 新`out/map-coordinate-review-v140/node16.xodr` SHA **b86d805e8eaae028cdbf8f6711150b61bfb8e05f6b987f887e21f66953fc8cca**：原始9车道全登记；G8中位 .097/P95 .310/max .714m，原件/G8/G11/口部内外缘/内部driving边缘/2cm动力学/XSD/**78处esmini中心边缘**全PASS。最大jerk .979992。普通road各1长Line，普通边界独立跨度≥15.036m，13连接各3spiral最短5.358m；固定C2连接宽度记录最短2.190m，不可泛称所有记录≥15m。
- 已查看`out/preview/map-coordinate-review-v140/node16-source.png`及`node16-esmini.png`三机位：西侧长车道恢复、无明显大洞/旧斜切；缺测区推断展宽/贴边标线、0.71m来源偏差仍未认定真实复原。10 restricted辅助面仍sim-only，绝对CRS未核验，候选继续BLOCKED。
- 可复跑`spikes/map_coordinate_family.py node16 out/map-coordinate-v140`及`scripts/freeze_full_source_candidate.py --case node16 --coordinate-v140`。`joint_mouth_candidate.run`现在返回成功布尔值，CLI失败退出非零，两个冻结入口检查返回，不能误把旧target当本次成功。默认正式14文件没有覆盖。
- **node4失败具体位置**：`out/map-clarabel-diagnostic-v140/node4.joint.json`，road10/right/-2/s=281.662699m，初始精确κ′=.00533744/m²、构造上限 .000211934/m²，比25.184；原始Lane2/3在s266→286→296有共同横向折返。可选Clarabel内点QP（Apache-2.0，venv新增0.11.1）确认部分phase初始线性解可行，但精确动力学的当前线性化子问题失败。**不是全局无解证明**；不要继续盲换求解器、放松容差或降速，应检查缺测/生灭/口部建模与非线性可行性恢复。
- **node18原点待核，不自动修复**：north/from(500,20)/Lane1第二点经纬度整数1063346865/295139806，距前后邻点弦1719.940m（弦长49.955m），原XML哈希与图在`out/map-raw-geometry-review-v140.json`及`out/preview/map-real-source-review-v140/node18-north-raw-detour.png`，图已查看。完整G8如实FAIL，不能裁掉该点再PASS。其余工程仍可推进。
- 本轮相关测试 **68 passed / 134.18s**（来源、离群复核、两个QP、联合口部、整路族、G8交付、连接宽度、边缘、铺面、MAP转换），compileall及diff-check通过；非全仓或SHP全流程。未commit、goal active。当前没有仍运行的验证进程。
- **下一步**：先node4失败局部及node17 road10/12/13的模型冲突；SHP仍需实测边界/中心/via联合，不能照搬无实测via的MAP连接。node18修正需明确审计输入；没有权限暗改原件。候选推广和整批14份闭环尚未做。

### 2026-09-10 最新断点：NODE5完整长度修复，仍不能当全产品完成

- 先读[完整原件覆盖复核](docs/复核补充-MAP完整原件覆盖与长参考轴-2026-09-10.md)。`map_to_xodr`来源manifest不再接受裁后的points_override；所有main来源车道先登记，选中的real邻居Link所有车道也登记。Link首点前有效车道前缀可INFERRED补参考支撑；末端停止线不自动移动。
- 新`validate/map_source.py`逐原始XML独立检查完整身份、点数/坐标顺序、来源遗漏、资格，记录SHA。`finalize_opendrive_g8(..., raw_map_paths=...)`对MAP必需`G8-source-integrity`；无原件UNAVAILABLE/BLOCKED，不能用缓存manifest自证。CLI、gen_all、closed_loop已传原始输入，闭环汇总也补纳真实边缘子门禁；本轮没有重跑/覆盖正式14份。
- `out/map-full-source-review-v139/NODE5.xodr` SHA **fac58f8059c16e7eccb57594fa4685230c195d60ebe77b2df9ecdf800836954b**：原始8车道34顶点全部登记；G8 P95 .302554m/max .472257m，原始顶点独立最大 .473384m。普通road分别202.311/119.410/240.828/115.409m，各1原语；11连接各3回旋线，最短6.406m；普通边界独立控制跨度≥15.0517m。全套G8原件/G8/G11/口部内外缘/内部driving边缘/2cm动力学/XSD/66处esmini中心边缘均PASS，jerk最大 .915541m/s³。
- 复跑：先`spikes/map_full_source_candidate.py NODE5 out/map-full-source-axis-v139`，再`scripts/freeze_full_source_candidate.py`。该长参考轴只对接近直的原始Link适用，最终Lane保真独立验收；不是把实测车道替换成直线。未推广默认。失败的5长回旋线南road10候选保留在`out/map-raw-support-v139/`（jerk10.924，未降速掩盖）。
- 已目检`out/preview/map-full-source-review-v139/`来源对比、路口俯视/斜视、完整范围、北远端、南远端图；截断/大空洞消除，局部仍 .47m误差，缺观测区的推断展宽细带与贴边标线未认定真实复原。10条restricted辅助铺面仍sim-only；绝对CRS未核验，全部BLOCKED。
- **下步全局优先**：`out/map-source-inventory-audit-v139.json`只读回查旧正式MAP，node3/node13来源清单PASS，其余5份FAIL，共13条被裁/改变。node4/17/18尤其是邻居real出口长度未完整覆盖；需要扩展公共道路支撑且不移动本路口接触端，不能回退成镜像或改身份。node16还有整路约束冲突；SHP需真实边界/via联合求解。缺观测≠车道不存在，应明确渐变/推断策略并继续查看远端标线。
- 本轮相关测试 **59项通过**：来源/交付/联合口部/整路族33项（3.79s），MAP/连接宽度/边缘/铺面26项（112.98s）；不是全仓或SHP全流程。源数据未改，无commit，goal active。

### 2026-09-10 最新断点：联合口部三份MAP通过，不能称默认工具已完成

- **最后一轮新发现 / 下步第一优先**：直接读取原始XML重新画来源图后，NODE5北lane1有4个、南lane1有5个点未被完整导出，最远原始点距同身份目标车道198.769m/161.421m，主要是纵向截断。G8仍PASS因为manifest来源被裁到较短Link道路支持范围。已在最终候选裁决加`raw_source_vertices_outside_written_lane`，图和点数据在`out/preview/map-joint-review-v138/NODE5-source.{png,json}`。先修完整原件覆盖门禁与道路长度建模，不能把这一问题作为一般0.6m拟合误差。node3/node13原始顶点没有>1m偏差，但未宣称全曲线完整性PASS。
- 先读[联合断面复核](docs/复核补充-路口断面联合求解与三份MAP验证-2026-09-10.md)。试验入口`spikes/joint_mouth_candidate.py`对普通道路共享边界族加junction端部`t′=t″=0`充分条件，在来源/正宽/世界动力学约束内求解，再以三段G2连接；固定C2变宽仅放入一个最长原语，避开参考κ′接头。普通road参考线、源停止线、速度/拓扑不变；绝非直接用推断轨迹替代SHP via。
- 最终隔离包 **`out/map-joint-review-v138/`** node3/NODE5/node13：G8/G11/口部内外缘/内部driving边缘/2cm动力学/XSD/228处esmini中心与边缘均PASS。`scripts/freeze_joint_mouth_candidates.py`可重建，图在`out/preview/map-joint-review-v138/`，三份三机位和来源叠图已逐张看。推断包络比较无超出/缺失、无>0.01㎡洞，但auxiliary面仍只作sim展示。
- 仍有保真误差：G8 P95分别0.470/0.510/0.385m；max0.596/0.608/0.399m。普通road独立控制跨度≥15m，连接3个参考原语，宽度至多5记录、最短2.529m，系数由两端宽度与整段长度固定，不可夸称所有记录都长于5m。
- 新`internal_edge_jets.py`在接头精确s处选取两侧表达式；独立esmini脚本新增`--edges`，边缘宽度及XY都由esmini自己求。先前“边缘单独G2”的3段反例导致中心曲率失配，5参考段/7横断面试验又违反结构与动力学，已保留在`connector-cross-section-v138`，勿复用作成品。
- **下一步**：node16 road12在来源/非负宽度约束冲突，6个结点相位均失败，报告`out/joint-mouth-v138/node16.joint.json`；不能自动放宽容差。MAP node4/17/18未解决；SHP需要带真实via/边界的联合求解，本轮未改SHP转换。还要将通过策略接入默认路径并跑全14张。
- **目检新增语义疑点**：node3有8条原始remote与所属Link.upstream相同（maneuver原值`100000000000`），现有回查逻辑生成同leg回转；未判定源/解析谁错、未改ID。证据`out/map-raw-connection-semantics-v138.json`。查看几何PASS时必须同时提示该疑点。
- 正式14份未覆盖；新三份也是候选/BLOCKED（未推广、辅助面边界、绝对CRS等）；goal active，无commit。本轮相关测试 **66 passed / 108.68s**，不是全仓或SHP全流程通过；新来源图按完整身份读取原始XML而非可能裁过的manifest support。

### v1.37历史断点：大斜角原因已解；真实边缘接头继续阻断

- 先读[铺面与边缘接头复核](docs/复核补充-MAP铺面斜切与车道边缘接头-2026-09-10.md)。旧双轴铺面把切线上不连续交集取min/max，填出斜角；连接路还用3.5m名义宽，而node16最终车道有2.261m。正常MAP生成已接入`ops/connector_width.py`真实端宽+固定三跨度C2，保持中心和速度。单三次试验造成边缘曲率跳变，已替换；不要复用`map-corner-width`早期单三次产物。
- 最终隔离包 **`out/map-surface-review-v137/`** 四份node3/NODE5/node13/node16：解析圆角带+内部受限铺面（`spikes/map_corner_surface.py`），普通道路全不变、连接planView/速度/拓扑不变；每个圆角3spiral，无1m宽度采样链。G8、旧G11、2cm driving动力学、XSD、102个独立esmini中心接头均通过。辅助面10road，仅sim展示，不得认定为AD严格图。新铺面尚未接入默认转换。
- 新`validate/junction_edges.py`分别审查真实左右边缘。四份仍有最大位置差0.0389/0.0910/0.1322/0.1466m，切向差0.305～1.622°，**全部边缘FAIL**。交付现在必需G8+G11+G11-edge-contacts；异常也阻断。原因是来源断面法向与按车道切向生成的连接road断面法向不同，不能仅钉中心或匹配宽度解决。下一步联合参考线/中心偏移/宽度/边缘端状态，而非碎段补丁。
- 候选面域越界面积由24.769/36.504/36.117/33.041㎡降到0.000018/0.000099/0.092403/0.001630㎡；node13尚有0.136㎡未覆盖，非实测路缘误差。`out/preview/map-surface-review-v137/`含旧/新3机位、轮廓差异图及原始Link/Lane分层叠图，已目检；4份无>0.01㎡孔洞。中心PASS仍不代表整体边缘PASS。
- 正式14份只刷新质量/裁决、XODR哈希未变，全BLOCKED；`out/g11-edge-contacts-v137.json`。候选四份也全BLOCKED。`scripts/freeze_map_surface_candidates.py`可重建/冻结，`--formal`只重算正式sidecar。源数据未改，无commit，goal仍active。
- 最新 **48项通过/143.57s**，不是全仓测试；SHP上轮20通过1失败仍未解决。本轮仅审计SHP原件的边缘，没有改SHP生成。其余MAP node4/17/18亦待解决。不要反复重跑旧图证明完成，按明确边缘端状态问题继续。

### 2026-09-10 最新断点：MAP四份候选通过；跨道路端状态已修复

- 先读[MAP端状态与少段候选复核](docs/复核补充-MAP车道端状态与少段候选-2026-09-10.md)。旧MAP连接误用参考线航向/常偏移曲率，已改为最终width/offset/border导数确定的车道端状态，data/default/full共用。G11-C现在审查每个显式junction laneLink的入口和出口，位置/航向/曲率俱查；未知引用失败。`route_continuity`也改为真实ID和精确端点，不再钳到最近车道。
- **正式14文件最新G11为0/14 PASS**，旧1/14也已失效。XODR原件未覆盖，只重算质量/裁决，见`out/g11-road-interfaces-v135.json`。此处v135指被复核资产。全部交付BLOCKED，`closed-loop-report.json`仍是历史全流程结果。
- **复核包`out/map-review-v136/`有node3、NODE5、node13、node16四份候选**，G8/G11/全图2cm动力学/XSD/esmini接口通过。102个接口独立测世界XY导数；不等于视觉或车辆控制器通过。四份普通road均每路1个长原语；node3无QP，另三份各一条road使用MAP中心观测联合边界QP，独立控制区间分别21.063/16.091/18.373m，严格保持同leg左右镜像，未减速。node13局部max从5cm变20cm是保真代价；NODE5选10cm构造容差；node16源误差也降低。
- 截图`out/preview/map-single-endpoint/`已逐张查看，**路口铺面小台阶/部分折角仍在**。不能用G11 PASS认定用户视觉目标完成。四份quality/decision均含candidate_not_promoted和visual_outer_edge_steps_unresolved，尚未替换默认资产。
- `spikes/map_single_primitive.py`为单段优先隔离选择；`spikes/road_boundary_family.py`新增source_mode=centers与严格mirror等式；`spikes/map_lane_family.py`消费manifest的真实Lane中心，绝非把中心当边界；`scripts/freeze_map_candidates.py`固定四份当前选择及速度元数据。OSQP仍仅试验依赖。node4/17/18候选失败（节点4原Link删点例外仍未解）。
- `scripts/esmini_lane_interfaces.py`新增独立位置/切向/曲率验算，0.02/0.01m双间距收敛；`local_geometry_audit.py`修复从文件名猜node ID导致node13/16漏测Link的问题，改按manifest实际region/node。
- 测试：MAP与端状态等37通过；最新快速收口37通过（重叠不累计）；SHP较广回归**20通过、1失败**，node4旧G11 PASS断言真实失败，未放宽/xfail。源文件、正式XODR不改，未commit，目标active。
- 下一轮：先解路口铺面台阶/折角并截图；继续MAP node4/17/18及SHP其余整路约束，不直接把spike全局开启。当前无外部输入阻碍工程继续。

### 2026-09-10 较早断点（已被上方复核覆盖）：世界曲率接缝补检；整路联合候选road13通过

- 先读[世界曲率与整路联合求解复核](docs/复核补充-世界曲率接缝与整路联合求解-2026-09-10.md)。参考线G2加横断面C2不保证世界车道G2；G11-D新增全部driving单侧接缝。正式14文件现在 **MAP1/7 PASS（仅NODE5）、SHP0/7 PASS，总1/14**，最新`out/g11-world-curvature-joins-v135.json`。下方2/14等是历史，不是当前结果。
- 新`spikes/road_boundary_family.py`联合laneOffset和左右边界、精确世界动力学、端状态和非负宽度。OSQP可选试验依赖，不是生产默认。候选`out/whole-road-worldg2/node18.xodr`（SHA ba29c045ce7b4c8ff28b5bca351ad34ab7ca62756d23e19f3bfb9de484520716）仅改road13：64个driving记录、2cm采样jerk0.9800003/ay0.855、162接缝0超限。原3段参考线不变，独立边界控制跨度≥15.455m。
- 来源边界769观测P95从0.367增至0.472m，max从0.805降至0.611m，保形/平滑权衡如实记录；构造容差0.5m未改G8阈值。G8仍只证明56个可比车道通过，322个排除出现未抹去。整候选其他road10/11/12仍失败，全图G11 FAIL，交付BLOCKED，未推广。
- 中间`whole-road-exact-050`粗采样0.98但2cm复核4.25，已否定。不要与最终`whole-road-worldg2`混淆。新验证`out/whole-road-worldg2/node18.verification.json`、三机位`out/preview/whole-road-worldg2/candidate_node18.png`已目检。esmini36接缝0.0cm/19穿越/0跳变，11次灭车道单列；路口小台阶仍在。
- 源车道`2023061915005392888`为5200→0mm宽，其LANE_LINK零宽末点距关联边界尖端1.883m，接邻车道中心。此处中心路径与边界中线不等价，不自动判源错或覆盖字段。见`source_lane_consistency.py`及资料盘点补记。
- CLI/`finalize_opendrive_g8`已要求G8+G11，异常也阻断；正式14份质量/裁决报告重新收口。51项较广回归与28项收口/数学单测通过（有重叠）；非全量转换回归。源文件、正式XODR不变，未commit，目标仍active。
- 下一步：用最新单侧曲率清单修MAP普通路（node3以前PASS但4处失败）；继续SHP其余道路的物理边界/端状态模型，独立处理路口外缘。无需等待外部输入才能继续。`closed-loop-report.json`未全流程刷新，不能引用旧PASS。

### 2026-09-10 最新断点：共享边界求解试验；驾驶车道不再按标签免检

- 先读 [共享边界拟合与免检漏洞复核](docs/复核补充-共享边界拟合与驾驶车道免检漏洞-2026-09-10.md)。`lane-transition-taper` 也覆盖正常宽度的 through lane，不能仅凭该标签跳过动力学。G11-D现审计全部写出的driving（包括旧/自定义policy试图排除的标签）；新正式14文件 **MAP2/7 PASS（NODE5、node3）、SHP0/7 PASS**。完整报告 `out/g11-no-driving-exemptions-v135.json`，正式XODR未改。
- 新隔离求解器 `spikes/source_boundary_joint.py`：按来源边界身份，在15–20m独立控制间距的三次B样条上联合求解，非负宽度采用Bernstein控制约束，保留junction口部三阶以内的位置/一、二阶导数；无连接裁剪端由来源约束，不继承旧错误端状态。仍是spike，未接入正式路径，未完成全路口。
- `out/source-boundary-kinematic/node18.xodr` 是有改进但仍FAIL的候选。road13同口径64个driving section/lane记录：jerk **243.85→3.76m/s³**，ay **22.91→0.787m/s²**；拟合来源边界采样P95 **0.566→0.350m**，max **0.805→0.794m**，最短独立控制区间15.455m。不要引用中途只查43条记录时的0.91“通过”。
- 该候选XSD通过、路口铺面无孔、esmini36接缝0.0cm/19有效穿越/0跳变（11次灭车道并线另列）。3机位图 `out/preview/source-boundary-kinematic/candidate_node18.png` 已目检，路口外缘仍有小台阶；这不是动力学通过。
- 最新求解器把全部driving纳入代理动力学约束后，node18 road13的15m/.35m组合返回不可行；`out/source-boundary-all-driving/` 保存REJECTED副本，不是更优结果。road10/12也不可行。后续应联合处理laneOffset、实测车道增减域、边界轨迹及参考线，不应继续叠局部补丁、豁免或降速。
- `scripts/probe_lane_section.py` 可逐断面显示原始SHP边界/中心、最终宽度/曲率；`tests/test_source_boundary_joint.py`覆盖无碎段、源边界恢复、口部保持、非负性失败不改文件。未提交。目标仍active、未完成，无须等外部输入才能继续工程。
- 本轮44项相关测试通过/111.09s（不含全量SHP转换测试）。MAP node4原始Link/Lane再叠图 `out/preview/no-driving-exemptions/node4-map.png` 已查看：车道可比中心P95=.195m，但原始Link参考点最大2.458m仍在，不能互相代替。候选node18 G8可比域通过，质量/交付已合并G11 FAIL和候选阻断。

### 2026-09-10 后续复核：旧14/14通过结论已被新的全车道审计覆盖

- 先读 [口部铺面与全车道动力学补充](docs/复核补充-口部铺面与全车道动力学-2026-09-10.md)。修复了 G11-D 按 `source-support-too-short` 免检已写 driving lane 的漏洞；当前正式14文件 **MAP7/7 PASS、SHP1/7 PASS（6 FAIL），总8/14**。报告 `out/g11-full-written-lane-review-v135.json`；旧 closed-loop-report 不是最新门禁结果。
- `junction_surface.py` 已实现有界沿路端帽修复、已知 median 延续和共享边界铺面家族。node18 完整 SHP 重跑候选 `out/mouth-surface-integrated/node18.xodr`：36/36未裁剪 via通过、0 fallback、G9无孔、XSD/G8通过、esmini36接缝/0跳变；**新G11-D仍7项失败**，没有提升默认路径。
- node4 的铺面候选 G9无孔，但原始曲线仍2条超线；西侧 road11/30 间可见空带。node13候选因并行出口额外1.05m间隙而明确失败，`.probe.json`/`.source-lanes.json`已保留，可不重跑曲线而复核铺面。
- 新实物证据 `out/preview/source-objects/`：node13该分隔附近有源护栏 `2023061511154388846`，不要把间隙拓宽进driving lane；普通道路也须按多车道组/分隔带一起重建。node4西侧两方向因覆盖长度差被拆开，不是缺出口。
- 无车无地面截图已目检：`out/preview/mouth-surface-integrated/integrated_node18.png`、`out/preview/mouth-surface-v5/surface_v5_node4.png`；仍有外缘小台阶。ASAM Annex D把restricted纳入平滑检查，辅助铺面只属sim档，不是通用禁行保证。
- 本次42项独立相关测试分批通过（含SHP真实4项/425.65s）；全量未重跑。原始数据、正式XODR未改，工作树未提交，目标未完成。G11新增failed_lane_intervals：node18候选最坏已定位road13/lane3、源2023061915005389658、s=38.0152～38.2652m，jerk约137.04；其次road12/lane4、s=50.0001～50.2501m。下一步联合修复短断面的宽度/偏移几何，不能靠来源排除项或单纯降速恢复全绿。

### 2026-09-10 最新断点：v1.35 局部实测复核，来源完全复原尚未完成

- 详细报告：[复核报告-源几何保真与路口平滑-v1.35](docs/复核报告-源几何保真与路口平滑-v1.35.md)。不要仅凭历史 14/14 PASS 宣称整个用户目标完成。
- MAP `Link.points` 为方向道路参考中心，`Lane.points` 为车道中心；两层分别测量。修复不同采样间距的点对点误差底噪，改连续线段距离后平滑候选能正确选择。
- SHP：固定真实外缘；node13 按共同支持范围处理边界续线；可比 via 优先在严格来源容差内选 3→4→5 段（每段≥6m）。node18/3/13 三条重点 via 的 P95 分别降到 0.208/0.226/0.306m，均为四段，真实回归已锁定 node18/node13。
- MAP 铺面圆角又发现真实缺陷：原代码以“口部→路口中心”向量替代道路方向；现用最终写出道路两侧外缘的位置/切线/曲率（包含 width/laneOffset 导数）连接偏心路口，已加偏心及变宽回归。
- 正式版仍有 SHP 30 条少段保真 fallback；原 52 条“端状态冲突”实为用 7m 弦代切线的误判，已恢复 G8 验收且全通过，阈值未放宽。最新 14 文件 G1–G11+esmini PASS。**新增未裁剪全域审计仍发现 59/189 条超过 P95 0.75m/max 1.5m 复核线**；189 条源 via 的前后端点全部在 1cm 内闭合。不能归因源拓扑断裂，不能宣称全部准确复原。
- node18 road135 原始端点被共同口部挪过 9.229/5.214m；新增隔离候选 `source-envelope-candidate` 把入口前移并按 TOPO 合入真实普通车道尾段。8m 试验中 node18 36/36 未裁剪 via 复核通过、0 fallback；road135 三段 15.340/7.761/8.388m、P95 0.398m/max 0.486m，esmini 36 对接缝/0跳变、G11 PASS。
- **候选尚未提升**：`out/mouth-candidate-8m-paving/node18.xodr` 的新入口尾带与旧源路口面并集为 MultiPolygon，G9 铺面/leg overlap=0。node4 3m 候选已生成（1 fallback），同样待处理铺面。默认仍为 legacy-max-endpoint；不要把隔离候选覆盖正式 `out/direct_xodr/`。
- 下一工程重点：基于实测边界重建前移入口后带中央岛/孔的物理面与尾段来源映射，随后对七路口做受约束入口选择；8m 只是 node18 探索值，不能直接全局常量化。不得靠提高门限、取凸包填岛或一味降速声称完成。车型转角/扫掠包络未验证。
- 产物继续放 `out/direct_xodr/`、`out/m2x/`；最新快照 `out/geometry-review-v135.json` 包含 SHA256、段长与连接例外；底图页 `out/preview/basemap-overlay/index.html` 把待核连接标红，支持点击查看源 ID。截图 `out/preview/sweep-v135-final/`、静态卫星叠图 `out/preview/basemap-static/`、局部热图 `out/preview/local-audit/`。
- 本轮相关回归 42 项分批通过；SHP+入口单元 8 passed/462.45s，后增跨 laneSection/laneLink 行车链复核回归，入口/审计单元 5 passed/1.01s。未裁剪报告 `out/unclipped-via-audit-v135.json` 与 snapshot 校验 SHA256；源/目标底图已改为原件完整 SHP 车道线，并把全域超线连接用独立置顶红线显示，已在浏览器复核。原始数据未改，工作树未提交；CRS 绝对校验仍缺失。

### 2026-08-22 最新断点：v1.34 少段平滑实现与真实数据闭环

- v1.33 专家重大修改项的核心代码已落地：`validate/g11.py` + G11-A～E 候选 policy、少段连接拟合、共享物理边界求解、精确 `lane.width` 物化、最终世界边缘精确对拍、车道中心动力学审计与显式 `v_supported`。
- 已重新生成 `out/direct_xodr/` 与 `out/m2x/` 共 14 文件；`scripts/closed_loop.py --no-regen` 结果为 **G1～G11 + esmini 14/14 PASS**，换乘 0.0cm、随机行驶 0 跳变、路面孔洞 0；最终全量 `pytest -q` 为 **84 passed**（627.62s）。
- 少段结构：SHP 普通 road 31 条/41 段、每路≤3、最短 17.265m；SHP connecting road 189 条/602 段、每路≤5、最短 1.039m、中位 11.585m；MAP 普通 road 每路≤4；MAP connecting road 95 条/285 段、每路≤3、最短 3.715m。不存在逐点米级碎段链。
- SHP 横断面先按绝对共享边界求解，再无损转成相邻边界差的 `lane.width`；最终文件不含 `lane.border`，避免 esmini 3.6 将其宽度读成 0。自动测试已锁定逐站几何等价。
- SHP 外缘门禁改为精确点到折线段投影，并按 provenance 区分可比边界与 source-extension/短支撑修复。7 路口双向 P95 全≤0.65m，最坏 node3 为 0.646/0.561m。
- 视觉证据 `out/preview/sweep-final/`：14 张四机位拼图、7 张 `overlay_*.png/.json` 已逐张复核。node17 弯边、NODE5 窄幅和 node3 中央白带均与 SHP 原始边界/中央分隔带重合，不是生成蛇形或铺装孔洞。
- 源 40/60km/h 与短距离车道生灭几何不自洽时，生成器不挪真实道路；按 `ay≤2.5m/s²、jy≤1.0m/s³` 写显式安全速度上限，保留 source speed/cap/basis provenance。G11 可 PASS/WARNING，但任何 FAIL 都阻断闭环。
- 权威方案已升 v1.34；专家稿已升 v0.3。准确状态是“金凤两条 G0 主线少段平滑技术闭环完成”，**不是**“所有图商/城市/消费者均完美”。仍开放：第二图商/跨区域样本、`odr15-ad-strict` 与 `odr15-sim` 显式 CLI Profile、G11 policy 跨域校准。
- 工作树仍未提交；原始数据未修改。生产交付仍因金凤 CRS 绝对坐标未核验而 BLOCKED，这与几何技术闭环相互独立。

### 2026-08-21 历史断点：v1.33 已吸收专家意见，等待实施

- 专家结论：v1.32 方向正确，但必须“重大修改后实施”；完整意见已逐项纳入 [docs/专家评审稿-OpenDRIVE少段平滑重构方案.md](docs/专家评审稿-OpenDRIVE少段平滑重构方案.md) v0.2。
- 最少段算法改为硬约束可行集 + 字典序选择；禁止用 `wN` 权重大小模拟工程裁决。
- G11 改为 A～E，覆盖 planView、laneOffset/width/laneSection、内部构造式连续、实际 lane center/edge 动力学以及来源/消费端兼容，防止复杂度转移。
- junction 使用车道级口部状态和受限联合优化；三 clothoid 是候选，不是固定输出；五段内无解则失败/例外。
- shared reference line 改为默认候选，必要时拆方向性 carriageway；`<5m` 是风险信号，退化按 `<0.5m`、`<3%`、滑窗、可合并性和动力学综合判断。
- 输出 Profile 分层：自动驾驶默认不输出 auxiliary paving road、不启用 `paramPoly3`；仿真展示 Profile 可在 restricted/无拓扑/不可路由前提下保留铺面。
- 实施顺序锁定 M0 基线冻结→M1 G11-A～E→M2 合成真值内核→M3 SHP→M4 MAP→M5 消费端矩阵/真实闭环。
- **状态边界**：上述均为方案更新，尚未修改生成代码；v1.31 正式输出仍不满足新的 SHP 反碎片化要求。

### 2026-08-21 最新断点：v1.32 少段平滑重构待专家评审

- 用户明确要求：生成 OpenDRIVE 的平滑不能依赖连续米级小段拼接，因为目标消费者包含自动驾驶系统。
- 对 v1.31 正式 14 文件复核：SHP 路口连接/附属道路共 1441 个 geometry，最短 1.20m、中位 2.72m、76.7% 小于 5m、单 road 最多 17 段；MAP 全部最短 3.73m、中位 13.84m、3.8% 小于 5m。SHP 明确不满足新口径，MAP 也必须经新门禁重新确认。
- 旧 `scripts/closed_loop.py` G7 允许普通 leg 最短/中位 3m，connector 最短 1m、中位 2m；因此历史 14/14 PASS 只能证明旧门禁通过，不能证明无碎段。
- 已落独立专家评审稿：[docs/专家评审稿-OpenDRIVE少段平滑重构方案.md](docs/专家评审稿-OpenDRIVE少段平滑重构方案.md)。其核心是“输入点只作约束、物理道路共用 reference line、误差约束下全局最少 line/arc/spiral、连接路固定少段 G2、G11 反碎片化与动力学门禁”。
- **状态边界**：v1.32 仅为方案，尚未修改生成器或门禁；`out/direct_xodr/*.xodr`、`out/m2x/*.xodr` 仍是 v1.31 基线，不得对外宣称“无碎段完美完成”。

### 2026-08-21 最终断点：道路边缘蛇形治理与 G10

- 用户再次用 odrviewer 和 SHP/xodr 叠图指出旧产物“道路边缘蛇形、多个位置不像源 SHP”。结论：质疑成立；旧 G1–G9 没有直接审计最终世界坐标边缘，参考线连续/路面无洞并不能推出 width/laneOffset 平滑。
- SHP：`_pair_legs` 升级为一进口对多并行出口的物理 leg 分组，node13 两组并行出口不再生成孤立 `road30`；长对向轴刚体补齐缺测侧；病态零/超宽 transition 显式绕过；接触端覆盖全部进出口 lane mouth，node18 短尾修复。7 个真实 SHP 均 G8/G10 PASS。
- MAP：逐 section 独立 FC 限幅改为整条 road 的形态保持 PCHIP 共享导数；node4/node18 最坏边缘切向跳变从约 6.46°/12.55° 降至约 0.0006°/0.0004°，且站点位置不变、镜像语义不变。
- 新增 `validate.smoothness.edge_shape_quality/edge_shape_audit` 与正式 G10：世界边缘横向二阶变化、曲率、外缘换向和 section 切向；新增 5m 宽度蛇形故障注入，证明旧参考线/端点门禁会放过而 G10 会拒绝。
- 新增 `validate/shp_boundary_fidelity.py`：按 source lane 身份将 SHP `LANE_BOUNDARY` 与 OpenDRIVE 最外缘双向对拍。7 个路口中位 0.136–0.249m、P95 0.353–0.603m；node4 最新为 S→T 0.208/0.418m、T→S 0.206/0.400m（中位/P95）。
- 正式复跑：`scripts/closed_loop.py --no-regen` 退出 0，**14/14 G1–G10 + esmini PASS**；`pytest tests -q` 为 **75 passed**。最短段 node17 写出为 2.999999999m 是十位有效数字的 1nm 浮点边界，仅在比较中容纳 1e-8m 数值误差，工程阈值仍为 3.0m。
- 视觉证据：`out/preview/sweep_edge_v38/` 内 14 张 × 5 机位拼图（70 个视角）已逐张检查；node4 边界叠图为 `node4_shp_xodr_overlay.{png,json}`。正式产物为 `out/direct_xodr/*.xodr` 与 `out/m2x/*.xodr`，不要再使用旧的 `out/manual_g0_*` 文件判断当前实现。
- 文献依据已核对 ASAM reference-line、lane smoothness Annex D、lane width/laneSection 规则与 MMS→OpenDRIVE 道路边界重建论文；详情写入方案 v1.31。
- 工作树仍未提交；生产交付状态仍因金凤 CRS 只有 `internally-consistent` 而 `BLOCKED`，技术门禁 PASS 不改变该红线。

### 2026-08-21 增量：MAP 严格镜像、路口中心圆角铺面与 SHP 边缘叠画

- 用户用 odrviewer 截图标出的尖角/蓝缝是真缺陷：旧门禁只覆盖车道中心线和 laneSection 边缘，不覆盖 road surface union。v1.30 将 MAP curb-return 改为两端零曲率的 `SolveG2` 回旋线链；MAP/SHP 均以两条近正交、重叠的 `junction_paving` road 表达二维路口面，且每条仅一个 laneSection、多条 width/laneOffset 记录，避免查看器逐 section 三角化裂缝。
- 新增 G9 路面连续性：铺面 road/并集均须单连通、孔洞（>1cm²）为 0、每条 leg 与铺面面积重叠 ≥0.2m²。真实 14 文件：MAP 最小 0.439m²、SHP 最小 0.243m²，全部 PASS；`scripts/closed_loop.py --no-regen` 为 G1–G9 + esmini 14/14 PASS，pytest 71/71。
- 同用户机位最终截图：`out/preview/sweep_surface_v129/m2x_node4_user_camera.png` 与 `direct_node4_user_camera.png`。MAP 箭头所指裂缝/尖点已消失；SHP 仍保留源边界本身的局部折线/收放，不将来源形状误判成生成裂缝。
- `scripts/visual_sweep.py` 改为等待有效帧后再终止查看器，解决复杂 SHP 在固定 7 秒内尚未出图而被误判的问题；最终 14 张四视角拼图在 `out/preview/sweep_surface_v130/`，已逐张汇总目检。

- `mapforge/ops/map_to_xodr.py` 的 MAP 缺出口兜底已改为同一 leg 进口**全断面的严格左右镜像**：镜像边界位置及其对 s 的导数，保留全部进口车道的实际宽度/变宽/生灭，出口车道数也等于该 leg 进口车道数；不再按 `connectsTo` 最大目标号裁少/补多，缺失目标号只在连接映射时回落到最近镜像车道。
- MAP 路口中心旧实现“连接路凸包 + buffer”会产生过大的八边形补丁并斜切道路口部；现改为按四个道路口部左右外缘构造开口、相邻开口间用三次 Bézier curb-return 圆角连接，铺面横断面用 1m PCHIP，扫掠轴锁到最远对向口部。铺面仍为 `restricted`、不入拓扑；16 条 G2 connecting road 才是行驶连接。
- node4 MAP 重跑：odrviewer 实图四角圆滑、无蓝色孔洞、无八边形斜切（`out/manual_g0_map_test/node4_odrviewer_fixed_center.png`）；XSD PASS、48 个几何接头 0 违例（最坏位置 0.126mm、航向 0）、断面边缘台阶最大约 11.34mm、16 个 junction 接缝约 0.0001mm；esmini 48 次穿越、0 跳变、换乘缝隙 0.0cm。全量 pytest **71 passed**。
- SHP 与 xodr 边缘叠加证据已落 `out/manual_g0_shp_test/node4_shp_xodr_overlay.png`，量化明细为同目录 `node4_edge_comparison.json`。全部配对边缘中位 0.077m/P95 0.505m；道路最外缘中位 0.077m/P95 0.569m、1.5m 内覆盖率 97.6%；约 5m 的长尾位于源边界尾段/Link 分界/车道生灭与桥接支持域交界，需后续按 source span 定位精修，不能用全局近邻把它隐藏。
- 本增量未提交；CRS 阻断状态不变。

> 本节是当前会话的最终断点。v1.26 的技术门禁已经完成；下方 v1.25 历史记录仍有效。注意：**技术门禁 PASS 不等于生产交付获批**，当前金凤 CRS 只有 `internally-consistent`，14 个样本的交付决策仍按硬约束为 `BLOCKED`。

### 当前结论

- GUI-01 已完成信息架构评审；GUI 实现未开始。正式 G8 依赖已解除，GUI-02 仍须等待 B1–B4。
- 正式 G8 的契约、来源 manifest、目标 occurrence/component、结构化 gate result、sidecar、交付阻断和机械校准脚本已落地。
- 固定 7 路口 × MAP/SHP 两管道的 14 文件可以完整生成；正式 policy 为 version=`1.0`、lifecycle=`active`，14/14 G8 均为 `PASS`。
- 最新正式全量结果：`scripts/gen_all.py --report-only` 生成矩阵 **14/14**、各 G8 sidecar 实际违规总数 **0**；随后 `scripts/calibrate_g8.py` **PASS**，calibration 8 文件/449 lanes 与 locked-validation 6 文件/320 lanes 的 ceiling 违规均为 **0**。实现过程中曾为 1298 项；没有提高或临时覆写阈值。
- `out/g8-opendrive-jinfeng-v1.candidate.yaml` 已通过语义差异审查并提升为正式 `profiles/validation/g8-opendrive-jinfeng-v1.yaml`。`load_policy` 复核语义 SHA256=`3409f658101c7550ffa1481a12f5ceade5173ac8d60a249f312b5caa71d58b15`，与候选一致；classes/sampling/exclusions/ceiling 未改变。两文件仅换行符不同（候选 CRLF、仓库 policy LF）。
- 全量 pytest：**66 passed in 94.32s**。
- `scripts/closed_loop.py` 与 `scripts/closed_loop.py --no-regen` 均退出码 0：14 文件 G1–G5/G7/G8 全部 PASS，suite 级 G6 esmini RoadManager 14/14 PASS；XSD 由 G1 逐文件验证，`out/closed-loop-report.json` 状态为 `PASS`。
- `scripts/visual_sweep.py` 已生成 14 张统一视角拼图并逐张人工检查；涉及本轮修复的 direct node3/node4/node13/node17 与 m2x node17 均平顺，全部 14 张未见新增孔洞、锯齿、合成振荡、断裂 taper 或明显路面不连续。
- 闭环报告的 14 个 `delivery_decision` 均为 `BLOCKED`，唯一共同硬阻断是 `crs_not_absolutely_verified`（当前仅 `internally-consistent`）；部分 MAP 文件另有 `mirror-no-source-geometry` 人工复核项。这是既定 fail-closed 行为，不是 G8 失败，禁止绕过或把 v1.26 表述为可直接生产交付。
- 正式 G8 技术闭环至此完成，版本基线更新为 **v1.26**；工作树仍未提交、未推送，R0 的恢复点/拆分提交事项仍未完成。

### 独立复验与发布后补正（2026-08-14，另一会话复核）

上述结论已由独立复跑逐条核对，**全部成立**：

| 复验项 | 实测结果 |
| --- | --- |
| `pytest tests -q` | `66 passed`（补正后 `68 passed`） |
| `scripts/closed_loop.py` | 14/14 文件 G1–G8 全 PASS，G6 esmini suite 级 PASS |
| `scripts/closed_loop.py --no-regen` | 同上，PASS |
| active policy 语义 SHA256 | `3409f658…d58b15`，与方案/README 声明一致 |
| 校准双 split | calibration 8 文件/449 lanes、locked-validation 6 文件/320 lanes，ceiling 违规均 **0** |
| 14 份 sidecar | 56 个 JSON 全部 `allow_nan=False` 解析通过；policy hash 与 active 全一致；`errors: []` |
| exclusion codes | 11 类全部在 `allowed_exclusions` 内，无未知码 |
| CLI 非 DELIVERABLE | `convert --to xodr` 打印 `G8: PASS` + `DELIVERY-STATUS: BLOCKED`，退出码 **2**，不打印最终 OK |
| `git diff --check` | 通过；`.claude/worktrees/` 未进入 Git 状态 |

**逼近上限的余量（后续数据/几何改动需盯住）**：769 条可比较 lane 的最坏值——endpoint `1.440 / 1.50`（余量 4%）、stopline `1.367 / 1.50`（9%）、s2t median `0.492 / 0.60`（18%）、p95 `1.080 / 1.50`、max `1.498 / 3.00`、coverage 全为 `1.000`。即 G8 是真通过，但 endpoint 一项余量很薄。

**复验中发现并已修复的两个流程缺陷（`scripts/calibrate_g8.py`）**：

1. policy 提升为 active 后再跑校准，会误报 `source_policy_not_draft`；
2. 更严重：该失败分支会**删除 `out/g8-opendrive-jinfeng-v1.candidate.yaml`** 这一提升证据（复验时已实际触发删除）。

现按 `lifecycle` 分两种模式：`draft → calibrate`（原行为，含候选清理）、`active → verify`（只读复验，绝不写/删候选，回归仍 FAIL）。报告新增 `mode` 字段。新增 2 项测试覆盖两种模式，`pytest 66 → 68`。当前复验输出：

```text
G8 verify: PASS
  calibration: 8 files, 449 lanes, 0 ceiling violations
  locked-validation: 6 files, 320 lanes, 0 ceiling violations
  active policy 1.0 (3409f658101c…) 仍被当前 14 文件满足
```

**draft policy 的可复现性**：`profiles/validation/` 只保留 active，且该目录**未纳入 Git**，draft 原件已不存在。经实测，draft 可从 active **无损复原**——去掉 `calibration.promotion`、`lifecycle` 改 `draft`、`version` 改 `1.0-draft`，复原后的语义 SHA256 = `e16d583dbb71d30ec33ef008fa3fc5fe37613652313a77b927035b78eace71a7`，与 `promotion.source_policy_sha256` **完全一致**，提升链可审计。为避免 `profiles/validation/` 出现两个 policy 造成加载歧义，未把 draft 落回该目录；日常复验请直接用上面的 `verify` 模式。

**遗留风险**：`profiles/validation/` 与 `out/` 均未入库，正式 policy 目前只存在于本地工作树；提交前若丢失，需按上述配方从方案文档记录的 hash 复原并核对。建议尽早把 `profiles/validation/` 纳入版本控制。

### 已落地代码

- 新增：
  - `mapforge/validate/g8_model.py`
  - `mapforge/report/decision.py`
  - `profiles/validation/g8-opendrive-jinfeng-v1.yaml`
  - `scripts/calibrate_g8.py`
  - `tests/test_g8_manifest.py`
  - `tests/test_g8_components.py`
  - `tests/test_g8_faults.py`
  - `tests/test_g8_delivery.py`
  - `tests/test_calibrate_g8.py`
- 主要修改：
  - `mapforge/validate/lane_fidelity.py`
  - `mapforge/adapters/opendrive/writer.py`
  - `mapforge/adapters/shp/ibd_reader.py`
  - `mapforge/adapters/shp/profile_source.py`
  - `mapforge/ops/map_to_xodr.py`
  - `mapforge/ops/shp_to_xodr.py`
  - `mapforge/report/deliver.py`
  - `mapforge/cli.py`
  - `scripts/gen_all.py`
  - `scripts/closed_loop.py`
  - `tests/test_map_to_xodr.py`
  - `tests/test_shp_to_xodr.py`

### 已确认并修复的真实缺陷

1. MAP real-exit 原来只按 node id 查邻居，导致 `(3,3)/(500,3)`、`(3,4)/(500,4)` 跨 region 串绑，出现 200m 级偏差；现已改为完整 `(region,node)` + upstream + 空间一致性匹配，不匹配时退显式 mirror exclusion。
2. MAP/SHP 点列端点外仍在 snap 半径内的点曾被压到同一 s；现已按端点纵向支持域裁剪。
3. SHP `eval_planview(0.5)` 的采样点距并非严格 0.5m，旧代码用 `index*0.5` 累计产生数米 support_s 漂移；现已改用真实累计弧长，并替换 `u/0.5` 索引。
4. SHP via 的 G2 桥接 apron 原来整段冒充实测 via；现在只比较实测中段。无法保留来源的拓扑间隙使用 `source-topology-gap-bridge` 显式 exclusion，并强制进入 review。
5. SHP 生灭/零宽车道 taper 原来参与完整中心线比较；现在用 `lane-transition-taper` 显式裁掉不可比较区间，来源 lane 仍保留在完整 manifest 分母中。
6. SHP 跨 span 配对原来按整段 median 横距，现改为接缝 `v1↔v0` 配对；长 laneSection 等分到不超过 20m。
7. G8 evaluator 现在强制采样每条 lane 的 `support_s` 精确边界，endpoint/stopline 不再受 1m 网格截断影响。
8. SHP 稀疏 2–5 点轮廓、MAP 稀疏点列改用区间内分段线性插值；观测区间外保持端值，禁止斜率无限外推。
9. SHP 端点纵向越界点此前虽已从 manifest/G8 支持域裁掉，却仍参与目标横向轮廓回归，形成不可审计的“幽灵影响”；现已让 `_prof_eval` 与 manifest 共用 `support_ps/support_pd`。定向重跑后 `2023061509384520037`、`2023061509384524082` 的端点、p95 和 coverage 违规全部消失。
10. SHP laneOffset/median/相邻宽度为保持 C0 连续会覆盖来源中心端值，但此前仍把被连续化占用的整段算作可比较来源。现按最终写出 width 的文件语义计算来源端点横移，只在来源 lane 首/末 occurrence 裁掉超过固定 0.5m 构造容差的连续化子段，并使用既有 `lane-transition-taper` 记账；不读取 policy 阈值。node3/node4/node17 的剩余 SHP 违规归零，且未再制造 one-source-many-target。
11. SHP 2–5 点来源在世界坐标中是分段直线，但相对弯曲参考线的 `d(s)` 并不线性；现先沿原折线 0.5m 加密再投影并分段插值，不平滑、不外推、端点和总长不变。node13 三条稀疏 lane 的 median 违规归零。`2023061416492734058` 经连续化裁剪后稳定域仅 1.85m（小于 3m），现整条以 `lane-transition-taper` 显式排除，来源仍保留在完整 manifest，`full_source_length_m=12.204`、`compared_length_m=0`。
12. MAP 稀疏点列存在同一“世界坐标折线 ≠ 弯曲参考线中的端值线性 `d(s)`”问题。现将原始支持点/manifest 与目标重建轮廓分离：公共 `_lane_profile` 默认行为和 manifest 均保留原点列；生产写出显式启用 0.5m 等价折线加密后再投影。MAP/node17 的最后 2 项 median 违规归零，且相邻 lane 无新增违规。

### 最近验证结果

```text
pytest tests/test_g8_manifest.py tests/test_g8_components.py tests/test_g8_faults.py
       tests/test_g8_delivery.py tests/test_calibrate_g8.py -q
=> 21 passed

pytest tests/test_map_to_xodr.py -q
=> 5 passed

pytest tests/test_map_to_xodr.py tests/test_shp_to_xodr.py::test_lane_fidelity_and_no_hairpin -q
=> 6 passed

python scripts/gen_all.py --report-only
=> 14/14 文件全部生成；draft policy 下均为 UNAVAILABLE（预期）

python scripts/calibrate_g8.py
=> FAIL；calibration 15 项 + locked-validation 5 项 ceiling 违规

# 上述正式全量之后的本轮定向验证（仅 SHP node3/node4/node13/node17）
=> node3 / node4 / node13 / node17 均无实际 G8 违规（仅 draft policy_not_active）
=> tests/test_shp_to_xodr.py::test_lane_fidelity_and_no_hairpin：1 passed
=> MAP/node17 无实际 G8 违规；tests/test_map_to_xodr.py：5 passed
=> scripts/gen_all.py --report-only：14/14，输出矩阵完整，各 sidecar 实际违规总数 0
=> scripts/calibrate_g8.py：PASS；calibration 8/449/0，locked-validation 6/320/0
=> active candidate 已审查并提升；语义 hash 3409f658... 保持不变
=> active policy 下 scripts/gen_all.py：14/14 G8 PASS，退出码 0
=> 全量 pytest：66 passed in 94.32s
=> scripts/closed_loop.py：G1–G8 + esmini 全部 PASS，报告 out/closed-loop-report.json
=> scripts/closed_loop.py --no-regen：G1–G8 + esmini 全部 PASS
=> scripts/visual_sweep.py：14 张统一视角拼图全部生成并人工检查通过
=> out/closed-loop-report.json：技术闭环 PASS；14/14 交付仍因 CRS 未绝对核验而 BLOCKED
=> 正式 G8 技术闭环完成，版本基线 v1.26
```

### 当前剩余 lane

最近一次正式 active-policy 全量门禁中，**无剩余 G8 违规 lane**。不得因后续新样本失败而提高 ceiling；应按本轮相同方法定位来源/目标语义。

### 下一接手动作

1. 若继续做 GUI-02，先完成 B1–B4：统一 `ConversionJob/ConversionResult`、run 目录、结构化 gate JSON 与统一预览入口；正式 G8 已不再是阻塞项。
2. 获取实测控制点并完成 CRS 绝对核验；在此之前保留 `crs_not_absolutely_verified` 交付硬阻断，禁止把 `internally-consistent` 改写成绝对正确。
3. 按遗留规划建立可回退恢复点并拆分当前混合工作树。此项涉及提交/remote，由仓库负责人明确授权后执行；当前会话未提交、未推送。
4. 后续代码改动的最小复跑命令：

```bash
.venv/Scripts/python -m pytest tests -q
.venv/Scripts/python scripts/closed_loop.py
.venv/Scripts/python scripts/closed_loop.py --no-regen
.venv/Scripts/python scripts/visual_sweep.py
```

### 仓库与清理注意

- 当前所有改动均未提交、未推送；原始 `shp_0222-0326/` 与 `v2x_map_xml/` 未修改。
- 必须保留本轮之前已有的 GUI-01 文档改动，不得回退。
- `out/debug-node18.*` 是本轮诊断产物，可删除；`out/` 不入 Git。
- `.claude/worktrees/` 是代理隔离目录，禁止加入补丁或提交。

---

## 一、当前状态

《地图格式转换工厂：首批 OpenDRIVE / SHP / V2X MAP 落地方案》**v1.1** 已定稿：v1.0 的 13 节框架不变，基于本地新资料完成重新规划（变更清单见方案文档头部 v1.1 变更记录）。

- 在线版（Artifact，可分享）：https://claude.ai/code/artifact/da8aaa79-5d69-4c80-9f3c-b275b68c7584
  - 更新方式：编辑本地文档后调用 Artifact 工具发布，**把上面 URL 作为 `url` 参数传入**（否则会新建独立 Artifact）。
- 本目录文件清单：

```text
F:\MapFactory\
├─ HANDOFF.md / CLAUDE.md                    # 交接 + Claude 指引（已含数据资产清单）
├─ OpenDRIVE_1.4H.xsd / OpenDRIVE_1.5M.xsd   # VIRES 官方 XSD（门禁资产）
├─ shp_0222-0326\                            # IBD 规格 SHP 真实交付（44 图层，重庆金凤）
├─ v2x_map_xml\                              # 7 路口 MAP XML + 消息层送审稿 docx + TCI asn + xml2xodr 参考代码
└─ docs\
   ├─ 地图格式转换工厂-首批三格式方案.md       # ★ 方案正文 v1.1（唯一权威版本）
   ├─ 资料盘点-金凤示范区数据与标准资料.md     # ★ 新资料消化结论（本地事实的权威来源）
   ├─ 调研报告-V2X-MAP标准与工具链.md
   ├─ 调研报告-OpenDRIVE生态.md
   └─ 调研报告-SHP高精地图与合规.md
```

## 二、v1.1 关键增量（速览，详见方案与资料盘点）

1. **M0 前置依赖解除**：消息层标准送审稿 docx 在手（五消息 ASN.1 代码块全）——"购买标准正文"改为 M0 第一工程作业"从 docx 提取拼装 .asn → pycrate 编译，用 TCI asn + 7 路口 XML 双向核对"。
2. **同目录 .asn 文件是 TCI 一致性测试控制协议**（TciMsgLayerFrame），不是消息层本体，不能直接编译出 MAP codec；它是 FusionTest 侧的一致性测试资产（已写入方案 9.5）。
3. **SHP Profile v1 落定 ibd-smarteditor-v1**：IBD 44 图层三层结构，含显式车道拓扑（LANE_TOPO_DETAIL）、路口内虚拟车道（LANE_LINK_MERGE）、交叉口面进出 Link 引用、停止线/信号灯/箭头的车道引用——SHP→MAP 关键环节从推断升级为字段直读。长度单位毫米；同名字段跨图层单位不一致等脏数据实录见资料盘点二.4。
4. **ID 台账清洗升为 M0 紧急作业**：7 个现网 XML 的 region 三种混用、双编号体系、交叉引用失配（实锤表格见资料盘点三.3）→ 产出 `ledger/jinfeng-2026.yaml` 初始台账，裁决规则需项目方确认。
5. **CRS 真伪核验 = M0 第一数据作业**：SHP 与 XML 都只是"声称 WGS84"（.prj 自定义 WKT 无 EPSG；xml2xodr 存在 GCJ02 纠偏开关）——核验前 crs_integrity=suspect 禁止进生产。
6. **MAP→OpenDRIVE 由 P2 上调 P1**：既有 xml2xodr"MAP 还原"链路证明业务需求真实（金凤+长沙两地），其产物无 junction/无高程/不可复现——mapforge 以完整 junction 直译替代。写出版本策略增补 1.5（本地消费端基线 1.4/1.5，XSD 已在仓库）。
7. **黄金测试集改以金凤 7 路口为基底**（真实 phase 绑定 + SHP↔MAP 同路口对拍），新增 ID 混乱、XML 方言容错两个场景；现网方言事实（绝对坐标分支、phaseId=0=右转无灯控、占位高程 500、位串带空白）已写入方案 1.2/5.4。
8. 仍开放的外部输入只剩：**配时表获取形态**（M1 需要）、**IBD 字段枚举权威文档**（值域反推可先行）、方案 13 节的部署形态/xodr 消费方/XML 生成方三问。

## 三、下一步待办（新会话从这里继续）

**v1.25 极小段假平滑已收口（2026-08-14，历史基线）**：v1.22 的中位段长门禁仍放过 0.667m 碎段，本轮改为最终 xodr 硬门禁：普通 leg 最短段 ≥3m、junction connecting road ≥1m；leg 同时要求来源偏差 ≤1.5m、|κ|≤0.04、|dκ/ds|≤0.0045、翻转≤8/100m。`fit_leg_refline` 只有全部条件同时通过才返回；无解抛 `ReflineFitError`，fallback 仅供诊断。SHP 拼链修正为比较真实连接端切向，候选自身绕街角则在完整 ROADLINK 边界停止；已删除静默裁源线的 `_trim_far_spikes`。车道写出新增 `mapforge.source_lane` provenance，并有按来源 lane 配对的保真/绑错故障测试。**当时验收：pytest 44/44 PASS；14/14 文件通过加强 G7、XSD、planView、G2、断面、换乘和 esmini；普通道路全局最短段约 3.2m。**该段的正式 G8 待办现已由本文件第〇节的 v1.26 闭环完成。

**GUI G0 会话补录（会话 `516d0bd7-6af0-4429-94d2-f011b6aa7a69`，GUI-01 已于 2026-08-14 收口）**：该会话生成的 `docs/GUI方案-mapforge控制台.md` 和 `docs/gui_g0_wireframes.html` 已完成后续结构评审，结论为“修改后接受”；五屏顺序、connect-mode 主区、源/产物常驻叠加和质量页同页拆层已裁决，线框升为 v0.2，并补齐 G8、运行/门禁/交付三轴、`full REVIEW_REQUIRED`、v1.25 G7 阈值与红线来源。在线 Artifact 的 403 不再阻塞本地评审。**GUI 实现仍未开始**；正式 G8 依赖已解除，GUI-02 仍须等待 B1–B4（统一 Job/Result、run 目录、结构化 gate JSON、统一预览入口）。

0. ~~参考线拟合 spike~~ **已完成（2026-08-13，结果见《Spike报告-参考线拟合验证》，方案已升 v1.5）**：A/B/C 全部通过——拟合路线验证成立（Town03 回拍类型零漏检、半径误差中位 6–15%；node16 端到端 XSD PASS + 连续性 0 违例；SolveG2 连接路可用；pyclothoids Windows wheel 直装成功）。**重大发现：IBD CURVATURE 字段核验不通过**（与几何不相关），曲率先验降级、字段核验升为强制门禁。已有代码资产：`mapforge/ops/refline_fit.py`、`mapforge/adapters/v2xmap/xml_reader.py`、`mapforge/validate/planview_check.py`、`.venv`（Python 3.10，正式环境换 3.11）。拟合器正式化待办：边界局部精修、段数最优化（Maier）、大半径假 arc 归直、spiral 档接入。
1. ~~M0 四项无依赖作业~~ **已完成（2026-08-13，《M0作业报告-ASN编译与数据核验》，方案升 v1.6）**：
   - ① ASN.1：163 定义编译 PASS + UPER 回环 PASS（`adapters/v2xmap/asn/msglayer-draft.asn` + 运行时 `msglayer_draft.py`；送审稿修复 5 处尾随逗号已留痕）；
   - ② 7 路口解码 + GeoJSON（`out/geojson/` ×7）；
   - ③ ID 台账 **已转正 `ledger/jinfeng-2026.yaml`**（2026-08-13 项目方裁决：`inherit-as-is`——编号沿用现网存量不重排、失配照抄；生成侧 `id_table_from_xml` 从现网 XML 继承 upstream/remote，交付包 upstream_ids_pending=0）；
   - ④ CRS 核验：**SHP↔MAP 同源同系（逐点重合），GCJ02 否定**，crs_integrity=internally-consistent；绝对校验（实测点）遗留 M1 前。
   - ⑤（追加完成）XML→ASN 全量值映射 + 7 路口整帧 UPER 回环全 PASS + 大小基线（绝对 678–1373 B / 偏移 483–911 B，省 32%，`out/uper_size_report.md`）；⑥ CLI 骨架 `python -m mapforge.cli`（preview/encode/decode/validate-xodr）。
   - ⑦（追加完成）xodr reader + junction 重塑（`adapters/opendrive/reader.py`、`ops/junction_to_map.py`、`ops/simplify.py`）：Town03 junction 422 端到端 → MAP UPER 回环 PASS（connectsTo 直译 18 条零丢弃）。
   - **（v1.7）M0 宣告完成（离线口径）**：mapforge 定位澄清为纯离线工具，"RSU 实机播发"移出工具里程碑、重定义为交付级可选验收（FusionTest 侧消费场景，方案 8.1⑤/9.1）；离线生态兼容证据 = 现网 7 路口 XML 对拍全等。
   - **M1 批量对拍已完成（《M1进展-SHP转MAP主线》）**：金凤 **7/7 路口** SHP→MAP 直读生成并与现网 XML 真回归对拍——横向中位的中位 **0.00 m**、车道数 6/7 完全一致、phase 5/7 全命中、平均覆盖率 78%、UPER 全回环 PASS。代码：`adapters/shp/ibd_reader.py` + `scripts/m1_shp_to_map.py`（批量版）。断链结论：TOPO 上游缺录（数据问题非算法）。
   - **转换矩阵已可用（统一入口 `python -m mapforge.cli convert`）**：MAP XML→geojson/uper/xodr、Town03(xodr)→MAP、IBD SHP→MAP（--like 参考 XML）全部实跑通过；新增 xml_writer（XER 写出，读写回环全等）与 ops/map_to_xodr。
   - **交付包已落地（`--to map` 即产方案 8.3 目录，report/deliver.py）**：三视图+quality/loss+id-mapping/diff+provenance+config+STATUS 共 11 文件；**红线实测生效**（无 phase → BLOCKED exit=2，--allow-no-phase 显式降级记 DROPPED）。样例 out/deliver/。
   - **工程化整备完成**：SHP 重塑与 GeoJSON 预览已入包（ops/shp_to_map、report/preview_geojson），CLI 自足不依赖 scripts/；拟合器加保真后处理（假 arc 归直/同类合并，refine 后禁并弧）；全量回归绿。已知限制：σ=5cm 压力组复合弧分段模糊（根治=Maier 最优分段，正式化项）。
   - **工程卫生完成**：pytest 26 项全绿（`pytest tests -q`，含金凤黄金回归门禁）；README 就位；**git 仓库已建**（main 分支，首次提交 24c2a9d，70 文件；.gitignore 排除 .venv/out/SHP 大数据/xml2xodr 参考代码）。
   - **转向补全模式 connect-mode（2026-08-14 第十五轮，用户"全连接/默认连接填满空隙，路口需要支持这种模式，因为地图本来可能就有问题"，方案升 v1.24）**：新增 `ops/junction_fill.py`（两管道 + CLI 共用）。三档：`data`（默认，仅源数据——"不发明拓扑"仍是默认立场）/ `default`（按车道位置补**应有而缺失**的转向；判"已有转向"用**几何分类**不用数据字段——字段正是可疑的那个）/ `full`（全连接：每条进口车道→每个非掉头出口腿的每条车道，治整片缺录 + 铺满路口）。补出连接标 INFERRED（`stats.conn_filled`）、走与数据连接同一套 G2 回旋链、受**曲率守卫**（|κ|>0.125 即 R<8m 判不可行，计 `conn_fill_skipped`）、掉头默认排除（`--allow-uturn`）。CLI：`--connect-mode data|default|full`。**实测 14 文件**：default 补 12/拒 7，full 补 759/拒 77，**门禁零降级**；esmini 抽检 4 文件全 PASS（96–116 对缝隙 0.0cm、零跳变）。pytest 37（新增 data 档 conn_filled==0 的行为门禁）。
   - **路口铺面材质与验收统一（2026-08-14 第十四轮，用户"补齐为啥不是路/验收和道路不一样"，方案升 v1.23）**：① **lane type 渲染逐类型隔离实测**（上一轮在拼图里目测，结论记反了——教训：颜色/材质这类判断必须单变量隔离+采像素，不能靠多目标拼图目测）：`driving/restricted/shoulder/parking/stop`=沥青(83,83,75)、**`none/border`=浅灰(125,125,113)**、`curb/sidewalk/biking`=混凝土(170,170,154)、`median`=绿化(70,139,88)。铺面原用 `none` → 浅灰补丁；改 **`restricted`**（沥青 + 规范语义"铺装不可行车"），路口内与进口道无缝同材质。② **铺面纳入门禁**：`audit_file` 断面台阶去掉"仅非 junction road"过滤，覆盖所有 road（铺面实测 0.3–0.7cm 达标）。③ 用户提议的"全连接/默认连接填空隙"**未采用**：多边形铺面已 100% 覆盖内部（out/preview/junction_close{,_m2x}.png 实证），而全连接会发明数据中不存在的转向、违反"不发明拓扑"硬约束；如需可行车全连接应设显式开关并标 INFERRED。七门禁 14/14 PASS、pytest 36 绿。
   - **段长与曲率蛇行治理（2026-08-14 第十三轮，用户质疑"极小段拼接会影响自动驾驶车辆"，方案升 v1.22）**：质疑成立——实测 SHP 侧中位段长 1.6–1.9m、50.3% 的段 <2m、node18 单路口 684 段。**先立判据再动手**：`smoothness.curvature_quality/curvature_audit`——段长只是辅助指标，真判据是 sharpness(dκ/ds) 变号率（方向盘微抖）与侧向 jerk v³·dκ/ds（leg 60km/h、conn 30km/h）。**三件核心改动**：① `simplify_planview` 曲率域精简 + κ 去噪（中值+均值，窗**从小到大够用即止**；目标先最少变号再最少段数；`_absorb_short_segs` 每次合并验偏差——**曲率图上直接删控制点会改 ∫κ ds 致航向漂移，实测偏差爆到 30m**）；② `weld_g2` 让 G2 成为**无条件承诺**（g2ify 失控分支残差 + 简化跳过零长阶跃 ⇒ 断差穿透 8e-3~3.3e-2，焊平后回 0.00e+00）；③ 连接路桥切口 3→8m（切口小把三段桥压成 1.6m 碎段）+ 最少段优先（3 段 G2 链偏差 ≤0.5m 就用）。**走不通并删除的路**：曲率剖面直接重建（离散曲率→RDP→clothoid + Kabsch 配准）最好只有 1.5–2.0m 偏差（曲率误差两次积分放大），需非线性最小二乘精修才可行——代码已删，不留死路。**成绩**：段数 3752→2089（−44%）、<1m 段 7.8%→**0.0%**、<2m 50.3%→3.1%、中位 2.00→4.20m；leg 蛇行 18–24→0–11.5/100m、leg jerk 667–1290→0–149。闭环升七门禁（G7 曲率品质）14/14 PASS、pytest 36 绿、视觉 14 张过。
   - **MAP 管道补齐到 SHP 同等（2026-08-14 第十二轮，"MAP XML 转 xodr 也要达到同等效果"，方案升 v1.21）**：`ops/map_to_xodr.py` leg 构建重写——站点网格多 laneSection（≈30m，共享站点 ⇒ 断面天然连续，8→34 段/路口）、实测轮廓跟踪（`_lane_profile` 投影 (s,d) + `_prof_eval` 局部回归 + `_bounds` 中点边界 + Hermite/FC 限幅，与 SHP 同机制；`fc_clamp` 移入 `refline_fit` 共享）、车道 speed 逐段落盘。**三个实测暴露的真问题**：① **车道存在性按点列覆盖判定**——MAP 各车道点列覆盖范围不同（进口 lane1/4 只覆盖近路口段=口部展宽；出口 lane1 只覆盖远端=下游加出车道），旧的"最近值外推"把远端车道摆进路口口部落进对向幅（重叠 2.9m）；改为覆盖外零宽（自然锥形收放）后降至 0.12m，识别 15 条展宽/加出车道，零宽端不写衔接。② 出口瞄准取 **written 链**（median 钳 0 时堆叠外移，否则错开一个车道宽）；口部零宽目标车道自动改瞄最近实存车道。③ **`simplify_planview` 后必须统一 G2**——精简/短段吸收留 8e-3~3.3e-2 κ 残差（旧代码只在精简失败时 G2 化 → SHP 侧 G3 全线回归）；用 `weld_g2` 焊平（不增段、护段长中位；g2ify 会把 node18 中位段长 3.9→2.9m 触发 G7），大阶跃仍走 g2ify 再焊。esmini 探针起点改用 `RM_GetLaneWidthByRoadId` 找车道真正存在的最小 s（零宽处起步会被归邻道，量到的是探针错误）。终态：七门禁 14/14 PASS、pytest 36 绿、视觉 14 张目检过。
   - **视觉全扫描 + FC 限幅（2026-08-14 第十一轮，/goal"多截图多检查不自以为是"，方案升 v1.20）**：`scripts/visual_sweep.py`（14 文件 × 4 机位 odrviewer 截帧拼图 out/preview/sweep/）纳入常规验收——数字门禁与逐张目检双过。目检结论：全部无空洞/锯齿/合成摆动；NODE5 双黄 S 弯 = 数据本形（源车道线交织借道段，node5_overlay*.png 叠画核实）；叠画所见"边缘钩子" = 叠画脚本索引拼接假象（车道数变化处不同物理边界被按序号硬连）——**自检工具也要被自检**。预防修复：轮廓 Hermite 全加 Fritsch–Carlson 单调限幅（`_fc`）。闭环 14/14 PASS、pytest 36 绿。
   - **实测轮廓横断面 + junction 铺面（2026-08-14 第十轮，用户"边缘不是道路形状/还有空洞/做到 SHP 的样子"，方案升 v1.19）**：① 横断面升级为**实测轮廓跟踪**——每车道全程 (s,d) 投影轮廓（`_span_recs` 存 ps/pd）、section 端点**局部线性回归**求值+斜率（`_prof_eval`；中位数在扇形段有一阶偏差）、边界=相邻实测中心中点（`_bounds_at`）、宽度/laneOffset/median 全 Hermite；`_reconcile` 交界调和 + `_emit` 写出层硬连续（续接车道起宽:=上段 written 末宽）。**实证排查链**（值得留档的调试史）：文件与内存记账矛盾 → Lane 对象打标（`_dbg`）对比 doc 与文件 → 从写出系数反解出真实多项式参数 → 定位 v1.18 衔接坡度上限误伤 born 锥形（w0=0 落进钳制分支，张开 3.35 被钳 0.93、记账 3.35 → 隐形 2.4m 台阶）——born 独立分支后**断面台阶全 0.000**。② **junction 铺面**：esmini 实测 outline object 不渲染（替身盒）→ 铺面 road 方案（`writer.add_paving_road`：多边形 PCA 主轴参考线 + type=none 车道逐站扫掠；SHP 用 IBD 交叉口面实测多边形 `stats.paving=polygon`、MAP 用连接路凸包+2.5m `hull` INFERRED；不入拓扑不寻路）。lane type 渲染实测：none/border=沥青、restricted=浅灰、median=绿化带（objtest 截图）。**闭环六门禁 14/14 全 PASS、pytest 36 绿**（路数断言计入铺面、_worst_seam_kappa_gap 跳过铺面路）。odrviewer 截帧 out/preview/odr_node4_paved{,_persp}.png——路口全铺装、边缘贴实测、无露底。shapely 已入环境（铺面切片用）。
   - **全网 G2 + 闭环跑分门禁全绿（2026-08-14 第九轮，/goal"全流程闭环+平滑达标"，方案升 v1.18）**：① `refline_fit.g2ify_planview`——结点开窗切除 + SolveG2（Bertolazzi–Frego）三段回旋线精确重连（位置/航向/曲率全匹配、下游零漂移 1e-10；短段整段吞并、右侧贪心跨段、双趟迭代），挂入 fit_leg_refline 与连接路中段/回退拟合；**v1.13 记档的 line-arc G1 设计点就此消除，14 文件 κ 结点断差全 0.00e+00**。② 桥"猪尾巴"守卫（node18 实锤 |κ|=1.8/R=0.55m 病态解）：桥内 |κ|>0.5 拒 + 切口 ×1/×2/×3 重试 + 次级纯 G2 合成（端点仍精确）。③ **`scripts/closed_loop.py` = /goal 验收闸门**：再生成 14 文件 × 六门禁（XSD/planView/κ<1e-6/台阶<5cm/换乘<1cm/esmini RM）**全部 PASS**；G2+台阶门禁锁入 pytest（36 绿）。odrviewer 截帧 out/preview/odr_node18_final.png（最难路口弯道全程顺滑）。复跑命令：`.venv/Scripts/python scripts/closed_loop.py`。
   - **对向幅连续化 + median C1 样条（2026-08-14 第八轮，用户"还不流畅+有空的"，方案升 v1.17）**：诊断手段升级——**headless odrviewer 截帧**（`odrviewer --headless --capture_screen --camera_mode top`，tga 用 pillow 转档）直接拿 esmini 自家渲染看用户所见；定位：北腿近路口细脖子/南腿轮廓鼓包 = 对向 span 覆盖开天窗。修复：① span 空档/重叠全部中点缝合；② 两端断面保持外推（远端到 0、路口端到 L，`left_extended_m` 记账 APPROXIMATED）；③ median 改**对向内边缘全程单调 C1 样条**（Hermite 逐段；关键：用边缘不用中心——数据链中心 d 与宽度在 span 界各自跳、边缘不跳）；④ leave 边界 6m 稀疏化（2m 微 section 会把锥形压成陡坡，node3 s103/105 实锤）。审计断面匹配先去重零宽车道重复边界（消幻影）。**终态 14/14 esmini 全 PASS 零跳变（node18 也过）**、断面台阶全 0、pytest 36 绿；odrviewer 顶视+透视截帧存 out/preview/odr_node4_after.png / odr_node4_persp.png（街道全宽连续、双黄+绿 median、车道线平顺、车辆正常行驶）。esmini 渲染知识：median 车道渲染为绿化带（非空洞）；密度车流沿车道行驶可当目检。
   - **双侧道路模型 + 自研 OpenDRIVE writer（2026-08-14 第七轮，用户裁决"严格按 OpenDRIVE 定义/scenariogeneration 不好用就不用"，方案升 v1.16）**：① 一条街一条 leg road（参考线沿进口幅、右侧进口 -1..-n、左侧对向出口 +1..+n 逆 s 行车、median 车道两端实测插值、中线双黄、出口连接路接 END 接触点；进出链端点位姿+方向自动配对，落单退化单侧）；② **scenariogeneration 弃用** → `adapters/opendrive/writer.py`（1.5 语义逐项对照 XSD；lane 子元素序/左侧 id 降序/rule=RHT；geoReference 落 PROJ 管线；lane speed/roadMark 原生；_strip/_inject 后处理废除）；共享几何工具移 `refline_fit`（planview_prims/seg_kappa/fit_leg_refline）。③ 消费级修复三件：**leg 曲率封顶 1/30**（node4-west 链 s255-298 实锤 R=10m 振荡噪声，双侧模型 1−tκ→0 车道中心驻点、esmini s 跳 80m；滑动平均渐进重拟合，偏差仍对原始顶点报——合帧 node4 dev 0.68 为修复代价，测试改锁 refit_smoothed≥1 & dev<1.0）；**衔接坡度上限 0.10**（written 末宽跨 section 传递）；对向拼链预算对齐进口参考线长（少留全幅漏斗）。④ 验证域双侧化：smoothness（_sections 含左侧、lane_edges_at(side)/cross_edges_at、route_continuity 支持 contact-end+正 id）、渲染器全断面、rm_check 出侧取 s=L、**车道锥形带豁免**（右灭边界前 22m/左生边界后 22m——探针骑收拢楔=并线非缺陷）。**终态：14/14 XSD+planView 全绿、esmini 换乘缝隙全 0.0cm（235 对）、行驶 13/14 零跳变**——唯一记档 node18 出口幅横断面重划分带（2×3.5→1.6+4.75，~12m 横摆 1.1 m/m，面连续）；pytest 36 绿（road 数公式改 links+conn_roads）。README 依赖行去 scenariogeneration 加 matplotlib。遗留：单侧回退路（30+）未做双侧/无 median；spikes/spike_b 仍引用 scenariogeneration（历史存档）；出口连接路 laneLink to 恒 -1（连接路自身车道）符合语义无需改。
   - **esmini 独立验证 14/14 PASS + 表面连续性修复（2026-08-14 第六轮，用户 odrviewer 截图"镂空+不平滑"驱动，方案升 v1.15）**：esmini v3.6.0 已就位 `esmini/`（用户自装，bin 含 odrviewer/esminiRMLib）。**镂空四类根因定位**（自研填充俯视渲染 `scripts/xodr_topdown.py` + 定位器 `scripts/xodr_diag.py`）：① 生/灭车道以全宽瞬现/瞬失（数据 Link 粒度，外缘 1.8–4.3m 矩形缺口，金凤 6 处大缺口）→ **20m 锥形收放**（`_width_pieces`，生从 0 张开/灭收拢到 0，smoothstep 两端零斜率）；② 断面重划分（node18: 2×3.5 → 1.6+4.75，外缘 0.65m 台阶）→ **匹配车道起宽衔接上段末宽**（sw_join，Σ宽边界连续）；③ 变宽线性折角（22 处，最大 11°）→ 全部 smoothstep 化；④ laneOffset Catmull-Rom 过冲 0.16m → **单调限幅**（Fritsch–Carlson）。附带：**默认标线**（中线实黄/车道间虚白/外缘实白，`_mark`，APPROXIMATED；连接路不加；`_inject_lane_speeds` 插入点改 width|roadMark 之后保 XSD 序）；**审计器升多段 width 求值**（`smoothness._sections/lane_edges_at`——旧读法只取第一段外推会报幻影缺口）。**esmini RM 独立验证**（`scripts/esmini_rm_check.py` 重写，对齐 v3.6.0 真实 ABI：id_t=uint32/double/出参取 lane id——旧 float 签名喂垃圾且对非法 lane id 会段错误）：14/14 PASS——换乘缝隙 esmini 自算全 0.0cm（235 对）、随机行驶零跳变、路口全穿越；两类非缺陷事件已归类为消费策略：junctionSelector"借道"（随机挑别车道的连接路横跳入场）与"灭车道并线点"（收拢归零无后继，探针不并线按同 ID 硬映射，SHP 侧 32 次；**不发明并线拓扑**）。剩余露底=中央分隔带/导流岛/junction 内部车道带间隙（模型语义非缺陷），查看加 `--ground_plane`：`.\esmini\bin\odrviewer.exe --odr out\direct_xodr\node4.xodr --density 2 --ground_plane`。批量再生 `scripts/gen_all.py`（14 文件）。pytest 36 全绿×2 轮。前后对比图 out/preview/node4_direct_{before,after}.png。
   - **换乘连续性构造性归零（2026-08-13 第五轮，回答"实际能否平滑接上/怎么验证"）**：① SHP 连接路两端加 **G2 桥**（`_bridged_geoms`：起点接进口车道、末端接出口车道的**文件语义堆叠位姿**——教训：桥必须瞄准 laneOffset+宽度累减出的堆叠中心而非实测偏移，仿真器消费的是前者；中段保留实测几何，桥失控 >40m 回退）；② laneOffset 升 **C1 三次样条**（Catmull-Rom 斜率，消断面交界斜率折点）；③ 新增 `validate/smoothness.route_continuity` **虚拟行车验证**（按文件语义走"进口车道中心→连接路→出口车道中心"，即任何仿真器的消费方式）——**金凤 7/7：换乘 A/B 两端跳变全 0.0cm、航向差 0.00°**，pytest 门禁锁 <1cm/<0.1°。XSD/连续性依旧全绿。esmini pip 无 wheel 装不上；用户侧独立验证命令：官方 esmini `odrviewer.exe --odr <file> --density 2` 或 esmini 场景放车沿路线行驶。SVG 预览脚本补断面交界采样（消渲染断口假象）。新增 `validate/smoothness.py` 三层审计（段间曲率跳变 Δκ / laneSection 边界车道台阶 / junction 接缝车道级位置闭合）。**审计抓出并修复三个真缺陷**：① spline 档在拼链接点近重复顶点上产生 κ=22 退化微回旋线（R≈4cm）→ 顶点 <3m 去重 + |κ|>0.15 整条熔断弃用；② SHP 进出口参考线短于车道（ROADCENTER 铺不到停止线）→ 按种子车道端点中位延伸；③ 濒死车道残宽台阶（node16 渐变到 1.16m 即断，右边界跳 1.15m）→ 无后继且末宽 <2m 的车道零收口（repair），**断面台阶全归 0**。种子断面改近路口 30% 段测车道偏移（口部对齐优先）。**量化并记档的表达极限**（非缺陷）：斜停止线（node18 各车道末端沿 s 错位 ±1.9m，单一 road 端面无法表达，连接路用真实几何吸收）+ 口部车道扇形分离 ≤1m（xodr 相邻堆叠模型限制）；line-arc 交界为 G1 设计点（Δκ 等效 R 4–9m 在路口口部转角，G2 化=插缓和段，正式化项）。最终：双向 7/7 XSD PASS、连续性 0 违例、断面台阶 0、MAP 接缝 G2 精确；SVG 俯视图产出 `out/preview/`。pytest 36 绿。
   - **两主线打磨完成（2026-08-13 第三轮，SHP→xodr / MAP→xodr）**：① **SHP→xodr 多 laneSection**——拼链改为同名跨车道数（此前遇车道数变化即停，产物含 8–54m 短桩路），每个源 Link 一个 laneSection：S_WIDTH→E_WIDTH 线性变宽（width a/b）、分段 laneOffset 线性过渡（lane0 边界连续）、相邻 section 车道一对一贪心匹配写跨段衔接（落单=生灭车道不写，消 scenariogeneration 覆写警告）。金凤实测：node4 进口路 217.7m/6 段 [2,2,3,4,4,4]（上游 2 车道到停止线 4 车道完整演化）；7 路口进出口路 laneSection 数百段、变宽车道数百条全落盘。② **车道限速注入**：SHP MAX_SPEED（km/h）与 MAP vehicleMaxSpeed（0.02 m/s 步长，833→60km/h）逐车道写 `<speed>`（`_inject_lane_speeds` 后处理，两管道共用）。③ **CLI 默认 SHP 源切 Profile 引擎**（ibd-smarteditor-v1）——实测其宽度阶梯从边界横距救回 6 条 WIDTH=0 脏数据车道（如 4272mm 真值），行为优于内置直读器；等价测试改为**超集契约**（结构全等+非宽度内容逐字节全等+宽度仅在内置缺数据处分歧）。④ 修 `_i` 小数容错（S_WIDTH='4272.0' 被 int(str) 吞 0）。真实数据验收：**SHP→xodr 7/7 与 MAP→xodr 7/7+合帧全部 XSD PASS、连续性 0 违例**；连接路长度分布正常（14–56m 无失控 U 转）。pytest 36 项全绿。
   - **MAP→xodr 真实出口优先 + 全接缝平滑（2026-08-13 第二轮）**：① 多节点 MAP 帧支持（`parse_map_xml_all`）——邻居节点中"上游=本节点"的 inLink 即本路口**真实出口路**（几何/车道全真），镜像降为无数据时的兜底（金凤合帧 node4+18+3 实测：真实 2 + 镜像 2，`--node-id` 选主节点）；② 平滑三件套：连接路起止改用**路模型端部位姿**（不再用原始点列末段航向，消停止线折角）、SolveG2 传入两端**车道级曲率**（κ/(1−tκ) 偏移修正）实现全接缝 G2（实测 16/16 曲率差 0.000000）、镜像只取近路口 120m+剔除偏移尖点；③ **拟合器新增稀疏链 spline 档**（`fit_sparse_g1_spline`，pyclothoids G1Hermite 顶点插值，正式化清单"spiral 档"落地）——修复附录 D 抽稀长弦点列上渐变曲率（缓和曲线）被误拟成直线+圆角链的问题（node17 north 顶点偏差 1.69→0.00），三档择优判据统一为**对原始顶点偏差**（弦上插值点不是测量，平滑曲线正确鼓出弦线不受罚）；resample 丢末点 bug 顺带修复。7/7 偏差峰值 0.13–0.31m 全 XSD PASS。pytest 升 36 项（合帧真实出口 + 接缝曲率 <1e-6 门禁）。
   - **MAP→xodr 的 junction 脑补已落地（2026-08-13，`ops/map_to_xodr.py` 重写）**：MAP 消息无路口内几何/出口路，junction 全部由消息自有信息推断（INFERRED 语义）——出口方向 = connectsTo remote 回查各进口 Link 的 upstreamNodeId（"驶向 X"="沿从 X 来的进口反向离开"）；出口路 = 对应进口 Link 左偏镜像（偏移=进口半幅+出口半幅）；连接路 = 进口车道末位姿→出口车道起位姿 G2 clothoid；进口路车道按点列实测放置（laneOffset+每车道真实宽）。**金凤 7/7：connectsTo 覆盖 95/96 条，XSD 全 PASS、连续性 0 违例；唯一 skip = NODE5 west→20701 已知 ID 失配引用（资料盘点三.3），设计裁决：失配诚实 skip 不得几何猜测编拓扑**。CLI `--connect` 手动通道退役。pytest 升 35 项（快测锁全覆盖+失配 skip 行为）。
   - **SHP Profile 引擎已实装（2026-08-13，`adapters/shp/profile_source.py`）**：一份 YAML 声明图商的图层/字段/单位/编码/几何路线，`convert --profile` 即用，内核零改动接新图商。双路线：A=车道中心线+宽度字段；B=边界线+左右关系（中线合成 `midline_of` + 边界横距推宽）。宽度阶梯 field→boundaries→spacing→default（非 field 记 APPROXIMATED，`derivation_stats` 计数随 CLI 输出）。可选层全可缺省降级：无 junction 层→`--at`+端点聚类（REVIEW）、无 topo→连接为空、无 road 层→按车道 road 字段合成归组、无中心线→中间车道替代。CLI 三件套：`profile-init`（注释模板）/`profile-check`（图层字段体检+宽度量级+降级预告）/`convert --profile`。**证据**：`ibd-smarteditor-v1.yaml` 与内置 IbdSource 产物去时间戳逐字节全等；`ibd-boundary-demo.yaml`（屏蔽 WIDTH 全走边界推导）恒宽车道误差中位 14mm、离群 2 条=口部展宽变宽车道（数据真相）。pytest 升 32 项。仍开放：字段别名词典/自动推断向导（待第二家真实交付）。
   - **SHP→xodr 直转已修正落地（2026-08-13，`ops/shp_to_xodr.py`）**：纠正架构违规——CLI 曾把 SHP→xodr 借道 SHP→MAP→xodr（空口消息窄门，产物无 junction 的孤立 road）。直转要点：ROADCENTER 中心线同名拼链 ~160m + 偏差驱动升级档拟合（>0.3m 换细档，5° 微折角→line-arc-line）、laneOffset 按车道实测横向位置（IBD 中心线在本幅中间，±1.75/±5.25 对称跨线）、每车道真实宽度、完整 `<junction>` + 连接路（TOPO 两跳中间车道实测几何；**路口内连接车道不在 MERGE 层**，在普通层路口内 Link 上）+ Connection/laneLink + 路级/车道级前驱后继。**金凤 7/7：213 条连接路全实测几何（G2 合成 0），XSD 1.5M 全 PASS、连续性 0 违例、拟合偏差峰值 0.29–0.38m**。CLI 新增 `--at lon,lat` 定位（摆脱对参考 XML 依赖）；pytest 升 27 项（test_shp_to_xodr 锁结构底线）。新文档《接入指南-新图商SHP数据需求与Profile》（图商字段差异四步流程 + 数据需求分层表 + 最小可转集）。遗留简化：S_WIDTH/E_WIDTH 变宽、高程（SLOPE 未核验）、多 laneSection、标线 roadMark。
   - **M1 继续**：Link 中线正式化（车道组中线）、出度策略开关、配时表模板替代演示通道、MapIR pydantic 数据类合流、M0 近似项正式化清单：Link 上游回溯拼接、Link 中线（参考线→车道组中线）、多 laneSection/laneOffset、remote ID 接 ledger、MapIR pydantic 数据类合流、拟合器正式化（边界精修/段数最优化/假 arc 归直/spiral 档）。
2. **方案继续细化清单**（v1.0 遗留，未变）：MapIR v0.1 pydantic schema、CLI 命令面（mapforge convert/validate/preview/diff/profile-wizard）、M0 工作包人日分解、profiles/shp YAML 规范文档、SHP→MAP 与 OpenDRIVE→MAP 流水线伪代码。
3. **SHP 字段映射三件套**（v1.0 遗留）：~~字段级校验报告~~（已实装=`profile-check`）；仍开放：字段别名词典 / Profile 自动推断向导——ibd-smarteditor-v1 是第一个词典输入源（含 10 字符截断实例 STOPLINE_R、单位不一致实例 CENTRE_X），待第二家真实交付后再抽象。
4. 向项目方提问清单（已答 2 项）：~~ID 裁决~~（inherit-as-is 已落地）；~~phase 缺失处理~~（按设计：有来源即绑、无则 BLOCKED/显式降级，金凤场景现网 XML 即来源）；仍开放：配时表形态（新路口用）、部署形态、xodr 消费方、XML 生成方、J2735 出口需求、IBD 枚举文档。

## 四、与原项目（FusionTest）的关联（不变 + 一条新增）

- FusionTest（F:\1\next_FusionTest\FusionTest，Python+Zenoh MEC 测试框架，活跃线 origin/feature/20260429）消费本工厂交付包做 RSU 实机播发 HIL 验证；其 MEC 感知消息 `map_location` 引用 MAP 的 region/node/lane ID。
- feature 分支 `report/renderers/precision_map_renderer.py` 可复用为 MAP GeoJSON 底图渲染参考。
- **新增**：`v2x_map_xml/` 下的 TCI 一致性测试控制协议 asn（v1.0.1）是 FusionTest 可消费的设备测试协议资产，建议移交测试框架侧评估。
- 工程边界不变：mapforge 独立仓库，FusionTest 只消费交付包。

## 2026-08-21 v1.29 增量：SHP 真实物理外边界闭环

- SHP→OpenDRIVE 已改为读取 `LANE_BOUNDARY_REL` 关联的真实边界，使用连续线段投影、10m 横断面站、外缘锚定和共享边界栈，不再把“中心线±字段宽度”冒充来源边缘。
- 车道生灭收口导致的道路表面缺口由 provenance=`physical-edge-fill` 的非驾驶 shoulder 补齐；不改变驾驶车道 ID、中心、拓扑及 junction 连接。
- node4 最终双向外缘误差：SHP→xodr 中位 0.144m / P95 0.440m / 最大 1.403m；xodr→SHP 中位 0.142m / P95 0.363m / 最大 1.266m。
- 最终门禁：pytest 71/71；7 SHP + 7 MAP 共 14 文件 G1–G9 全 PASS；esmini 全部加载/穿越 PASS，换乘缝隙 0.0cm、跳变 0。证据为 `out/direct_xodr/node4_shp_xodr_overlay_final.{png,json}` 与 `out/closed-loop-report.json`。
- 技术几何闭环已完成；生产交付仍因金凤绝对 CRS 未核验而 BLOCKED，该阻断与平滑度/道路外缘无关。
