# 共享断面自由度、联合求解与全局重构路线复核

> 2026-09-10 · 对应总方案 v1.45。整体目标未完成，默认转换器未推广，MAP 未重转，所有试验仍 BLOCKED。

## 1. 本轮不是只查论文

已核对当前模型并实现 `mapforge/ops/shared_section.py`：用共享断面方向 q 替代接头两侧独立零斜率。固定参考线时仍保持原有 n+2 横断面区间，不通过加小段提高拟合度；n−1 个共享 q 提供横向形状自由度。零 q 与旧模型数值等价。

证明与工程路线见 [全局重建与约束式交互修形方案](全局重建与约束式交互修形方案.md)。该文件包含论文、统一源身份/三张图/共享口部、离散模型选择与连续联动求解、正宽约束、局部编辑事务、离线 Web 技术选型和完成标准。GUI 方案升为 v0.5，替换旧“不支持任何几何修形”的边界；**Web 代码未实现**。

## 2. 冻结输入与试验范围

父文件 `out/shp-joint-review-v144/node4.xodr`：

```text
ff0613ef8443ab36063363494c3ac13ab2bf91e1820c64ba202a42220cad327a
```

真实 SHP via 原始中心和两侧边界均进入约束；相邻支持按原始 TOPO 拼入并与 via 分开统计。源点不裁，源文件、ID、TOPO、普通道路和文件速度均未改。此处仍固定普通道路口部状态；**不是已经完成整张路网的共享端变量优化**。

新增 `spikes/shared_section_candidate.py`：先固定参考作 A/B，再用 `--joint-reference` 联合参考参数和 q。有源预热结果只作初值；构造条件包含 Bernstein 正宽界、来源和动力学；最终由另一个脚本读回 XODR 检查，不采信优化器 success 字段。

## 3. 固定参考 A/B：负宽可消除，但外观贴合不等于驾驶平滑

前半轮冻结 `out/shp-shared-section-v145/node4.xodr`，SHA `78d084aa3e0594324b9a46dad14bf0aa856ee7cf6d7f78dbe81637df783106f7`，完整对照在其 `review/`。该批选择来源误差较小的正宽诊断值，后半轮已改成失败候选优先比较可行性残差；不要把这份历史对照当当前接受策略。

| 连接 road | 原始 via 三曲线最大偏差 | 精确最小宽度 | 15km/h，2cm 复算最大 jerk | 独立结论 |
|---|---:|---:|---:|---|
| 104 | 0.407034m | 2.873260m | 29.898040m/s³ | FAIL |
| 122 | 0.344260m | 3.105045m | 5.249642m/s³ | FAIL |
| 108 | 0.219623m | 3.396476m | 0.088119m/s³ | 局部 PASS |

104 输入原有负宽 -2.811628m 在该诊断中消失，图上自交和严重外翻消失；但动力学更差，明确不作为可交付改善。三者来源与接头均通过，不能拿这些 PASS 替代动力学。图已亲自查看。

## 4. 联合参考与横断面：122 局部通过，104 仍失败

联合探索批 `out/shp-shared-section-joint-v145/node4.xodr`：

- 104 当前端状态/少段模型内，150 次迭代未找到满足全部条件的解。记录了几何、来源、动力学及闭合残差，不能宣称全局无解；没有移端点假装闭合。
- 122 收敛到带 2% 动力学构造余量的候选，最小构造余量约 `-2.69e-9`，闭合最大残差 `4.81e-9`。首轮程序以精确 `>=0` 判断数值结果，把它错标 REJECTED；修为仅允许 `1e-8` 构造舍入误差。**最终来源与动力学门槛不变**，仍独立重算；不是将实际 jerk 上限从 1 放宽。
- 参数可重放且校验父 XODR SHA、参考结构、参数数量和范围，重放不重新优化，也不直接修改历史结果 JSON。

最终仅替换 122 的隔离文件 `out/shp-shared-section-final-v145/node4.xodr`：

```text
abf59437b504c8b3fd6faa8cb439632906b7a5b7538a00b48bf6ad477709b9c9
```

该目录中的 final 仅指本次单连接选择，不是产品验收完成。其他 road 保留 v1.44 输入，包括仍负宽的旧 104；未把固定参考的诊断几何写回正式或该最终文件。

| 项目 | 122 独立读回结果 |
|---|---|
| 原始 via 中心/两侧边界最大偏差 | 0.245168m；完整支持域双向指标及 raw via 单列均 PASS |
| 精确最小宽度 | 3.343351m |
| 最大横向加速度 / jerk | 1.165049m/s² / 0.982006m/s³，按 15km/h 附加设计档、2cm 复算 |
| 内部两侧世界边缘与两端中心/边缘 | PASS；条带 0.05m 采样无自交，非解析全域证明 |
| 参考段长度 | 6.405762 / 12.303789 / 6.000000 / 6.023527 / 6.000000m，共5段 |
| 横断面 | 7条 width/offset 记录，最短3m；不能称所有记录都长或已满足用户最终非碎段要求 |
| XSD 1.5M | PASS |
| esmini 144项全路口中心/两侧接口 | PASS；最大位差0.004455m、航向差0.001534°、曲率差2.56e-10/m |
| 整图 G11 | A/B/C/E PASS，D FAIL；不是整图通过 |

esmini 仍打印不支持 geoReference 的 `+R`、`+lat_ts`、`+no_defs`，本次仅证明局部 XY 消费接口。连接路文件已有较低限速，15km/h是附加检查，不代表来源60km/h的道路/转向速度语义已解决；未降速过关。

## 5. 视觉检查

已查看：前半轮3条源叠图、最终122源叠图、最终完整范围/口部俯视/斜视 esmini 截图，均绑定上述最终 SHA。另修 `review_measured_ribbon.py` 单图标题超出画布的问题。

- 122 蓝色新边缘明显贴近黑色来源，旧红色偏离消减。
- 最终整图仍有西侧间隙、路口蓝缝及外缘小台阶，斜视仍可见；不能因为 122 通过就忽略。
- 辅助铺面可能遮住连接内部缺陷，必须同时看未填充的来源/边缘叠合；本轮没有宣布铺面适用于自动驾驶。

工件位于 `out/shp-shared-section-final-v145/review/`：`all-connectors.png`、`review.json`、`whole.png`、`junction.png`、`oblique.png`、`esmini-three-views.png`、`esmini-captures.json`。

## 6. 复跑

以下命令输出须使用不存在的新目录，防覆盖历史证据。试验入口非零退出是 BLOCKED，不代表允许正式交付。

```powershell
.\.venv\Scripts\python.exe spikes/shared_section_candidate.py `
  out/shp-joint-review-v144/node4.xodr out/shp-shared-section-replay-v145/node4.xodr `
  --road 122 --joint-reference `
  --replay-report out/shp-shared-section-joint-v145/node4.ribbon.json

.\.venv\Scripts\python.exe scripts/esmini_lane_interfaces.py `
  out/shp-shared-section-replay-v145/node4.xodr --edges

.\.venv\Scripts\python.exe scripts/capture_geometry_review.py `
  out/shp-shared-section-replay-v145/node4.xodr out/shp-shared-section-replay-v145/review

.\.venv\Scripts\python.exe -m pytest tests/test_shared_section.py tests/test_connector_cross_section.py `
  tests/test_measured_connector_caps.py tests/test_joint_source_mouth.py `
  tests/test_road_boundary_family.py tests/test_g11.py -q
```

69项相关回归通过/9.61s，compileall、git diff --check 通过（现有 CRLF 提示）；非全仓、非14图回归。无 commit，正式输出和源原件未覆盖。

## 7. 下一步不是继续逐条改数字

按新方案 R1：将 SharedPort 从“普通路先解后冻结的字典”改为真正共享变量；将该口普通物理边界、全部依赖连接、参考轴纳入同一约束图，先在 node4 同口范围证明闭包，再验证完整普通路/铺面。保留当前原型作为可重放对照，不反复盲移结点、不加微段。

Web 端可以并行开发只读源加载、EditIntent/revision/事务基础，但真实拖动和导出必须调用同一内核；不做会动却不能正确导出的小演示。MAP 的 node4/17 及 node18 原始异常仍未解决，不继承 SHP 单连接结论。未标记目标完成。
