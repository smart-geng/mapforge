# 2026 年 8 月 2 日—8 月 14 日工作日报

## 整理口径

- 依据以下 5 个项目在 2026 年 8 月 2 日至 8 月 14 日的 Git 提交、测试记录和关键文件修改时间整理：
  - `F:\sensors_replay_tool`
  - `F:\test`
  - `F:\yolo`
  - `F:\XYZ_Robotic_Arm_and_adb_Cockpit_Testing_System`
  - `F:\项目文档编写`
- 按要求，周末不单独形成日报：8 月 2 日并入 8 月 3 日，8 月 8—9 日并入 8 月 10 日。仓库中未发现上述周末日期的独立提交，因此不额外虚构周末事项。
- 各项目开发密度不同，日报按实际工作日期归并，重点记录已完成事项和可核验结果。

---

## 8 月 3 日（星期一，含 8 月 2 日）

1. 完成机械臂加速度参数的分轴调优并将推荐档位收敛到 500，机械臂运行漂移降低一个量级，连续运行后的回零周期由约 34 次提升到数百次。
2. 补齐机械臂速度调优与 Z 轴方向保护测试，更新机械臂 SDK 参数、标定参数和调参工具，为后续真机稳定性测试建立可复现基线。
3. 修复设备体检工具中的陈旧导入问题和 ADB 状态误判问题，消除健康检查中的假红结果。

**完成情况：** 机械臂稳定性调优方案已落地，核心参数、测试用例和设备检查工具同步更新。

## 8 月 4 日（星期二）

1. 优化机械臂回零机制，引入单一里程账本、案尾异步回零和多轴并发回零，XYZ 回零耗时由约 15 秒降至约 6.96 秒，XZ 回零耗时由约 10.98 秒降至约 6.82 秒。
2. 增加跨服务重启的漂移遥测基线、回零超时分层和并发超时保护，修复回零在途被误判为断电或未回零的问题。
3. 减少机械臂触屏期间约 400 毫秒的软件额外开销，避免普通点击被车机识别成长按。
4. 完成预算管理系统编辑器体验优化：按财务口径重述核心指标，支持面板拖拽缩放、批量折叠，并将项目完成后的人工成本改为按年录入。

**完成情况：** 机械臂回零速度、稳定性和触控时延均得到改善；预算编辑器的财务展示和录入效率同步提升。

## 8 月 5 日（星期三）

1. 修复 CAN 同帧多发送器争用问题，将同一帧请求改为按位叠加，并在除霜用例断言期间摘除同帧心跳，真机连续验证达到 7/7 稳定通过。
2. 推进相机视觉链路整改：切回 DirectShow 后端以稳定曝光，增加黑帧护栏，并在标定页面显式展示平场拟合状态，避免平场失败后静默关闭色度归一。
3. 调整屏幕亮度和相机增益工作点，指示灯识别由 0/12 提升到 11/12；同时将电池故障断言素材由手册示意图替换为真机素材。
4. 编制 SnowLeopard V5.9.0 八月绩效目标及任务清单，明确当月阶段任务和交付范围。
5. 对预算系统导出流程开展多轮联调验收，生成不同场景的 Excel 导出件并核对结果。

**完成情况：** CAN 通道争用和相机基础采集问题得到控制，视觉识别进入可持续标定阶段；V5.9.0 月度工作目标完成拆解。

## 8 月 6 日（星期四）

1. 完成相机颜色状态与平场标定链路重构：将平场模型改为格点场，建立开机仪式和颜色指纹门，并将采样定位改由 ADB 真值裁决、相机仅负责输出色度。
2. 重建颜色基线与色心库，收紧指纹门阈值，修复形状门在真机数据上的误放行问题，使色度判定由形状兜底恢复为真实颜色判定。
3. 完成预算管理系统研发费用九大类改造，统一委外费用、摊销率和第九类费用口径，并同步更新前后端数据结构、计算逻辑和迁移测试。
4. 生成财务统一模型 V2.0 模板，实现导出件保留原模板公式链；补齐旧版本按当前口径重新计算、超区间费用归首年和导出结果复核机制。
5. 新增一键发布脚本并固化数据库备份、镜像依赖、强制重建和回归数据清理流程，修复备份目标和 `pipefail` 场景下的校验误判。

**完成情况：** 相机标定链路具备状态约束和可追溯基线；预算系统九大类模型、V2.0 模板及安全发布流程完成闭环。

## 8 月 7 日（星期五）

1. 完成相机移位后的整链重标，更新机械臂标定、颜色 Profile、颜色状态和单应矩阵，指示灯颜色族连续两轮达到 12/12，通过双轮复现验证。
2. 优化视觉等待与颜色竞争逻辑，取消单帧“洗白”即失败的判定方式，改为轮询至超时；颜色竞争范围仅限已完成 Profile 标定的颜色族。
3. 为预算系统导出链路增加公式注入、岗位名净化和超长文本等对抗性测试，修复 Excel 单元格上限静默截断、公式注入和回填错位问题。
4. 修复编辑器未保存改动判断逻辑，改为按业务内容比较而非 JSON 文本比较；完成 v1.0.0 版本发布及运维手册更新。
5. 上线盈亏平衡智能分析，提供敏感性分析、三情景测算和关键洞察，并修复助手在首字输出前停止时留下空消息的问题。
6. 完成 SnowLeopard V5.9.0 软件测试计划 V1.0.1 的整理与归档。

**完成情况：** 台架视觉基线达到稳定复现；预算系统完成 v1.0.0 发布，导出安全性和经营分析能力同步增强。

## 8 月 10 日（星期一，含 8 月 8—9 日）

1. 完成传感器分布式回放可信启动控制面，实现内容根、就绪租约和提交确认机制，并修复分布式冒烟套件中的失效门禁与波动用例。
2. 统一传感器回放场景配置和数据目录布局，支持真实数据清单配置、时间线缓存新鲜度校验，以及 PCAP、JSON UDP 等通道的回放时间偏移证据。
3. 修复视频循环重启偏移下溢、非法帧率计算和 JSON UDP 循环接缝丢帧问题；新增 12 路视频加 4 路激光、12 路视频加 2 路激光及四激光等压力场景。
4. 增加跨主机 UDP 到达探针和 RTSP 交付验证，区分“发送端已发送”和“接收端已收到”，并将真实链路、长稳测试结果补入生产运行手册。
5. 为预算系统增加 AI 盈亏情景设计、项目立项结论简报和利润归因能力，形成“AI 提假设、计算引擎出数字”的可审计流程。
6. 完成座舱测试工作台动作白名单补齐、运行状态修正和 AWB 重锁；新增音频电平诊断、语音天气问询用例，并将空调语音用例判据改为车机 CAN 请求，连续验证达到 15/15。
7. 完成 SnowLeopard V5.9.0 方案设计 V1.0.1 和测试用例 V1.0.1 的整理、模板回填与归档。

**完成情况：** 传感器回放完成可信启动、统一场景和真实链路验证的集中收口；预算 AI 分析、座舱语音测试及 V5.9.0 设计与用例文档同步推进。

## 8 月 11 日（星期二）

1. 增加传感器回放 20 路视频加 8 路激光的现场最大规模场景，记录实测容量边界，并明确单机回放与分布式回放的选型条件。
2. 补充循环模式下 `evidence_ok=false` 的原因和使用边界，避免将预期行为误判为回放缺陷。
3. 完成预算系统数值字段负数校验和修改预览金额识别，规范固定资产分类与名称字段。
4. 完成助手对外文案脱敏，避免暴露底层模型和供应商标识；补齐 Chat 风格接口的工具调用循环，确保切换模型后仍能执行试算工具。
5. 将座舱测试 5 条交付用例归入已验证金样分类，补齐仅提供 `case_id` 时的加载解析能力。
6. 完成 SnowLeopard V5.9.0 第一轮测试结果回填和测试报告整理。

**完成情况：** 传感器回放容量边界与架构选型完成量化；预算助手兼容性、隐私口径和座舱交付用例管理进一步完善。

## 8 月 12 日（星期三）

1. 完成多目标检测离线评测工具主体开发，支持时间映射与分段交集、WGS84 坐标处理、二维旋转框、距离/IoU 匹配、全局一对一目标分配和 ROI 内外独立 Precision/Recall 统计。
2. 建立可审计结果包，输出 JSONL、JSON、Excel、HTML、日志、输入输出哈希和逐帧 BEV 证据图；通过 `RUNNING/COMPLETE/FAILED/CANCELLED` 状态文件避免未完成结果被误认为成功。
3. 完成真实数据验证：VG3D 样本 Precision 95.0000%、Recall 90.3236%，1,524 张证据图和 31,059 条逐目标明细均完整生成，计数守恒与文件哈希校验无异常。
4. 完成证据图多进程优化和全量验收：12,356 张证据图处理速度达到 77.99 帧/秒，400 帧端到端加速 2.864 倍，13 项自动化测试全部通过。
5. 生成 Windows x64 免安装便携版，集成评测 GUI、ROI 绘制工具、两套预测数据、真值、时间映射、相机标定和文件完整性校验，目标电脑无需预装 Python。
6. 完成座舱测试流程控制 v3：支持 cleanup 必执行段、两档停止和步骤属性；工作台加入标准动作词汇表和复合动作库，并修复工作流/并行引擎吞掉停止信号的问题。
7. 完成工作台全链闭环测试，修复 10 个问题，其中包括 3 个假绿；5 条交付用例在三种通道下均达到 5/5，通过批量判定格和状态归口校验。

**完成情况：** 多目标检测评测工具从方案、实现、性能验证到便携交付完成闭环；座舱测试工作台流程控制与判定可靠性同步收口。

## 8 月 13 日（星期四）

1. 优化预算系统全周期预览，补齐各项比率展示并避免多产品场景显示无意义的混合单价；指标行增加计算公式提示。
2. 重做预算系统品牌顶栏和项目工作台，修复列表翻页问题，并将恒为“草稿”的状态列替换为真实可变化的“已存版本”；同步调整首页缓存策略。
3. 完成座舱测试 AI 移植与视觉定位专项：建立统一考题和定真工具，扩充 grounding 评测集，完成多模型同卷横评、分辨率影响测试和默认模型稳定性验证。
4. 将 `vlm_locate` 接入 MCP 工具链，加入默认断电、外发审计、结果回贴和受控启用机制；完成多轮真机定位验证，并将定位能力接入素材备料流程。
5. 清理 ROI 与模板资产债务，统一语义化命名，增加悬空 ROI、垃圾命名、被引用素材未入库和空用例假绿等质量门禁；5 条交付用例补齐 ROI 并完成 ADB 复证。
6. 优化标准动作与流程块设计，收敛核心动作词汇，增加并行执行组入口和流程块 v4 树形容器；修复逐步 CAN 场景提前过期问题。
7. 完成传感器可信回放相关专利交底书、申请文件及配图生成脚本整理。
8. 编制 SnowLeopard V5.9.0 阶段一里程碑报告、测试报告和版本发布报告，并整理配套自动生成脚本。

**完成情况：** 预算系统界面与指标解释进一步完善；座舱测试 AI 定位、素材治理和流程设计形成体系化质量门；专利与阶段里程碑文档完成集中整理。

## 8 月 14 日（星期五）

1. 将传感器回放此前未纳入的 4 组测试接入统一门禁，并增加时间线缓存陈旧保护；完成 44 步全系统测试门禁、四激光接收配置和运行指南整理。
2. 补充传感器回放项目的场景目录、工具输出、CRLF 构建注意事项及会话交接文档，形成可交接的部署和验收说明。
3. 提升预算系统边界健壮性：非法实体编号等边界输入不再触发 500，空行快照和加载时预览重算不再误报冲突。
4. 修复预算系统“复制项目”只建空壳、不复制数据的问题，并增加内存库、PostgreSQL 和前端联动测试；同步更新 OpenAPI、验收清单和系统测试交接文档。
5. 完成座舱系统全面系统测试，修复长红门禁、屏幕依赖负对照、设备接口心跳、日志无界增长和超时误报等 5 类假绿/假红问题。
6. 将逐动作烟测由 15/22 提升到 22/22；补齐“运行整案”入口和实时步骤进度，优化 `listen` 断言成功早退，语音用例耗时由 81 秒降至 56 秒，ADB 基线达到 6/6。
7. 完成座舱测试系统交接文档和系统测试报告，统一流程块“路”术语、运行入口和逐步 CAN 使用说明。

**完成情况：** 三个主要系统均完成阶段性质量收口：传感器回放形成 44 步门禁，预算系统完成边界与复制项目缺陷修复，座舱系统动作烟测达到 22/22 并完成交接。

---

## 阶段总结

本阶段工作覆盖传感器可信回放、预算管理与 AI 辅助分析、多目标检测评测、机械臂与座舱自动化测试，以及 SnowLeopard V5.9.0 文档交付五条主线。阶段成果主要包括：

1. 传感器回放完成可信启动控制、统一场景配置、真实链路验证、容量测试和 44 步全系统门禁。
2. 预算系统完成研发费用九大类、公式链导出、安全发布、盈亏智能分析、助手工具调用、界面升级及系统边界修复。
3. 多目标检测评测工具完成从算法口径、GUI、审计报告、多进程优化到免安装便携版的完整交付。
4. 机械臂与座舱测试系统完成机械臂稳定性、相机颜色标定、工作台流程控制、AI 视觉定位、素材治理和全系统测试闭环。
5. SnowLeopard V5.9.0 完成绩效目标、测试计划、方案设计、测试用例、测试报告、版本发布报告和阶段里程碑报告等文档建设。
