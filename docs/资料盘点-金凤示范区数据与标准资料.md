# 资料盘点：金凤示范区数据与标准资料

> 盘点日期：2026-08-13。对 `F:\MapFactory\` 下三块新资料的消化结论，供方案 v1.1 引用。
> 事实来源：DBF/SHP 头部解析、pandoc 提取 docx、逐文件代码走读。结论仅陈述文件中实际看到的内容。

### 2026-09-11 v1.58 MAP宽度字段与原SHP候选补记

重读消息层送审稿DOCX（SHA `eb209e5593bdbc48bee3c9aa52f588a62ba9ad0bd085ae5ec8965c7680432b6f`）：DF_Lane的laneWidth为可选；DE_LaneWidth定义车道宽度、1cm分辨率、0..32767，所核定义未明确全程常宽/名义值/站位语义。

node13 south lane3原点1（索引从0）字段350cm，距唯一0.06m内SHP候选`2023061416264724852`中心约0.0000833m；原S_WIDTH/E_WIDTH为5650/6700mm，关联两边界局部法向截宽6.351509m，点位于边界间。NODE5 north lane2原点0距候选`2023061315010051235`中心约0.001190m，该记录S_WIDTH/E_WIDTH=0/2350mm；另有相邻候选。此点截线缺交点，**没有测得0或2.35m局部净宽**。

这些是几何/原TOPO候选而非获批身份绑定。`out/map-source-contract-v158-final-{NODE5-north,node13-south}/report.json`保留全部候选/原件快照/166项hash；9/19原点及7/16相邻点对均有至少一路局部TOPO候选，不等于唯一匹配。没有改MAP、借用SHP作隐藏转换输入或批准“按名义宽推断”。[完整限制与证据](复核补充-联合初始化与MAP宽度语义证据-2026-09-11.md)。

### 2026-09-11 v1.49原路口TOPO与区间核对补记

原`IBD_OBJECT_INTERSECTION_SURFACE`第5行（从1计）PID `2023041113170861627`，名称“金玥路-凤苑路”；ENTER/LEAVE各4个Link，普通层分别16/8条车道。按原始TOPO、Profile显式IS_JUNC=1内路分类独立遍历，得到48条边、24条“进口→内链→出口”，四进口各6条。随后与v1.45旧junction1的24条逐路径核对，漏连/多连/重复均0；这是通行关系复核，不是几何正确。

当前road10准备域有47原路径、71个独立边界ID、118个part/3009原顶点。完整via和两条北侧出口支持保持原件；在旧Line法平面/原身份section切口下，仍有15.7846729m累计源线未分配，其中路径6.52171594m、边界9.26295697m，最大独立片段1.2655565m，涉及48个要素。这些是不同线要素弧长累加，不是道路缺失长度、横距或面洞，不能反推原件错误。

证据`out/joint-source-domain-v149-final/source-domain.json`保留原TOPO行、路口行、内路分类行、原路径/边界part与每顶点站位；`interval-review.png`标出未分配区间，不是新XODR截图。原路口范围及分配核对见[实施计划](实施计划-整路联动重建与编辑闭环.md)。以下v1.48的“尚未独立枚举”仅是当时状态。

### 2026-09-11 v1.48历史：整路联动准备包原件身份补记

`out/joint-reconstruction-input-v148-final/`直接读取38个文件/配置形成快照：road10与12条既有连接及固定对端涉及47个源车道、71个BORDER_PID、23个共享边界ID，47个源MAX_SPEED均60km/h。普通层和MERGE层有11个跨层同ID，但点列范围不同，不作为可直接合并的重复几何；当前IBD Profile明确普通层优先，聚合表示完整另存。该规则是读取策略，不是对原件准确性的裁决。

106/107/114/115/122/123 的源via与当前road10结构口部之间分别还有`2023081515293628015`或`2023081515293622479`，原TOPO无分支链已纳入准备包。当前12条只是旧XODR现有连接闭包，尚未从源路口独立证明无漏连，源区间也尚未分配给普通路/连接；不能据此宣称全来源覆盖完成。[实施与复跑](实施计划-整路联动重建与编辑闭环.md)。

### 2026-09-11 node4北侧完整原路径与限速补记

按原SHP的明确TOPO及走向核对三条路径：road10/right−1对应4个LANE_LINK、联合去重共端后9点；left+3对应3个/6点；right−3对应4个/7点。涉及的11个原LANE_LINK的`MAX_SPEED`均为60km/h，与试验输入XODR一致。完整原几何没有裁到旧road长度或重采样后冒充原点。

原件hash、要素快照和逐路径核对在`out/sparse-path-v147-reviewed-final/report.json`及其引用的试验报告；[本轮模型与结果](复核补充-完整源路径少原语与直线保护-2026-09-11.md)。这些只证明原始字段/路径身份相符，不证明它们与物理边界中点等价，不认定原件错误，也不授权改限速或坐标。

### 2026-09-10 真实出口范围与node18孤立原点补记

全原件读取：node4西侧可由node18东侧真实车道支撑延长10.592m；node18南側可由node17北侧支撑延长91.434m。邻居来源点比本路口Link长或只记了远端，不等于近端车道不存在。此次没有把真实出口退回镜像或修改身份。

原文件`map凤苑路-金剑路node18.xml` SHA256 `51380de2a39551d3842d06f43075b8a0f14aea3c94b0db5aa0d055e0d016c4df`，north/from(500,20)/Lane1第二个原点：longitude_e7=1063346865、latitude_e7=295139806。前后邻点间49.955m，此点距邻点弦1719.940m，其他邻车道未同样远跳。只标记REVIEW_REQUIRED、原值保留，不自动修正。完整来源G8因此失败；旧支持域裁剪漏掉此冲突的PASS不可沿用。见`out/map-raw-geometry-review-v140.json`、[出口与坐标轴复核](复核补充-MAP真实出口覆盖与坐标轴联合候选-2026-09-10.md)。

### 2026-09-10 NODE5 Link与Lane几何语义补记

逐数组核对`map凤苑路-金坪路NODE5.xml`：south/west/north/east四个Link.points分别与对应Lane2.points完全相同，并非两条车道中心的平均线。Lane1在北/南方向比Link长得多，不据此认为原件错误或车道出生位置已知。v1.39候选已补回完整来源：8车道34顶点全部保留，独立原始顶点最大距离0.473m。旧正式七份MAP清单只读复核还有5份存在裁剪/改变（含邻居真实出口），详见`out/map-source-inventory-audit-v139.json`及[完整原件复核](复核补充-MAP完整原件覆盖与长参考轴-2026-09-10.md)。

### 2026-09-10 NODE5原始Lane长于目标道路补记（旧v1.38）

按原始`map凤苑路-金坪路NODE5.xml`完整身份读取Lane.points：北向from(500,20702) lane1共6点，南向from(500,21501) lane1共7点。对`out/map-joint-review-v138/NODE5.xodr`的同身份最终车道复核，分别4/5个顶点距目标>1m，最远198.768762m/161.421480m；原始点图显现主要是纵向截断。旧manifest来源支持域比这两条原始Lane短，不能代表完整原件。图、每点局部坐标和距离见`out/preview/map-joint-review-v138/NODE5-source.{png,json}`。不据此说原始数据错误；应先修道路长度建模/完整覆盖门禁。

### 2026-09-10 node3连接字段相等现象补记

只读`map含金路-金玥路node3.xml`（SHA256 `ce123cd4acf67bb85825b6b22b8bb2937e9dc0b1de7717fda74982db977395a5`）可见四个Link各lane2/3的连接remote与所属Link.upstream相同，共8条，maneuver原值均`100000000000`。当前按remote回查upstream方向的实现会映射到本leg并生成回转路径。本记只确认原始字段和当前实现行为，不认定源数据或语义解析谁错，不授权改ID或自动换成对侧。其余六份没有该字段相等现象。逐条记录见`out/map-raw-connection-semantics-v138.json`，脚本`map_connection_semantics.py`可复跑；见[v1.38补充](复核补充-路口断面联合求解与三份MAP验证-2026-09-10.md)。

### 2026-09-10 MAP身份与参考点检查补记

按XML实际解析：node13文件主节点为region=21901/node=21901，node16文件为region=500/node=21801，文件名数字不是现场节点ID。局部叠图现从manifest main上下文取完整身份，缺失报错。四个本轮MAP候选的原始Link点数为node3=19、NODE5=13、node13=19、node16=13；不再把误找ID后的空集视为零误差。Link.points与Lane.points分别按方向参考中心、车道中心核查，见[本轮补充](复核补充-MAP车道端状态与少段候选-2026-09-10.md)。

### 2026-09-10 渐变车道中心与边界不等价补记

只读脚本`source_lane_consistency.py`复核node18 road13所关联的23条源车道。源`2023061915005392888`（Link `2023061914592874873`，SEQ=3）S_WIDTH=5200mm、E_WIDTH=0；两条关联边界零宽端重合，但LANE_LINK末点与相邻SEQ=2车道末点相同，距边界尖端约1.883m。途中点与边界中点相差约0.986m。这证明该渐变段的车道点列与边界等距中线不等价，不证明哪一图层错误，也不授权自动改原件或跳过保真验收。证据`out/preview/source-semantics/node18-road13.{png,json}`，详见[本次复核](复核补充-世界曲率接缝与整路联合求解-2026-09-10.md)。

### 2026-09-10 局部实物层核对补记

只读脚本 `scripts/source_surface_evidence.py` 核对路口±70m窗口，结果及完整字段在 `out/preview/source-objects/{node4,node13}.{png,json}`。node4找到35个ROAD_SURFACE对象；node13找到37个ROAD_SURFACE、5个DIVERSION、3个TRAFFIC_BARRIER对象。两个窗口均未找到有效ISOLATION_BELTS/CURB对象，不能据此断言现实中不存在设施。

node13南侧两出口组间隙附近有护栏线 `2023061511154388846`，局部x约-6.89～-6.81m，y约-70～-20.20m；TYPE=0、SIDE=0、HEIGHT=0、IS_BLOCKED=2仅按原值记录，枚举意义未擅自解释。这个约1m间隙不能简单按缺车道宽度填成driving lane。详见[复核补充](复核补充-口部铺面与全车道动力学-2026-09-10.md)。

## 一、资料清单总览

| 位置 | 内容 | 对项目的意义 |
|---|---|---|
| `shp_0222-0326/` | IBD 规格车道级高精地图 SHP 交付（44 图层，重庆金凤片区约 8.5km×11km） | **SHP Profile v1 的定义样例**（方案原第一外部依赖，已解除） |
| `v2x_map_xml/map*.xml` | 7 个真实路口的 V2X MAP 消息 XML 明文（凤苑路/凤阁路/含金路 × 金剑路/金坪路/金玥路） | **黄金测试集原料 + 存量 node ID 台账来源** |
| `v2x_map_xml/*.asn` | 《基于LTE的车联网无线通信技术 消息层协议一致性测试控制消息-ASN_1源码 v1.0.1》 | **TCI 一致性测试控制协议**（非消息层本体，详见三.2）：核对参照 + FusionTest 侧资产 |
| `v2x_map_xml/*.docx` | 《基于LTE的车联网无线通信技术 消息层技术要求》送审/报批稿（标准号 YD/T XXXX 占位） | **消息层标准正文**：五消息 + 全部数据帧/数据元素定义与释义 |
| `v2x_map_xml/xml2xodr/` | 既有 MAP XML→OpenDRIVE 转换代码（多版本迭代，2023） | 单位换算/坐标处理经验 + 反面教训（详见四） |
| `OpenDRIVE_1.4H.xsd` / `OpenDRIVE_1.5M.xsd` | VIRES 官方 XSD（2015/2019） | 本地工具链 OpenDRIVE 基线为 1.4/1.5 的证据；XSD 门禁资产 |

**关键交叉事实（2026-08-13 已核验升级）：SHP 与 7 个 MAP XML 不止同区域，而是同源**——三路口叠合核验 H0（同坐标系）全胜（中位距离 0.00 m），node4/node17 的 MAP 点与 SHP 车道线**逐点重合（max 1 cm）**：金凤 MAP XML 就是从这套 IBD 交付生成的；GCJ02 错配假设被数据否定，crs_integrity=internally-consistent（绝对真伪的实测点校验遗留）。→ SHP↔MAP 对拍是**真回归**（存在存量转换先例），生成工具是谁待问项目方。详见《M0作业报告-ASN编译与数据核验》④。

## 二、SHP 交付：IBD 规格解剖

### 2.1 版本与坐标

- `IBD_VERSION`：EDIT_VER=**IBD_V1.0.0**，APP_VER=**smarteditor**（编辑工具），COORD_SYS=**84**（声称 WGS84），COORD_UNIT=2（度），CREATETIME=2023-11-15。
- `.prj` 全部为**自定义 WKT**：`GEOGCS["My geographic coordinate system", DATUM["D_WGS_1984", SPHEROID["My_WGS84_Spheroid",...]]]`——无 EPSG 码，工具随手生成的痕迹。**声称 WGS84 但未证实**（结合 xml2xodr 存在 GCJ02 纠偏开关，坐标真伪必须实测核验后才能进生产，见方案 4.3 CRS 硬约束）。
- 几何全部带 Z（PolyLineZ/PointZ），高程 242–320 m（重庆合理范围，疑似正常高，垂直基准未声明）。
- DBF LDID=0x57（ANSI），实际中文内容按 GBK 解码正常（ROADNAME="高新大道辅路"、NAME="高新大道-白鹭大道"）。

### 2.2 单位与 ID 约定

- 长度/宽度单位**毫米**（WIDTH=3500 → 3.5 m；LENGTH=155071 → 155 m；MARK_WIDTH=200 → 0.2 m）；速度 **km/h**（MAX_SPEED=40/60）；角度**度**（HEADING 0–360）；曲率 1/m、坡度/超高带 4–6 位小数。
- 主键 `*_PID`：19 位数字串，前 8 位是编辑日期（如 2023041211102754016）；另带 GUID 列；MESH 为图幅号。
- 关系表借壳空几何 PolyLineZ 存纯属性（bbox 全 0），是"用 shp 装关系表"的惯用做法。

### 2.3 图层结构（三层 44 图层）

**A. 道路级**（Link-Node 骨架）：

| 图层 | 关键字段 | 说明 |
|---|---|---|
| IBD_ROADLINK（5356 条） | LINK_PID、S_NODE_PID/E_NODE_PID、KIND、SEPARATION、DIRECT、LANE_NUM、LEFT_NUM、ROADNAME、ACCESS_CAR、LENGTH | 道路段 + 起止节点 + 车道数 + 路名 |
| IBD_ROADLINK_NODE（4285） | NODE_PID、MESH | 道路节点 |
| IBD_ROADLINK_TEMPLATE（13673） | LINK_PID、LANE_NUM、SEQ_NUM、WIDTH | 断面模板（每 Link 分段的车道数/宽度） |
| IBD_ROADCENTER（4412） | CENTER_PID、LINK_PID、S/E_MID_PID、**IS_JUNC** | 道路中心线（IS_JUNC 标记路口内段） |
| IBD_POSITION（208030 点） | LINK_PID、SEQ_NUM、HEADING、CURVATURE、SLOPE、BANKING | **道路中心线逐形点姿态**（航向/曲率/坡度/超高） |
| IBD_LINK_FORM / IBD_NODE_FORM / IBD_NODE_CURVATURE / IBD_NODE_SLOPE | ROAD_FORM、NODE_FORM、节点处 IN/OUT 曲率坡度 | 形态与节点姿态补充 |

**B. 车道级**（车道中心线 + 边界 + 拓扑）：

| 图层 | 关键字段 | 说明 |
|---|---|---|
| IBD_LANE_LINK（10889） | LANE_PID、LINK_PID、**S_LANE_PID/E_LANE_PID**、SEQ_NUM、LANE_DIR、LANE_TYPE、WIDTH、S_WIDTH/E_WIDTH、MAX/MIN_SPEED、LENGTH | 车道中心线：所属 Link、车道序号、宽度（含起止变宽）、限速、**前后接续** |
| IBD_LANE_LINK_MERGE（1582） | 同上 + **S_JUN_PID/E_JUN_PID** | **路口内虚拟车道**（进/出 junction 引用）——MAP connectsTo 的直接对应物 |
| IBD_LANE_TOPO_DETAIL（12010） | **IN_PID → OUT_PID** | **显式车道级接续拓扑表** |
| IBD_LANE_NODE（9766） | NODE_PID | 车道节点 |
| IBD_LANE_POSITION（519684 点） | LANE_PID(列名 LINK_PID)、SEQ_NUM、HEADING、CURVATURE、SLOPE、BANKING | **车道中心线逐形点姿态** |
| IBD_LANE_LINK_ACCESS（10262） | LANE_PID、ACCESS_CAR（位图，如 1032）、VALID_TIME | 车道通行权限 |
| IBD_LANE_BOUNDARY（15301） | BORDER_PID、START/END_PID、BORDERTYPE、MARK_COUNT、TRAVERSAL、ADJACENCY | 车道边界线 |
| IBD_LANE_BOUNDARY_REL（21778） | LANE_PID、BORDER_PID、SIDE | 车道↔边界左右关系 |
| IBD_LINK_BOUNDARY_REL / IBD_ROADCENTER_BOUNDARY_REL | LINK/CENTER_PID、BORDER_PID、SEQ/DIRECT/SIDE | Link/中心线↔边界关系 |
| IBD_MARKLINK（12171） | BORDER_PID、MARK_TYPE、MARK_COLOR、MARK_WIDTH、MATERIAL、LEFT/RIGHT_FLAG | 边界线的标线属性（虚实/颜色/宽度/材质） |

**C. 对象层**（30 个 IBD_OBJECT_* 图层，均含 OBJECT_PID/GUID/MESH/ROAD_PID）：

- **交叉口面 INTERSECTION_SURFACE（163）**：TYPE、FORM、**NAME（路口名）**、**ENTER_ROAD/LEAVE_ROAD（分号分隔的 Link 引用列表）** → 交叉口发现免推断。
- **停止线 STOPLINE（415）**：WIDTH、COLOR、STOP_TYPE、**LANE_REL（车道引用）** → 附录 D"末点=停止线中心"判据可直接实现。
- **信号灯 TRAFFIC_LIGHTS（673）**：CENTRE_X/Y/Z、DIRECTION、ROW/COLUMN_NUM、HEADING、**CROSS_REL（路口引用）、LANE_REL（车道引用列表）** → 灯↔车道硬件关联现成（注意：这不是 phaseId，配时绑定仍需外部输入）。
- **地面箭头 ARROW（4151）**：CENTRE_X/Y/Z、LENGTH、WIDTH、**DIRECTION（转向类型）、LANE_REL** → maneuvers 的主要数据源。
- **人行横道 ZEBRA（479）**：CROSS_REL、STOPLINE_R（注意：**字段名被 DBF 10 字符截断**，应为 STOPLINE_REL）。
- 其余：斑马线前自行车道 CROSS_BIKE、导流区 DIVERSION、路面 ROAD_SURFACE、护栏 TRAFFIC_BARRIER、路缘石 CURB、隔离带、墙、标志牌 TRAFFIC_SIGN（含 TYPE 编码）、文字 TEXT、符号 SYMBOL、龙门架 GANTRY、情报板 MESSAGE_SIGN/GUIDING_PANEL、杆 ZQ_POLE（1950）、路灯 STREETLIGHT（3390）、相机 CAMERA、电警 ELECTRONIC_POLICE_FLASH、流量检测 FLOW_DETECTOR、公交站 BUSSTOP/BUS_STATION、停车位 PARKING、隧道墙警示等。
- 对象↔路网关系表：OBJECT_LANE_LINK_REL（对象↔车道，TYPE 编码）、OBJECT_LINK_REL（对象↔Link）。

### 2.4 对转换流水线的直接影响

1. **SHP→MAP 的 connectsTo 不再依赖几何推断**：LANE_TOPO_DETAIL（IN→OUT）+ LANE_LINK_MERGE（S_JUN/E_JUN）给出显式车道级拓扑 → 转换状态从 INFERRED 升级为 EXACT/TRANSFORMED，人工复核压力大幅下降（几何推断降级为缺失兜底）。
2. **交叉口发现免推断**：INTERSECTION_SURFACE 的 ENTER_ROAD/LEAVE_ROAD 直接给出进出 Link 集合。
3. **maneuvers 数据源**：ARROW.DIRECTION（车道级地面箭头）为主，路口内虚拟车道几何方位角为辅。
4. **SHP→OpenDRIVE 拟合数据源**：LANE_POSITION/POSITION 是独立密集采样点层（~1 m 间距，PointZ 自带几何，非 LANE_LINK 形点属性），点密度优秀。**（2026-08-13 Spike-C 核验）逐点姿态字段可信度分化：HEADING 与几何航向吻合（北向、度）可用；CURVATURE 与几何曲率整体不相关（150 条车道批量核验，相关系数中位 -0.005，仅 2/150 条 |corr|>0.5；典型矛盾：字段记 R≈60–128 m 弯而几何为直线）不可作先验，语义待向交付方确认；SLOPE/BANKING 未核验、按同等怀疑对待。**
5. **真实脏数据清单（Profile 引擎必须容忍）**：
   - UPDOWN_DIR 字段数值溢出成 `****`（DBF N 字段写坏）；
   - 同名字段单位不一致：CENTRE_X/Y 在 ARROW/SYMBOL/TEXT 是投影米（如 629033, 3267021），在 TRAFFIC_LIGHTS 是经纬度（106.33, 29.51）——**字段语义必须按图层配置，值域探测不可省**；
   - 字段名 10 字符截断（STOPLINE_R、PROVINCE_L/R）；
   - 字段宽度全部虚标（C,100/150/254），DBF 以空格填充，文件因此虚胖（IBD_POSITION.dbf 470MB）；
   - 关系表借壳空几何；.prj 自定义 WKT 无 EPSG；
   - **字段内容与字段名不符的整层案例**：LANE_POSITION.CURVATURE 与实际几何曲率不相关（见上第 4 条）——字段级核验（值域探测 + 与几何交叉验证）必须是 Profile 引擎的强制步骤，不能信任字段名。

## 三、V2X MAP 标准资料

### 3.1 消息层技术要求 docx（标准正文）

- 封面标准号为 **YD/T XXXX—XXXX 占位**（送审/报批稿），前言引用 YD/T 3400-2018、YD/T 3340-2018——即后来发布的 YD/T 3709《基于LTE的车联网无线通信技术 消息层技术要求》的定稿前文本。
- 内容：消息帧-消息体-数据帧-数据元素四层定义，UPER 编码；**MessageFrame ::= CHOICE {bsmFrame, mapFrame, rsmFrame, spatFrame, rsiFrame, ...}** 五消息齐全；每个数据帧/数据元素带【定义】释义与【ASN.1代码】。
- MAP 关键定义与方案 2.3 节假设一致：MapData{msgCnt, timeStamp OPTIONAL, nodes NodeList}；DF_Movement 明文："相位ID事实上也是MAP消息与SPAT消息的唯一关联"（Movement{remoteIntersection NodeReferenceID, phaseId PhaseID OPTIONAL}）。
- **对 M0 的影响：方案 v1.0 唯一前置依赖"购买标准正文提取 ASN.1"已被此资料解除**（正式发布前仍建议购正式版 YD/T 3709-2020 做差异核对，送审稿与发布稿可能有细节出入）。

### 3.2 ASN.1 源码文件（v1.0.1）——是 TCI 控制协议，不是消息层本体

- 模块名 `TciMsgLayerFrame`（AUTOMATIC TAGS，无 OID/版权声明，1008 行）。顶层 `TciMsgLayer{version(=2), msgNo, msgFrame TciMsgFrame}`，`TciMsgFrame ::= CHOICE {request, response, indication, exceptionInfo, ...}`——**测试系统(TS)↔被测设备(DUT)的一致性测试控制协议**：7 种控制消息（SetInitialState / Start·StopMsgLayerTx / Start·StopMsgLayerRx / SetBsmInfo / SetRsmInfo）+ 收包上报（eBsmRx/eMapRx/eRsmRx/eSpatRx/eRsiRx）。
- **文件中不存在 MessageFrame/MapData/NodeList/Link/Lane/Movement 的完整定义**——MAP 只有概要上报类型（BasicMapNodesInfo/BasicMapLinkInfo/BasicMapLaneInfo/RxMapInfo）。→ **不能直接用它编译出 MAP 编解码器**。
- 但它完整定义了一批与消息层同名同构的子类型，与 7 个 XML 逐字段吻合，可作核对参照：`NodeReferenceID{region OPTIONAL, id}`、`Connection{remoteIntersection, connectingLane OPTIONAL, phaseId OPTIONAL}`、`ConnectingLane{lane, maneuver OPTIONAL}`、`AllowedManeuvers BIT STRING(SIZE(12))`（位 0-11：straight/left/right/uTurn/leftTurnOnRed/rightTurnOnRed/laneChange/noStopping/yieldAllwaysRequired/goWithHalt/caution/reserved，**无扩展标记**）、`SpeedLimitList(1..9) OF RegulatorySpeedLimit{type, speed}`（13 种限速类型带 `...`）、`PositionOffsetLL ::= CHOICE` 7 分支（position-LL1..LL6 偏移档位，注释 LSB=0.1 微度，±22.6m 至 ±92.8km + position-LatLon 绝对分支）、`VerticalOffset` 7 分支。
- 关键取值约束（可直接进 MapIR 校验规则）：Latitude -9e8..9e8+1（1e-7°）、Longitude -1799999999..1800000001、Elevation -4096..61439（0.1 m）、NodeID/RoadRegulatorID 0..65535、LaneID 0..255、PhaseID 0..255、LaneWidth 0..32767（1 cm）、TimeMark 0..36001（0.1 s）。
- 注释大量出现"参考《基于LTE的车联网无线通信技术 消息层技术要求》中 DF_xxx/DE_xxx"——**字段语义外包给同目录 docx**，两份资料是配套关系。
- 对项目的定位：① 消息层子类型核对参照与约束表来源；② **FusionTest 侧资产**（设备消息层一致性测试的控制协议，正是测试框架可消费的东西）；③ 不承担 MAP codec 编译职责。
- **结论：消息层本体 ASN.1 的获取路径 = 从 docx 的【ASN.1代码】块逐段提取拼装 + 用 TCI 子类型与 7 个 XML 实例双向核对**——这成为 M0 的第一个工程作业（替代原"购买标准正文"前置依赖）。

### 3.3 7 个路口 MAP XML

- **标准 MessageFrame 的 XER 风格实例**（逐字段元素化，枚举=空元素、BIT STRING=01 串、CHOICE=分支名元素；无属性、无命名空间）。结构：MessageFrame > mapFrame > {msgCnt, timeStamp, nodes > Node ×1}；Node > {name, id{region,id}, refPos, inLinks > Link ×4（name 固定 west/north/east/south）}；Link > {upstreamNodeId, linkWidth, points, movements > Movement{remoteIntersection, phaseId} ×3, lanes > Lane ×2-4}；Lane > {laneID, laneWidth, maneuvers, connectsTo > Connection{remoteIntersection, connectingLane{lane,maneuver}, phaseId}, [speedLimits], points}。量级：每文件 1 Node / 4 Link / 8-16 Lane / 44-113 RoadPoint。
- msgCnt/timeStamp 各文件不同（18-120 / 203493-203588，后者是 MinuteOfTheYear 量级）→ **更像从实际播发流导出的单帧快照**，而非手写模板。
- **坐标：全部走 position-LatLon 绝对经纬度分支（1e-7° 整数），7 个文件无一使用偏移编码分支**——现网实践未用 offset 压缩（消息偏大），mapforge 的偏移编码输出反而是增量价值；同时 v2xmap reader 必须兼容"绝对坐标点列"方言。
- **高程：仅 node13 有，且恒为 500（=50 m，与当地实际海拔 242-320 m 不符，显然是占位值）**——现网 MAP 基本无真实高程。
- **ID 体系混乱（实锤，直接决定台账清洗优先级）**：

| 文件 | region | Node id | 文件名一致? | 邻居 id |
|---|---|---|---|---|
| 含金路-金玥路node3 | **3** | 3（name=node_c） | id 一致 | 1, 2, 4 |
| 凤苑路-金玥路node4 | 500 | 4 | 一致 | 3, 9, 18, 401 |
| 凤苑路-金坪路NODE5 | 500 | 5 | 一致 | 21501, 20603, 20702, 20703 |
| 凤阁路-金玥路node13 | **21901** | 21901 | **不一致** | 3, 21803, 21903, 22003 |
| 凤阁路-金剑路node16 | 500 | **21801** | **不一致** | 21204, 21301, 21803, 22001 |
| 含金路-金剑路node17 | 500 | 17 | 一致 | 15, 16, 18, 22 |
| 凤苑路-金剑路node18 | 500 | 18 | 一致 | 4, 17, 20, 23 |

  region 三种取值混用（500=重庆区划前缀 / 21901 / 3）；小场地号（1-23）与 5 位码（2xxxx）两套编号并存甚至同文件混用；**交叉引用失配**：node17 引用邻居 (500,16)，node16 文件自称 (500,21801)；node13 引用 (21901,3)，node3 自称 (3,3)。→ 这批数据进黄金测试集前必须先做 **ID 台账清洗**（唯一化 + 引用修复 + 登记为 `ledger/` 初始台账）。
- **phaseId 现网样例**（7 文件取值集合完全相同 {0,1,2,9,10,17,18,25,26}）：每进口两个相位、编号间隔 8（north 1/2、east 9/10、south 17/18、west 25/26）；**右转一律 phaseId=0**（"不受灯控"的现网表达习惯）；左转用每对第一个、直行用第二个。node3 整个缺 movements 层但 Connection 仍带 phaseId。→ 黄金测试集自带真实 phase 绑定样例；"phaseId=0 表示无灯控"应写入 v2xmap Profile 的方言语义。
- speedLimits 仅 node3/4/18 三个文件有（Lane 级，vehicleMaxSpeed，speed=833 按 0.02 m/s 即 60 km/h）。
- 方言容错点：maneuvers 的 01 串值两侧带换行缩进空白（解析需 strip）；movements 层可整体缺失；refPos 字段名 lat/long（XER 下的标准命名）。

## 四、xml2xodr 参考代码评估

既有内部代码（2023，无版本管理/测试/README，按文件名日期迭代），实现 MAP XML→OpenDRIVE 1.5。

### 4.1 值得吸收

1. **MAP XML 字段与单位换算表**（v2xmap adapter 对照物）：坐标 1e-7 度、laneWidth/linkWidth **厘米**、速度 **0.02 m/s**、高程**分米**；Node→Link(upstreamNodeId)→Lane(connectsTo/maneuvers 12 位位串/phaseId)→RoadPoint 解析层级完整。
2. **局部切面投影选型**：以路口 refPos 为原点 `+proj=sterea +ellps=GRS80` 平面投影 + geoReference 写 PROJ 串（方向正确；mapforge 应改为规范 CRS 管线记录而非 lxml 后处理注入——其 modify_header 会追加出重复 geoReference，已有脏输出实例）。
3. **GCJ02/BD09 纠偏开关（location_type）的存在本身**：证明现场 MAP 数据确实可能是火星坐标——"CRS 可疑即停"硬约束的实证依据。
4. **车道对齐重建思路**：以最长车道为虚拟参考线投影重采样对齐各车道点列，逐段实测相邻车道间距得到起止变宽（road_info.py 的 align_lanes + reference_line_tuple_with_width）——车道级宽度重建的可用雏形。
5. **反面教训**："每两点一条 road"方案导致接缝补丁链（缩段 20–30% + AdjustablePlanview 过渡 + 手工 pred/succ 串联）——反向论证必须做整条参考线连续拟合。

### 4.2 必须抛弃/重写

- 逐段 Line 几何（无曲率连续性）；高程解析后丢弃（xodr elevationProfile 恒空）；
- **connectsTo/movement/phaseId 解析了但从未消费——没有任何脚本生成 junction/laneLink**（所有输出 road 均 junction="-1"）；xodr 目录里带 junction 的 a_intersection*.xodr 是 scenariogeneration 官方示例的手工试验残留，无对应脚本；
- maps-0605.py（最新版）依赖本地魔改库私有函数 `create_road_geng`，仓库不自包含、不可复现；
- 手写 GCJ02 近似互转脚本（网络流传版）；字符串拼接式 XML 序列化；入口路径/魔数硬编码（+0.4 m 车道加宽、road_id 拼号会撞号）；裸 except 吞错；限速解析后未消费。

### 4.3 结论

**这条内部链路证明"MAP→OpenDRIVE"有真实业务需求**（把现网 MAP 变成仿真可用地图），但现状产物是"无路口、无高程、不可复现"的骨架——mapforge 的 MAP→xodr 方向（方案原 P2）应上调优先级，并以"junction 完整生成 + 高程保留 + 连续参考线"为对既有链路的替代价值。

## 五、OpenDRIVE XSD

- `OpenDRIVE_1.4H.xsd`：VIRES 官方 Rev. 1.4H（©2015），`OpenDRIVE_1.5M.xsd`：Rev. 1.5M（©2019），均注明 excluding SET records。
- 与 xml2xodr 输出 revMinor=5 相互印证：**本地既有工具链的 OpenDRIVE 基线是 1.4/1.5**。
- 影响：方案写出版本策略需覆盖 1.5（见方案 v1.1 第 1.1 节修订）；两份 XSD 直接纳入 `validate/` 的 XSD 门禁资产。

## 六、遗留问题（消化后仍开放）

1. ~~SHP 与 MAP XML 坐标真伪~~（2026-08-13 部分解决）：**同源同系已证**（H0 全胜、GCJ02 否定，生产内互转无错配风险）；遗留"绝对为真 WGS84"的实测控制点/权威影像校验（M1 前）。
2. MAP XML 的生成方（RSU 配置平台？厂商工具导出？）与现网 XML→UPER 编码器是什么——已确认消费方是 xml2xodr（"MAP 还原"用途，长沙观沙岭场地也用过同套代码）；生成方待问项目组。
3. 配时表（phaseId 权威数据源）仍无着落——7 个 XML 自带现网 phase 绑定结果可作回归基线，但**新路口生成仍需配时表输入**（FORBIDDEN_AUTO 不变）。
4. IBD 规格正式文档（LANE_TYPE/BORDERTYPE/KIND/ACCESS_CAR 等枚举值权威定义）——当前只能从数据反推，向交付方索取。
5. 消息层 docx 为送审稿（标准号占位）：与正式发布的 YD/T 3709-2020 可能存在细节差异——发布级交付前购正式版做一次差异核对（开发可先行）。
6. TCI ASN v1.0.1 对应的一致性测试标准版本号（YD/T 375x 系列？）——文件无标注，待核。

## 七、2026-09-10分合流原件复核补充

- node4北側：原SHP `2023081117282865661` 末宽4350mm，TOPO同时到 `2023081117251325687`（起宽4350mm）和 `2023081117251327605`（起宽0mm）。后者是零宽渐宽支路；不能把源0当空值，更不能按更近的路径中心把前者全宽车道错误收为零宽。两reader现保留端宽是否显式提供的标记。
- node4西侧MAP Lane2原始点索引4→5，虽然各点在某条SHP中心的约5mm内，但跨了不同SHP车道链，原TOPO局部前向无路径。属于两份原件的一致性疑点，未裁定哪份正确、未改点/ID。不是“坐标同系”结论的反证，也不能以坐标重合推断语义完全同源。
- 原始数据、本次图和逐点证据、转换修复边界见[分合流复核报告](复核补充-源车道分合流与错误零宽修复-2026-09-10.md)。

## 八、2026-09-11零宽端点和物理边界接触复核

- 原路口PID `2023041113170861627`的北侧源链，原TOPO和lane端点`S_LANE_PID/E_LANE_PID`及边界`START_PID/END_PID`共同确认20个事件（18普通延续、2零宽出生）和40对物理边界接触。不是把距离接近的不同ID合并，也不是拟合后的连续性验收。IBD Profile已显式补上述字段映射，其他图商须映射等价语义。
- `2023081117223317273`起宽0：原路径首点局部XY `(6.5060339564,61.1873035822)`，两侧边界汇合点 `(8.2542649773,61.1892034738)`，相距1.7482320533m。
- `2023081117251327605`起宽0：原路径首点局部XY `(-4.1154899034,79.1255561038)`，边界汇合点 `(-6.2895518139,79.0249003237)`，相距2.1763907684m。两例路径首点沿父车道中心，边界汇合于父车道边缘；这说明该端点不能未经角色核定就同时约束为同一个零宽物理中心。
- 仅在两者必须对应同一物理端点的假设下，公共点距两观测均≤0.35m不可能；不据此判断原点错误或全部重建无解。原始点、边界关系和端宽全部保留，未改角色/源数据，绝对CRS仍未核验。
- 当前原件重读一致的证据为`C:\Users\geng\.codex\visualizations\2026\08\21\01a02206-86a5-7041-afef-1cdb390ada9a\mapforge-source-contacts-v150\final`；完整投影管线、原记录号/字段/part在其`input/`，原接触证据在`contact-model.json`，局部图在`source-role-review.png`。复跑及边界见[实施计划v1.50记录](实施计划-整路联动重建与编辑闭环.md)。没有新地图输出。

### v1.51用户裁决（原件事实未改变）

用户已确认仅上述两处以实测左右边界作为物理道路依据，原车道线保留为通行路径观测。`profiles/repair/node4-zero-width-source-roles-v1.yaml`保存该解释，精确绑定复核源版本；不是源数据纠错、不是宣称原路径无效，也不扩展到MAP或其他车道。规则应用于试验联合块，默认转换器未提升。原件快照在项目内另有只读副本`out/source-role-input-v151/`；候选诊断和全部记录见实施计划。

### v1.52原件物理延续分类

按同一原TOPO/SIDE/端点见证去重，40个接触见证形成26对物理关系。含`2023081117182080869`的三叉有1条普通车道边界延续及1条仅零宽渐变接触；含`2023081117255130014`的三叉有2条普通车道边界延续。不能将两处分叉都描述为“两条原车道延续”，也不能从一条零宽通行边反推两个普通laneLink。新`physical_continuations.py`保留全部原证据，不修改源TOPO或ID；G2是项目拟合约束，不是原件已证明G2。

## 九、2026-09-12 node4东侧零宽端点独立原件核查

旧地图road13所关联的完整源域已展开。原TOPO及端节点证据确认29事件：26普通延续、2零宽出生、1零宽消失；没有来源结构缺失。下列事实由`review_source_role_conflicts.py`直接读原SHP/DBF再次确认，非拟合/插值结果。

| 原LANE_PID | 原字段 | 车道线端点到边界收合点 | 边界端点关系 |
|---|---|---:|---|
| 2023041111113051690 | S_WIDTH=0 | 1.768531336m | 左右原点完全重合、原节点ID相同 |
| 2023041111104112823 | S_WIDTH=0 | 2.343631538m | 左右原点完全重合、原节点ID相同 |
| 2023041810460339677 | E_WIDTH=0 | 1.757130184m | 左右原点完全重合、原节点ID相同 |

原始SIDE=1/2、边界BORDER_PID和START_PID/END_PID、原记录号及全点列均保留于`out/node4-east-source-v166/independent-role-review-final/report.json`；原图/端点近景为同目录`raw-source-conflicts.png`。报告含源文件/代码hash，contact内容SHA为`d0ecca3b7554da7f6d257853e3e7ead45a2e408f8e524da5e41ba3172cfb15d3`。局部投影仍不代表绝对CRS已核验。

这些点若都要求表示**同一零宽物理中心**，公共点到双方原观测的必要半径至少为距离一半，即0.884266/1.171816/0.878565m，超过现0.35m。此条件性判断不证明任何图层错误或一般道路重建无解。三ID不在v1.51批准范围，当前仍**未作角色重分类**，也没有生成新XODR。

## 十、2026-09-13 东侧完整端点库存与明确追加批准

第九节是当时状态；前三处随后获得用户批准，本轮直接重读准备包内全部57条原车道的114个端点（road13本身33条，其余为关联支持），确认只有4处显式零宽。新增第四处位于包外端、不在既有29条普通TOPO事件内，原事件内检查因此漏检；这不是第五个转向或新造拓扑。

- 原LANE_PID：`2023061915261945676`，`S_WIDTH=0`，`E_WIDTH=2650`，`WIDTH=1400`，`MAX_SPEED=40`；原Link为`2023061915240377612`。
- 原左右BORDER_PID：`2023061915245016082` / `2023061915245014402`，首节点均为`2023061915241735288`，原坐标完全相同。局部投影边界收合点`(195.346474237, 6.736183056)`；原车道线起点`(195.365014571, 4.873655548)`，距离**1.862619784575m**。
- 用户针对该唯一新增项明确答复 **“同意，仅补这一处”**。四处合并记录为`profiles/repair/node4-east-zero-width-source-roles-v2.yaml`，仅前三处继承加第四处，不扩其他车道/MAP。边界定义物理道路，原车道线完整留存为通行路径另验，不是修改原材料。
- 原完整记录/SIDE/边界节点核查保留于`out/node4-east-source-v166/all-original-endpoints-v167/`，它还明确指出旧编译器漏掉第四处；新准备包`out/node4-east-source-v167/`已覆盖全114端点，content SHA **f45c41aa5fbd53117fe3ab00c3f82a1412b932d9bca81657245c4bcb12b2379b**。原scope/domain及全部原点未改，仍142要素/3178点、24原转向MATCH，绝对CRS仍未核验。
- 原始东側两方向的外端本就不齐（局部x约195m与220m），口部原边界端点也分别位于局部x约22.6m与23.6m；不能为满足写出器共同端面假设删除较长一侧。拟合误差、端部写出和全连接验收属于工程报告，不回写为原件事实。

## 十一、2026-09-14 node4四方向完整原件端点清查（新增三项未批准）

本次一次展开旧地图road10/11/12/13所引用的完整原件及原TOPO关联支持，范围为node4，不是所有SHP地图。`out/node4-all-source-v169/`含120条源车道、171个边界ID、291个完整要素/6290个原始顶点；原路口24条转向核对MATCH。直接pyshp重读120条源车道的首末端，共240个端点；9处显式零宽全部进入编译库存，无新增漏检。77个TOPO过渡事件不是端点总数，不能用事件检查代替外端检查。

其中北侧2处、东侧4处已有用户精确ID批准，本次不重复请求，也不扩大或重新绑定旧决策包。**仅以下西侧2处、南侧1处为新增待裁决项**：

| 道路 | 原LANE_PID / 端部 | 路径点到边界收合点 | 原LANE_LINK记录号（0起） | 左右边界共同原节点 |
|---|---|---:|---:|---|
| 西侧road11 | 2023041111104060474 / start | 1.566211092m | 664 | 2023041110483943210 |
| 西侧road11 | 2023041111104128071 / start | 1.767184577m | 686 | 2023041110480983288 |
| 南侧road12 | 2023041810470164021 / start | 1.828768700m | 727 | 2023041015285999221 |

三项均原`S_WIDTH=0`，不是缺值补零；原SIDE=1/2关系、BORDER_PID、边界节点身份及坐标均由独立原件重读证实。每项左右边界端点同节点且同坐标，原车道路径首点在别处。只有在要求两种点代表同一零宽物理中心的条件下，双方各0.35m预算不相容；不判定哪层错误、不证明一般重建无解。

完整证据：`out/node4-all-source-v169/independent-role-review/report.json`；四向contact内容SHA **2030a2aefb440c203ac4278214f52f9fa55ff492ca4dbc250eeb8c782086c6b4**。新增项汇总及完整原记录/端点放大图见`out/node4-all-source-v169/pending-review-final/`，没有改动原件或拟合曲线。独立西侧准备包SHA **96575b11c50fc6f2bedb849cc46bdc92aed2b744c8acbecdb9deb40668ce132a**，南侧 **1026c4ac08634605382a475785baf4b6b9131cd3404068f867a1d12182bb3f7b**。旧批准只被列出，不能因为合包后hash不同就冒充已迁移批准。

待明确是否仅这三项继续采用“原左右边界定义物理道路，完整原车道线作为通行路径另验”。在新决定前不为road11/12删除或降权这些原路径约束，不把新增角色自动套成已批准；源数据、源速度和绝对CRS状态不变。几何转向122的改进另记工程验收，不能代替本节的来源解释。
